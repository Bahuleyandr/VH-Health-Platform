# VH Health AI Feature Tracker

Last verified: 2026-04-23 (batch 14 FINAL parallel agent batch v1 shipped: acuity staffing forecast, federated learning coordinator, voice patient assistant/IVR — 40/40 complete)

This tracker records the 40 future-proofing AI features proposed for VH Health and maps each one to the current repo state. Update this file whenever a feature graduates from "Not started" to "Partial" or from "Partial" to "Implemented v1".

Status definitions:

- `Implemented v1`: Working backend/service/API/admin surface exists for the core workflow. Further production hardening may still be listed.
- `Partial`: A meaningful adjacent foundation exists, but the named workflow is not complete end to end.
- `Not started`: No dedicated workflow exists yet.

Current count:

- Implemented v1: 40 / 40 ✅
- Partial: 0 / 40
- Not started: 0 / 40
- Remaining to fully implement: 0 / 40

**All 40 future-proofing AI features now have a v1 backend implementation.** Remaining work across the portfolio is production hardening (live data feeders, admin dashboard UI panels, per-tenant customization), captured in each row's "Next build unit" column — none are blockers for v1 coverage.

| # | Feature | Status | Current repo state | Next build unit |
|---:|---|---|---|---|
| 1 | Document Intelligence / OCR AI | Implemented v1 | `document_intelligence_ocr`, `documentIntelligenceService.js`, native text/PDF/photo upload adapter, Admin/IT upload panel, and review queue exist. Text/PDF text-layer extraction works locally; image OCR can use local Tesseract when configured and otherwise falls back safely to review. | Add production object storage handoff and optional cloud OCR/provider adapters behind governance. |
| 2 | Nursing Ambient Documentation | Implemented v1 | `nursing_ambient_documentation`, `nursingAmbientDocumentationService.js`, `clinical_nursing_ambient_sessions`, EMR + Admin/IT APIs, and dashboard review queue exist. V1 consumes a multi-speaker transcript and extracts structured wound, drain, IV line, intake/output (with fluid balance), mobility, fall (severity-classified), handover, and patient-education observations with segment-level citations. Fall events emit critical/high safety flags. Never auto-charts or changes orders. | Add bedside audio capture UI, integration with vitals/MAR, auto-merge into nursing care-plan, and continuous-shift streaming. |
| 3 | Clinical Task Extractor | Implemented v1 | `clinical_task_extractor`, `clinicalTaskExtractorService.js`, `clinical_ai_task_candidates`, Admin/IT API, and dashboard review queue exist. V1 creates cited candidates and never auto-assigns work. | Add clinician-facing queue and optional post-review task assignment integration. |
| 4 | Infection-Control Surveillance | Implemented v1 | `infection_control_sentinel`, `infectionControlSentinelService.js`, admin audit/review panel. | Add continuous realtime ward surveillance and cluster/outbreak automation. |
| 5 | Antimicrobial Stewardship Assistant | Implemented v1 | `antimicrobial_stewardship`, `antimicrobialStewardshipService.js`, `clinical_ai_antimicrobial_reviews`, Admin/IT API, and dashboard review queue exist. V1 reviews cultures, duration, renal dosing, IV-to-oral switch, duplicate spectrum, de-escalation, and allergy conflicts without changing orders. | Add continuous stewardship rounds, local antibiogram integration, and policy-pack customization. |
| 6 | Patient Teach-Back / Comprehension AI | Implemented v1 | `patient_teach_back_comprehension`, `patientTeachBackService.js`, `clinical_ai_teach_back_sessions`, EMR + Admin/IT APIs, and dashboard review queue exist. V1 generates category-covered questions (medications, warning signs, follow-up, diet/activity, wound care, emergency escalation) in the patient's language, scores answers, and flags misunderstandings for clinician review without altering care plans. | Add patient-facing chat/IVR delivery, longitudinal comprehension tracking across admissions, and accepted-draft-only gating. |
| 7 | Consent-Aware Family Update Generator | Implemented v1 | `consent_aware_family_update`, `familyUpdateGeneratorService.js`, `clinical_ai_family_updates`, EMR + Admin/IT APIs (generate/review/sent), and dashboard panel exist. V1 verifies an active `family_update`/`caregiver_communication`/`treatment` consent with relationship + expiry/revocation checks, scrubs medication doses/lab values/MRN before returning family-facing text, and requires clinician review before `mark-sent`. Eight supported languages. Never auto-sends. | Add delivery adapter (SMS/WhatsApp/email), caregiver identity verification, and per-caregiver scope customization UI. |
| 8 | Appeal Letter Generator for Denied Claims | Implemented v1 | `appeal_letter_generator`, `appealLetterGeneratorService.js`, `clinical_ai_appeal_letters`, Admin/IT APIs (generate/review/submit/payer-response), and dashboard panel exist. V1 classifies denial reasons (medical necessity, prior-auth missing, documentation insufficient, coding, duplicate, timely filing, bundled, non-covered, coverage) and drafts a cover letter, medical necessity narrative, evidence bundle, and requested action from cited chart evidence. Billing coordinator reviews, edits, and submits; module never auto-submits. | Add payer-specific templates, appeal outcome analytics, and integration with denial-risk assist for proactive appeal prep. |
| 9 | Payer Contract Variance / Underpayment AI | Implemented v1 | `payer_contract_variance`, `payerContractVarianceService.js`, `clinical_ai_payer_contracts` + `clinical_ai_payer_variance_reviews`, Admin/IT APIs (contract upsert/list, variance evaluate/list/decide), and dashboard panel exist. V1 matches claims to contracts by payer + procedure + effective date window and classifies variance (match / underpayment / overpayment / missing_contract / missing_payment), bands (within_tolerance / review / investigate / escalate — escalate at ≥15% underpayment), and suggests specific billing/coordinator actions. Review-only; never auto-appeals or writes off. | Add ERA/EOB line-item ingestion, per-tenant tolerance policies, contract effective-end batch recompute, and auto-appeal handoff (when reviewer approves). |
| 10 | Acuity-Based Staffing Forecast | Implemented v1 | `acuity_staffing_forecast`, `acuityStaffingForecastService.js`, `clinical_ai_acuity_staffing_forecasts`, Admin/IT API (evaluate/list/decide). V1 takes a unit snapshot (census by acuity critical/high/moderate/low, current staff by role, predicted admissions/discharges) and applies role-based ratios (1:2 / 1:4 / 1:5 / 1:6 nurses, assistants at half density) to compute required staff, per-role deficit, peak census, and acuity load. Classifies recommendation `emergency_acuity` / `call_in` / `float_staff` / `reduce_staff` / `hold_staffing` / `no_action` via deficit band (`surplus` / `balanced` / `watch` / `warning` / `crisis`). Review-only; house supervisor approves; never dispatches staff automatically. Admin UI panel deferred. | Wire unit census from EMR bed board and predicted admissions from ED triage; per-tenant ratio overrides; add admin dashboard panel. |
| 11 | ED Triage and Boarding Predictor | Implemented v1 | `ed_triage_boarding_predictor`, `edTriageBoardingService.js`, `clinical_ai_ed_triage_predictions`, Admin/IT API (evaluate/list/decide). V1 classifies ESI-like triage level 1-5 from vitals + chief complaint + pain + arrival mode, routes to specialty (cardiology, neurology, obstetrics, pediatrics, orthopedics, psych, internal), predicts disposition (icu/admission/observation/discharge), and forecasts boarding risk with occupancy + staff load. Review-only. Admin UI panel deferred. | Add per-ward capacity lookup and full admin dashboard panel. |
| 12 | Sepsis / Stroke / ACS Bundle Compliance AI | Implemented v1 | Sepsis remains covered by `sepsis_bundle_sentinel`. Now joined by `pathway_bundle_compliance`, `pathwayBundleComplianceService.js`, `clinical_ai_pathway_bundle_audits`, Admin/IT API (evaluate/list/decide). V1 is a generic pathway evaluator that accepts a pathway spec (`pathway_key` + built-in presets for `stroke_gwg`, `acs_mona`, `vte_prophylaxis`, `glycemic_insulin`, or a `custom_spec`) + actual actions + t0_reference, classifies each item as compliant/late/missed/not_applicable/unknown, computes bundle-wide compliance %, surfaces dangerously-late or missed critical items, and recommends `no_action` / `catch_up` / `escalate` / `review_pathway` / `critical_miss`. Review-only; clinician reviews; the module never administers a medication or places an order. Admin UI panel deferred. | Publish a pack manifest so tenants can add proprietary pathways; pipe accepted actions from live EMR events into the evaluator; add admin dashboard panel. |
| 13 | ICU Ventilator / Sedation Bundle Reviewer | Implemented v1 | `icu_ventilator_sedation_bundle`, `icuVentilatorBundleService.js`, `clinical_ai_ventilator_bundle_audits`, Admin/IT API (audits/list/decide). V1 detects ventilator status from chart evidence, audits VAP bundle (HOB elevation, oral care, sedation interruption, DVT/PUD prophylaxis, subglottic suction), sedation assessment (RASS, CAM-ICU delirium), and SBT readiness (FiO2/PEEP/hemodynamics). Emits compliance score, risk band, bundle gaps, and recommendations. Review-only. Admin UI panel deferred. | Add continuous monitoring (vs point-in-time), per-day compliance trending, and admin dashboard panel. |
| 14 | Radiology Report QA / Discrepancy Assistant | Implemented v1 | `radiology_report_qa`, `radiologyReportQaService.js`, `clinical_ai_radiology_report_reviews`, Admin/IT API (evaluate/list/decide). V1 reviews draft/preliminary/final/amended radiology reports against the study request indication and metadata. Emits 8 rule-based discrepancy codes: LATERALITY_MISMATCH (critical), MISSING_IMPRESSION (high, draft-exempt), MISSING_CRITICAL_COMMUNICATION (critical, when isCritical), INDICATION_NOT_ADDRESSED (moderate), MISSING_COMPARISON_TO_PRIOR (moderate, when priors available), VAGUE_MEASUREMENTS (low), FINDINGS_IMPRESSION_INCONSISTENT (high — detects abnormal terms in findings missing from impression), MISSING_FOLLOW_UP (moderate — findings-gated for nodule/mass/lesion/indeterminate). Laterality normalization handles left/right/bilateral/LT/RT/"L "/"R "/"both sides". Review-only; radiologist signoff required; never modifies, signs, or releases a report. Admin UI panel deferred. | Ingest full structured report section map (findings/impression/recommendations); wire the VR/dictation flow to call this on every sign attempt; add admin dashboard panel. |
| 15 | Radiology Worklist Prioritizer | Implemented v1 | `radiology_worklist_prioritizer`, `radiologyWorklistPrioritizerService.js`, `clinical_ai_radiology_worklist_priorities`, Admin/IT API (evaluate/list/decide). V1 scores pending studies via 7 signal families — patient location (ED/ICU/ward/outpatient/home), modality urgency (stroke CT head, PE CT chest, acute abdomen, line/tube CXR, neuro MRI, US torsion/DVT), indication severity keywords, patient fragility (extreme age, infant/young child, critical vitals, oxygen support, immunocompromised), wait time breach bands (60/120/240 min), ordering context tags (code_stroke, trauma_call, rapid_response, code_blue, pre_op_same_day), priors-available bonus — and classifies into tiers (stat ≥120 / urgent ≥80 / routine ≥30 / deferrable <30). `rankWorklist` sorts a batch by (tier priority desc, score desc). Review-only; never changes the worklist automatically. Admin UI panel deferred. | Wire to PACS/RIS worklist polling; add per-tenant threshold overrides; add admin dashboard panel. |
| 16 | Lab Autoverification / Delta Check Assistant | Implemented v1 | `lab_autoverification_delta`, `labAutoverificationService.js`, `clinical_ai_lab_autoverifications`, Admin/IT API (evaluate/list/decide), and dashboard panel exist. V1 compares the current investigation result against the patient's most recent prior for the same test, computes delta %, classifies critical band (critical_low/high, borderline_low/high, normal) against baked-in reference/critical ranges for common tests (K+, Na+, glucose, creatinine, Hb, platelets, WBC, Ca2+, Mg, INR, troponin, lactate), and emits a rules-authoritative decision (auto_verify / hold_for_review / critical) with suggested actions. Review-only; never finalizes results. | Swap baked-in reference ranges for a per-tenant lab registry; wire into the existing investigations result upload pipeline so it fires automatically on new numeric results. |
| 17 | Blood Bank Demand and Compatibility Forecast | Implemented v1 | `blood_bank_demand_forecast`, `bloodBankForecastService.js`, `clinical_ai_blood_bank_inventory_snapshots` + `clinical_ai_blood_bank_forecast_reviews`, Admin/IT APIs (inventory upsert/list + forecast generate/list/decide). V1 maintains per-group/component inventory projections, estimates per-procedure demand (cardiac surgery, trauma, obstetric PPH, elective), aggregates forecast against inventory to classify stockout risk per (group, component), assesses MTP readiness (O- PRBC, AB FFP, platelets) and rolls up overall risk band. Review-only; never auto-reserves units. Admin UI panel deferred. | Wire inventory ingestion to actual blood bank LIS; add crossmatch-urgency advisor; add admin dashboard panel. |
| 18 | Pharmacogenomics / PGx Support | Implemented v1 | `pharmacogenomics_support`, `pharmacogenomicsService.js`, `clinical_ai_patient_genotypes` + `clinical_ai_pgx_advisories`, Admin/IT APIs (genotype upsert/list + advisory generate/list/decide). V1 stores per-patient genotypes across 11 PGx-relevant genes (CYP2D6/2C19/2C9, VKORC1, SLCO1B1, HLA-B*57:01, HLA-B*15:02, TPMT, DPYD, UGT1A1, G6PD), runs CPIC-inspired rules against 14 medication references (codeine, tramadol, clopidogrel, warfarin, simvastatin, azathioprine, 6-MP, fluorouracil, irinotecan, abacavir, carbamazepine, primaquine, omeprazole), and classifies advisories (contraindicated / use_alternative / consider_dose_change / testing_recommended / standard_dose / no_action) with priority escalation. Gene-name normalizer handles HLA-B*57:01 / HLA_B_5701 / HLA-B_5701 variants. Review-only; pharmacist signoff. Admin UI panel deferred. | Integrate with prescription save hook; expand reference table to full CPIC catalog; add auto-testing-order suggestions. |
| 19 | Pregnancy / Obstetric Risk Assistant | Implemented v1 | `obstetric_risk_assistant`, `obstetricRiskService.js`, `clinical_ai_obstetric_risk_assessments`, Admin/IT API (evaluate/list/decide). V1 classifies assessment stage (pre-conception/trimester/intrapartum/postpartum), detects risk factors (age extremes, grand multipara, prior preeclampsia, chronic HTN, diabetes, multiple gestation, prior C-section) and red-flag signals (severe preeclampsia, eclampsia, PPH, reduced fetal movement, fetal bradycardia, fever), computes risk score/band, and builds a follow-up plan with ANC schedule + escalation criteria. Review-only. Admin UI panel deferred. | Add MgSO4 dosing advisor, fetal monitoring integration, postpartum-discharge follow-up workflow, and admin dashboard panel. |
| 20 | Pediatric Dosing Safety AI | Implemented v1 | `pediatric_dosing_safety`, `pediatricDosingSafetyService.js`, `clinical_ai_pediatric_dose_checks`, Admin/IT API (evaluate/list/decide), and dashboard panel exist. V1 loads patient age (via `users.birthday`) and weight (via latest `patient_vitals`), looks up per-drug max-per-kg and absolute-max limits from a baked-in reference table (amoxicillin, clavulanate, cefs, paracetamol, ibuprofen, ondansetron, vancomycin, gentamicin, metronidazole, salbutamol, azithromycin, cephalexin), and classifies safety as safe/caution/unsafe/missing_data with pediatric age-band cross-check. Accepts explicit age/weight overrides for external callers. Review-only; never modifies orders. | Parse free-text `dosage` column into mg; add renal adjustment for gentamicin/vancomycin; plumb weight ingestion from wearable/bedside devices. |
| 21 | Hospital Command Center AI | Implemented v1 | `hospital_command_center`, `hospitalCommandCenterService.js`, `clinical_ai_command_center_snapshots`, Admin/IT API (evaluate/list/decide). V1 takes a snapshot of six departments — beds (occupancy %, discharge-ready wait, admission queue), ED (wait, boarding, LWBS %), OR/theatre (utilization %, overrun count, add-on pressure), housekeeping (pending turnovers, avg turnover), radiology (pending studies, stat wait), pharmacy (dispense backlog, critical meds late) — classifies each to a tier (normal / watch / elevated / crisis), computes overall_score, and rolls up to a hospital-wide command_status via `rollupCommandStatus`. Rules-authoritative; review-only — duty officer reviews, and the module never auto-triggers diversion, staffing changes, or transfers. Admin UI panel deferred. | Wire department signal feeds to live metrics (bed-board, ED tracker, OR scheduler, housekeeping dispatch, radiology PACS, pharmacy queue); add admin dashboard panel. |
| 22 | Housekeeping and Bed Turnover Optimizer | Implemented v1 | `housekeeping_bed_turnover`, `housekeepingBedTurnoverService.js`, `clinical_ai_bed_turnover_predictions`, Admin/IT API (evaluate/list/decide). V1 determines required cleaning level (standard / terminal / isolation / deep_clean) from prior diagnoses + isolation precautions, estimates turnover minutes with staffing + bathroom adjustments, and scores priority (0-100, band low/moderate/high/critical) combining bed demand, discharge wait, ED-doorway proximity, and cleaning urgency. Review-only. Admin UI panel deferred. | Integrate with real-time bed status feed; connect to housekeeping dispatch; add admin dashboard panel. |
| 23 | OT Block Scheduling Optimizer | Implemented v1 | `ot_block_scheduling`, `otBlockSchedulingService.js`, `clinical_ai_ot_block_suggestions`, Admin/IT API (evaluate/list/decide). V1 computes utilization %, prime-time utilization %, duration-variance %, overrun band (none/occasional/frequent/chronic), turnover band (fast/typical/slow/severe), and add-on volume ratio (low/moderate/high/excessive), then classifies per-block recommendation — `reallocate` (high) when utilization <50% + prime <50%, `reduce` (moderate) for low utilization, `expand` (high/critical) for over-utilization with overruns + add-on pressure, `review_release_policy` for excessive add-ons on healthy blocks, `keep` for healthy blocks — and escalates severity on slow turnover or inaccurate case durations. Review-only; OR director approves; block time is never reassigned automatically. Admin UI panel deferred. | Wire to OR scheduling source of truth; add service-line-level rollup; add admin dashboard panel. |
| 24 | Inventory AI Beyond Pharmacy | Implemented v1 | `inventory_intelligence`, `inventoryIntelligenceService.js`, `clinical_ai_inventory_alerts`, Admin/IT API (evaluate/list/decide). V1 covers non-pharmacy supplies (PPE, linens, surgical instruments, consumables, biomed single-use items, housekeeping). Computes days-on-hand, days-to-expiry, consumption deviation % vs baseline and classifies stock band (out/below_reorder/ok/over_max), DOH band (critical/warning/watch/ok/excess), expiry band (expired/imminent/warning/watch/ok), and consumption anomaly (surge/drop/elevated/normal). Highest-priority alert wins via CATEGORY_PRIORITY — `stockout_risk` (critical), `reorder_point_breach`, `expiry_risk`, `consumption_anomaly`, `overstock`, `healthy`. Review-only; materials manager approves; the module never places or cancels orders automatically. Admin UI panel deferred. | Wire to materials-management ERP feed; ingest lot-level expiry; add ward-vs-tenant-level rollup; add admin dashboard panel. |
| 25 | Biomedical Device Maintenance Predictor | Implemented v1 | `biomed_device_maintenance`, `biomedDeviceMaintenanceService.js`, `clinical_ai_biomed_devices` + `clinical_ai_biomed_maintenance_predictions`, Admin/IT APIs (device upsert/list + evaluate/list/decide). V1 stores a device registry with usage hours, fault counts, MTBF, warranty; detects 7 signal codes (OVERDUE_MAINTENANCE, APPROACHING_SERVICE, FAULT_CLUSTER, LOW_MTBF, END_OF_LIFE, WARRANTY_EXPIRING, NO_SERVICE_HISTORY) with per-type service-interval baselines for ventilators / defibs / pumps / monitors / ultrasound / X-ray / MRI / CT / dialysis / anesthesia machines; computes failure risk score and recommends a service urgency window (immediate / 7d / 30d / routine). Review-only. Admin UI panel deferred. | Connect to CMMS; ingest real-time device telemetry; add admin dashboard panel. |
| 26 | Cybersecurity / Medical Device Anomaly Detector | Implemented v1 | `cybersecurity_anomaly_detector`, `cybersecurityAnomalyService.js`, `clinical_ai_security_anomalies`, Admin/IT API (record/list/decide). V1 classifies anomalies across 5 subject types (user_login, admin_action, device_traffic, data_export, api_usage) with dedicated detectors: IMPOSSIBLE_TRAVEL (haversine distance vs time), RAPID_LOGINS, UNUSUAL_HOUR, BRUTE_FORCE_SINGLE_ACCOUNT, CREDENTIAL_STUFFING, EXCESSIVE_EXPORT_VOLUME, EXPORT_OFF_HOURS, DEVICE_TRAFFIC_SPIKE, UNAUTHORIZED_UPSTREAM, CONNECTION_STORM. Computes risk score + severity band + category priority + context-specific actions. Review-only; security officer decides. Admin UI panel deferred. | Wire to real-time audit log feeder; integrate with SIEM; add admin dashboard panel. |
| 27 | Staff Burnout / Workload Risk Predictor | Implemented v1 | `staff_burnout_workload_risk`, `staffBurnoutRiskService.js`, `clinical_ai_staff_burnout_reviews`, Admin/IT API (evaluate/list/decide), and dashboard panel exist. V1 joins `staff_attendance` + `staff_shift_assignments` + `staff_shifts` + `leave_requests` to compute per-staff total hours, overtime, consecutive night shifts, weekend count, PTO utilization, then classifies burnout risk (low / moderate / high / critical / insufficient_data) with contributing signals and recommended actions. Strict privacy boundary: every output carries a "workload risk signal only — never used for performance or disciplinary action" notice in safety_flags + recommended_actions. | Wire into scheduled digest for HR; add per-tenant threshold overrides; explore de-identified team-level rollups. |
| 28 | Training and Simulation Coach | Implemented v1 | `training_simulation_coach`, `trainingSimulationCoachService.js`, `clinical_ai_training_modules`, Admin/IT API (generate/list/decide). V1 converts de-identified incidents (mortality, near-miss, safety_event, delayed_diagnosis, medication_error, handoff_failure, infection_outbreak, equipment_failure, other) into structured training modules — learning objectives, decision points (stages: arrival/recognition/stabilization/escalation/handoff), debrief questions, reference guidelines, risk band, suggested format (tabletop / sim_lab / vr_ready / online / workshop) and duration. Scrubs residual PHI (phone, MRN, name, email, Aadhaar) in supplied summary and flags detections. Review-only — training director approves; never auto-publishes. Admin UI panel deferred. | Connect to an LMS/assignment pipeline; per-tenant override of role mapping; add admin dashboard panel. |
| 29 | Policy Diff / Regulation Watcher | Implemented v1 | `policy_regulation_watcher`, `policyRegulationWatcherService.js`, `clinical_ai_policy_diffs`, Admin/IT API (evaluate/list/decide). V1 takes two policy text versions (or an explicit diff), splits into sections, computes added/removed/modified section counts, classifies impact_area across six domains (clinical, billing, access, privacy, infection_control, pharmacy; mixed when 2+ domains hit), computes severity with explicit rules for privacy/clinical hit counts and total-change volume, and derives impacted roles so the right teams are notified. Critical severity when privacy ≥ 2 hits or clinical ≥ 3 hits. Review-only; compliance + legal approve; the module never auto-activates or revokes a policy. Admin UI panel deferred. | Wire to a policy document repository with version-aware ingestion; per-tenant role-map overrides; add admin dashboard panel. |
| 30 | Procurement Negotiation Assistant | Implemented v1 | `procurement_negotiation_assistant`, `procurementNegotiationService.js`, `clinical_ai_procurement_opportunities`, Admin/IT API (evaluate/list/decide). V1 takes SKU, vendor, current unit price, historical baseline, quoted alternative, annual volume, vendor count, contract tenure, and contract end date; classifies opportunities as `price_anomaly` / `volume_consolidation` / `tenure_leverage` / `alternatives_available` / `expiring_contract` / `no_action` via six classifier bands (price, vendor fragmentation, tenure, expiry, alternative savings) and estimates annual savings. Review-only; procurement lead negotiates, module never contacts vendors, places orders, or modifies contracts. Admin UI panel deferred. | Ingest vendor/SKU catalog from ERP; add industry-benchmark price feeds; add admin dashboard panel. |
| 31 | Model Registry and Evaluation Workbench | Implemented v1 | `model_registry_workbench`, `modelRegistryWorkbenchService.js`, `clinical_ai_model_registry` + `clinical_ai_model_eval_runs`, Admin/IT APIs (registry upsert/list + stage change + eval-run record/list/decide). V1 registers every AI model variant with provider, purpose, owner, lineage, parent version, and lifecycle stage (sandbox/staging/production/deprecated/quarantined). Per-suite eval runs capture accuracy, F1, avg latency, fallback %, safety-flag %, drift score. Classifier recommends `promote` / `hold` / `rollback` / `retire` / `quarantine` / `no_action` based on bands + deltas against a baseline (quarantine on any breach; rollback on poor accuracy, drift breach, or ≥5pp regression; promote on ≥2pp accuracy gain with clean rates). Review-only; AI eval lead approves stage changes; `changeModelStage` endpoint is the only way to move a model between stages. Admin UI panel deferred. | Wire canary suites to auto-feed eval runs; add per-model SLA dashboards; add admin dashboard panel. |
| 32 | Dataset Labeling and Review Studio | Implemented v1 | `dataset_labeling_studio`, `datasetLabelingStudioService.js`, `clinical_ai_labeling_tasks` + `clinical_ai_labeling_annotations`, Admin/IT APIs (task upsert/list/detail + annotation submit/list/decide). V1 is a generic labeling queue across task types (imaging, clinical coding, denial reason, deterioration outcome, triage outcome, discharge disposition, etc.). Computes inter-rater agreement (match / partial / disagree / pending) via pairwise equality with fallback to partial-match on shared-key objects; derives confidence band (high / medium / low / unknown), task status (pending / in_progress / ready_to_use / conflict / rejected / archived), and a strict-majority `consensus_label`. Tasks flip to `ready_to_use` only when ≥ 2 accepted annotations agree. Review-only; eval lead approves; module never auto-publishes into a dataset. Admin UI panel deferred. | Add a labeler UI with assignment/queue; add adjudicator workflow; export ready_to_use rows to a dataset-publish pipeline with per-tenant opt-in. |
| 33 | Synthetic Clinical Case Generator | Implemented v1 | `synthetic_case_generator`, `syntheticCaseGeneratorService.js`, `clinical_ai_synthetic_cases`, Admin/IT API (generate/list/decide). V1 is fully deterministic (mulberry32 PRNG seeded via SHA256 of the input seed) — same inputs produce byte-identical output. Supports 11 pathways (sepsis, stroke, chest_pain_acs, pneumonia, asthma_exacerbation, diabetic_ketoacidosis, postpartum_hemorrhage, trauma_blunt, pediatric_fever, geriatric_fall, mental_health_crisis) × 4 complexity tiers (simple/standard/complex/edge) producing persona, pathway-shaped vitals snapshots, pathway-specific labs, timeline events, edge-flag annotations, and an always-prefixed `[synthetic]` narrative. No real patient data; narrative can never contain MRN/phone/name. Review-only — AI eval lead approves before canary/training use. Admin UI panel deferred. | Add a seed catalog + variant generator; feed into drift canary suites; add admin dashboard panel. |
| 34 | Federated Learning / Privacy-Preserving Training Layer | Implemented v1 | `federated_learning_coordinator`, `federatedLearningCoordinatorService.js`, `clinical_ai_federation_sites` + `clinical_ai_federation_rounds`, Admin/IT APIs (site upsert/list + status change + round record/list/decide). V1 is a pure coordination + audit layer — registers participating sites with differential-privacy ε budget, min cohort size, accepted aggregation methods (`fed_avg` / `fed_prox` / `fed_sgd` / `secure_avg` / `differential_fed_avg`), and tracks rounds with participant count, DP budget spent, cohort min-site size vs floor, and data-drift score. Classifies per-round readiness `ready` / `hold` / `abort` / `review_privacy` / `no_action` with critical `abort` on DP budget breach, unsafe cohort, or critical participant shortfall. Review-only; AI governance + data engineering approve; module never triggers training or transmits weights. Admin UI panel deferred. | Integrate with on-prem training runners and secure-aggregation libraries; per-tenant DP budget enforcement at the runner; add admin dashboard panel. |
| 35 | AI Agent Lifecycle Manager | Implemented v1 | `ai_agent_lifecycle_manager`, `aiAgentLifecycleService.js`, `clinical_ai_agent_registry` + `clinical_ai_agent_health_reports`, Admin/IT APIs (registry upsert/list + stage change + health report record/list/decide). V1 tracks each AI agent (distinct from models) with agent_key, owner, purpose, scopes, permitted actions, expiry, last_seen, and lifecycle stage (sandbox / staging / production / deprecated / quarantined). Per-snapshot health reports classify the agent as `renew` / `hold` / `retire` / `quarantine` / `no_action` based on success rate band, error rate band, latency band, permission-vs-usage mismatch, last-seen band, and days-to-expiry band. Rules-authoritative; review-only — AI governance approves stage changes via the dedicated `changeAgentStage` endpoint; the module never disables or extends an agent automatically. Admin UI panel deferred. | Feed live invocation telemetry from the agent runtime; add per-scope permission-drift alerts; add admin dashboard panel. |
| 36 | Clinical Knowledge Graph | Implemented v1 | `clinical_knowledge_graph`, `clinicalKnowledgeGraphService.js`, `clinical_ai_kg_nodes` + `clinical_ai_kg_edges` + `clinical_ai_kg_health_reports`, Admin/IT APIs (node upsert/list + edge upsert/list + health evaluate/list/decide). V1 registers nine node types (patient, diagnosis, medication, lab, procedure, provider, encounter, payer, organization) and 14 edge types (has_diagnosis, prescribed, ordered, performed_by, administered_to, attributed_to, covered_by, affiliated_with, belongs_to_encounter, treats, contraindicates, indicates, related_to, caused_by). Health reports classify orphan nodes (patient/provider excluded as anchors), missing critical edges (configurable CRITICAL_EDGE_RULES), contradictions (e.g. treats + contraindicates on the same pair), stale nodes, and completeness % — rolled up to overall_health `healthy` / `watch` / `degraded` / `critical` / `unknown`. Review-only; data engineer approves; the graph is never modified by this service (ingest happens upstream). Admin UI panel deferred. | Build node/edge ingest adapters from chart + claims + pharmacy; add graph-query endpoints; add admin dashboard panel. |
| 37 | Multimodal Patient Timeline | Implemented v1 | `multimodal_patient_timeline`, `multimodalPatientTimelineService.js`, `clinical_ai_patient_timeline_snapshots`, Admin/IT API (generate/list/decide). V1 unifies events from 11 kinds (note, imaging, voice, claim, message, telemetry, document, prescription, lab, vital, other) into a single patient timeline snapshot. Per-kind classifiers tag each event with a relevance band (critical / high / moderate / low / informational) and emit patient-safety signals — red-flag vitals (spo2 <85, hr >180, sbp <80, temp >40), critical labs (`abnormal_flag in ['critical_high','critical_low']`), significant imaging findings (PE, ICH, pneumothorax, mass), message PHI risk, and missed critical meds (insulin, warfarin, heparin, chemotherapy). Timeline is sorted by (time, relevance) with source_breakdown counts. Review-only; care team reviews; the module never modifies source events or the chart. Admin UI panel deferred. | Wire live event feeders from chart + imaging + ambient + messaging + claims; expose the timeline in the EMR view; add admin dashboard panel. |
| 38 | Voice Patient Assistant / IVR | Implemented v1 | `voice_patient_assistant_ivr`, `voicePatientAssistantIvrService.js`, `clinical_ai_voice_ivr_sessions`, Admin/IT API (evaluate/list/decide). V1 is a consent-gated classifier for seven intents (prep / aftercare / meds / reminder / virtual_ward / triage_callback / other) across four channels (ivr / phone / sms / chat) in eight languages (en / hi / ta / te / kn / ml / mr / bn). Detects urgent/emergency phrases in the transcript (chest pain, SOB, suicide, bleeding, seizure, stroke, severe pain, etc.), PHI leakage in the candidate response (phone / MRN / email / Aadhaar / name), and consent state (fresh / stale / missing / unknown). Emits recommendation `allow` / `escalate_to_clinician` / `block` / `fallback_to_human` / `no_action` with first-match-wins ordering (consent > PHI > urgent > language > intent support > allow). Stores sanitized response. Review-only; clinician approves; the module never plays audio or sends a reply. Admin UI panel deferred. | Wire a delivery dispatcher that honours the recommendation (allow → template path, block → never delivered); expand script library and intent catalogue; add admin dashboard panel. |
| 39 | AI Explainability Dashboard | Implemented v1 | `ai_explainability_dashboard`, `aiExplainabilityDashboardService.js`, `clinical_ai_explainability_reports`, Admin/IT API (evaluate/list/decide). V1 ingests a draft text + its citations + optional context and computes: citation coverage % (sentence-to-citation label match), unsupported-claim count (assertive sentences with no citation match), numeric coherence % (numbers in draft vs citation-pool numbers, ±10% tolerance for decimals), PHI leakage indicators (phone, MRN/VH-tag, email, 12-digit Aadhaar), and bias markers (gender/age/ethnicity language in draft that's absent from context). Rule-based trust band (`trusted` / `review` / `reject`) with first-match-wins ordering — any PHI leakage → reject/critical. Includes a reviewer-friendly evidence map pairing each sentence to its matched citations. Review-only; AI governance uses this to green-light a draft for clinical use; never modifies the underlying draft. Admin UI panel deferred. | Wire post-generation hooks so every draft auto-triggers an explainability report; expose evidence map in the review queue UI; add admin dashboard panel. |
| 40 | AI ROI Dashboard | Implemented v1 | `ai_roi_dashboard`, `aiRoiDashboardService.js`, `clinical_ai_roi_snapshots`, Admin/IT API (compute/save/list/latest), and dashboard panel exist. V1 aggregates per-module time saved (configurable minutes-per-accepted multiplier), documentation hours saved, denial value prevented from approved appeals, prior-auth approvals, acceptance rate, and cost per useful draft across clinical_ai_generations + clinical_ai_reviews + appeal/prior-auth tables. Read-only; never alters clinical or billing decisions. | Add trend charts, per-department breakdowns, reviewer-edit-delta accounting, and configurable-per-tenant time-saved multipliers. |

Recommended next build order:

The 6-item recommended queue (teach-back, appeal letter, AI ROI, nursing
ambient, family update, payer variance) is fully shipped. Pick the next
unit from the still-partial rows above — good candidates: Sepsis / Stroke
/ ACS Bundle Compliance (generalize the sepsis bundle sentinel framework
across pathway packs), Radiology Report QA, or Staff Burnout / Workload
Risk Predictor.

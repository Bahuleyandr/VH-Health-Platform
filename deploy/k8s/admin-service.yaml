apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: vhhealth
  labels:
    app: admin-portal
spec:
  type: ClusterIP
  selector:
    app: admin-portal
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP

# Goal — 2026-06-16: All 11 journeys green, zero critical/high in-flight

**Set:** 2026-05-16  **Target:** 2026-06-16 (1 month)

## Definition of done

1. Every one of the 11 swarm journeys passes end-to-end on a fresh `vhhealth_test` DB, on the latest `main`, under the API surface.
2. `findings/in-flight/` contains **0 critical and 0 high** severity findings. Medium/low may remain.
3. The "green" state holds for at least 3 consecutive swarm ticks (no flaky regressions).

This is the gate before any pilot-hospital deploy conversation. Until it holds, no new feature work merges to `main` outside the bugs the swarm surfaces.

## Baseline (2026-05-16, swarm tick `befe`)

### Journey pass rate (last ~13 ticks)

| # | Journey | Best state | Most common failure | Blocking root cause |
|---|---|---|---|---|
| 1 | `cross-tenant-rls` | not yet attempted | — | unknown — needs first run |
| 2 | `dynamic-acute-abdomen` | 2 ok / 11 fail | receptionist step 0 | empty-test-db (now fixed upstream) |
| 3 | `emergency-walk-in` | 1 ok / 11 fail | receptionist step 0; 1× admission | empty-test-db, ED triage tables missing |
| 4 | `follow-up-opd` | 2 ok / 12 fail | receptionist step 0 | empty-test-db |
| 5 | `inpatient-admission` | 1 ok / 12 fail | receptionist step 0 | empty-test-db |
| 6 | `lab-walk-in` | 1 ok / 12 fail | receptionist step 0 | empty-test-db + lab tables |
| 7 | `obstetric-anc` | 1 ok / 12 fail | receptionist step 0 | empty-test-db |
| 8 | `pediatric-opd` | 1 ok / 12 fail | receptionist step 0 | empty-test-db |
| 9 | `surgical-day-care` | 1 ok / 12 fail | admission step | OT case auto-create + admission flow |
| 10 | `tpa-insurance-claim` | 1 ok / 12 fail | admission step; 1× billing (missing preauth state) | preauth flow gaps |
| 11 | `walk-in-opd` | 1 ok / 12 fail | receptionist step 0 | walk-in 500 + token_number type mismatch |

### Findings in-flight by severity

- **critical: 8**
- **high: 54**
- medium: 32
- low: 4

### Top blocking criticals (top 5 from latest curation queue)

1. `POST /api/v1/prescriptions/create` always returns 500 — RETURNING references missing columns
2. Phone normalisation mismatch — patient logs into empty ghost account
3. `POST /api/v1/appointments/walk-in` still 500s under ADMIN JWT
4. Walk-in registration still 500s on main — `token_number` int queried as text
5. ED triage subsystem unusable — `emergency_visits` and `triage_assessments` tables don't exist

## Tracker

Updated end-of-day from `runs/latest-curation-queue.md` and swarm tick artifacts. Each row gets a state per check-in. Don't edit history — append a new dated snapshot below the journey grid.

### Snapshot — 2026-05-16

(See baseline above.)

### Snapshot — YYYY-MM-DD

| Journey | State | Δ from last | Next action |
|---|---|---|---|

## Workflow

- Swarm runs continuously on dalekdefender (`tmux attach -t vh-swarm`).
- `SWARM_GATE_ALLOW_ON_APPROVALS=true` keeps it iterating overnight as long as ≥1 approval/merge happens per tick.
- `SWARM_AUTO_MERGE_SEVERITIES=low,medium` (default) — high/critical fixes still require human merge.
- Weighted journey scheduling biases toward incomplete journeys (post-flywheel upgrade May 15).
- Daily: skim the curation queue, merge any approved-but-deferred branches, update this tracker.

## Out of scope until goal hits

- Deployment runbooks / pilot hospital hardening
- Clinical AI rollout phases (`docs/CLINICAL_AI_ROLLOUT_PLAN.md`)
- Multi-tenant / multi-region work
- New feature surface — only bugs the swarm surfaces

Once the journeys hold green for 3 ticks, these unblock in priority order.

# Unified Care Pathways S1b-b — Implementation Plan

**Design:** `docs/superpowers/specs/2026-07-19-unified-care-pathways-s1b-b-pathway-execution-spine-design.md`
**Base:** `df4dba1c3e80d503397b37195516ede638d2dc53`
**Branch:** `feat/care-pathways-s1b-execution-spine`

## Scope guard

Land one dormant, default-off runtime slice. Do not ship a production or tenant pathway-definition seed, commit an active definition, register clinical handlers, change projector generation, add a scheduler, flip tenants, introduce pathway notifications, expose patient pathway state, modify Stroke/STEMI/OBGyn domain behavior, or encode D3–D7 or an external clinical/operational policy. The disposable comprehensive-test seeder may transiently activate one synthetic definition inside its transaction solely to satisfy the post-584 pinned-runtime conformance graph; it must retire that definition before commit and fail closed if any synthetic definition remains active. Preserve existing lab safety fan-out only after its clinical obligation commits. D3-D7 remain unsigned engineering recommendations, not adopted clinical/governance defaults; generic handoff fields, visibility-policy references, and child-relationship vocabulary are technical substrate only, and the reserved `ownership_transferring` value remains rejected until D6 is signed. Do not invent recipient policy, timings, thresholds, clinical defaults, or policy-resolution behavior. S1b-c reconciliation evidence, recovery tooling, code-reviewed allowlisted atomic domain-write capabilities for actions, and the production activation path remain mandatory follow-up work; S1b-b conformance tests and disposable seed data are not activation evidence.

## Work items

1. Add migration 580 with fail-closed preflights, same-run graph constraints, typed task/SLA and materialisation keys, four companion tables, indexes, pathway-transition-ledger append-only enforcement, deferred governed-run pairing, and Pattern-A RLS. Block active orphaned, malformed, cross-tenant, unknown, contradictory, source-mismatched, or lifecycle-inconsistent legacy SLA claims unless exact clinical evidence supports reconciliation; allow only explicit recognized policy-missing degraded contracts, and never delete a task/SLA or silently detach a link to make migration pass. A bounded pre-580 corrected-result chain may use matching `reopened_from_task_id`, reciprocal lineage, and the unique legacy `reopen_history` snapshot to prove chronology and preserve the overwritten predecessor deadline, but neither legacy metadata nor comments become the final acknowledgement authority. Reject comment-only, caller-authored/forged, ambiguous, cyclic, over-bound, cross-tenant/patient/SLA/resource, no-pointer, and stale-`completed_by_task` shapes. Never infer generation one or copy the current rearmed SLA deadline backward. Retain equal `metadata.sla_instance_id`/`metadata.sla_key` aliases through the two-release rolling bridge with task-row promotion and mirroring. Add the exact marker-free legacy materialisers for `critical_result_ack` and `mortuary_unclaimed_body`; reject dormant, ambiguous, or unsupported sources rather than guessing work or ownership. Atomic new writers carry `task_materialization_contract='application_atomic_v1'` and create their richer task. Make old PR #587 SLA rearm crash-atomic and require zero pending successor generations. Treat all migration-580 compatibility promotion as narrow proof-preserving cutover behavior, never general legacy auto-repair.
2. Before deployment, run `lab:audit-care-pathway-spine-readiness` and `lab:audit-ingest-582-584-readiness` against the primary as an all-tenant privileged actor in their explicit acknowledged repeatable-read/read-only modes, then complete clinical/operational evidence reconciliation of every blocked row. Only after clean reports and a clean PostgreSQL 18 production-clone rehearsal, follow `apps/backend/docs/RUNBOOKS/lab-oru-582-maintenance-cutover.md`: drain every old writer; require the exact pending set `[580, 581, 582, 583, 584]`, or `[582, 583, 584]` only with tracker proof that 580 and 581 landed together in a documented compatible release; apply the exact allowed set; run migration-581 reconciliation with `--old-replicas-drained`; repeat post-schema audits; and resume only on clean evidence. Reject a partial prerequisite state, an omitted 584, or a claim that 584 is already installed while 582/583 remain pending. Migration 580 repeats compatibility, lineage, lifecycle, and receipt checks under the task-table lock to close the opening-preflight race. Remove compatibility aliases/triggers only after later drain evidence. Promote old-PR-#607 acknowledgement state only from the exact same-tenant authenticated receipt, preserving submitted values and task-then-SLA lock order; leave malformed/mismatched state unchanged for deferred validation. Post-581, only the immutable `lab_critical_alert_acknowledgement_receipts` row plus its exact version-2 chain may preserve a superseded generation's closed clock; current SLA state and `reopen_history` are not authority.
3. Update task/SLA services and the three live linked-task producers so acknowledgement, task completion, cancellation, escalation, and policy degradation obey explicit semantics while preserving critical-result/cold-chain behavior and keeping mortuary active until release. Bind pathway SLAs to `workflow_steps:<integer text>`, acknowledgement SLAs to the task's exact resource pair, and mortuary to a real same-tenant `death_records:<integer text>` owner. Make human ownership task-first. The current executor may populate both `assigned_to_uid` and `assigned_role`; start validation accepts a same-tenant non-`PATIENT` UID, and migration 580 treats either a viable UID or a route-capable role as sufficient, so a role can mask an inactive or non-route-capable named UID. Do not claim exclusive active/route-capable named-owner enforcement in S1b-b. Generic clinical-inbox acknowledgement remains executor-blocked and no active definitions exist. Before activation, enforce exclusive owner resolution and add database conformance for active, route-capable named owners. Do not require a current active holder for a valid role queue before S1b-c activation evidence. Keep SQL role sets identical to route policy, resolve/fallback critical-result owners in the application, reassign stale actionable named owners, and use deferred `users` dependencies so identity changes/deletion require a valid same-transaction reassignment within the currently enforced baseline. Treat an absent/disabled rule as `null` even under strict mode; strict database/setup/materialisation errors still propagate. Never fabricate an SLA row, clock, link, or due time. Reject static task/approval `due_at`; calculate and select the exact SLA deadline in PostgreSQL. Bind other caller instants as epoch milliseconds and format durable JSON receipt timestamps explicitly in UTC. Enforce typed lifecycle and receipt constraints at commit without restoring task↔SLA cross-lock deadlocks. Keep generic task APIs tenant-transactional. Reserve approval kinds `care_pathway_definition_governance` and `credential_privilege_grant` from generic create/decide with `DOMAIN_OWNED_APPROVAL_KIND`, while pathway-linked approval mutation requires executor authority and returns `PATHWAY_EXECUTOR_REQUIRED` on bypass. Generic non-pathway approval commands remain available for unreserved domains. Preserve cold-chain role safety, atomic corrective acknowledgement, mortuary custody-only release, migration-352 global-rule visibility, unchanged noncritical notification taxonomy, integer-family legacy IDs, atomic manual/panel/ORU/ASTM and corrected/amended clinical obligations, authoritative migration-581 acknowledgement, and post-commit transport fan-out.
4. Add the immutable runtime registry and strict v1 definition compiler. Keep the production registry empty; preserve one exact registry object/version and compiled governed checksum across execution evidence; and require sealed system actors with independent projector causation plus immutable source/type/time signal context. Enforce pure task/approval gates. A trusted projector/domain integration may complete a typed `domain_evidence` task/SLA only through `completePathwayTaskAndExecuteFromRegisteredEvidence`, the sole production orchestration helper. Before any await, that combined helper normalizes and freezes one command envelope containing actor, signal, canonical key, activation capability, target, and domain-evidence operation; replay, completion, fingerprinting, activation, canonical writes, and execution use only that envelope and never reread mutable caller authority/provenance input. In one branded tenant transaction it uses an executor-minted object-identity capability held in a private `WeakSet` to complete the exact task/SLA and execute the pathway; direct primitive access, forged capabilities, and partial completion fail closed and roll back. Retain full normalized evidence in task/SLA audit records, emit a canonical `domain_evidence_task_completed` event containing a compact provenance-bearing reference plus SHA-256 fingerprint rather than duplicate raw evidence, and require the governed satisfied-evidence postcondition before commit. A task stage must complete; an evidence-satisfied approval may remain waiting for approval. Blocked/unsatisfied evidence, a forward-exception result, mismatched context, missing postcondition/canonical evidence, or canonical failure rolls task, SLA, comment, runtime, and evidence back together. Condition satisfaction alone never completes work. CAS-skip every intervening pending step, without executing its effects, on a valid ordinary forward exception. Split conditions into same-transaction `loadEvidence` inside an always-rolled-back savepoint followed by transaction-free pure `evaluate`; treat loaders as code-reviewed, side-effect-free readers because the savepoint is not a database-enforced read-only sandbox. Give every handler deep-frozen graph snapshots and make actions computation-only with no transaction/query capability. Re-lock and hash the protected parent/child/definition/ledger graph after an action as defense-in-depth; defer code-reviewed allowlisted atomic domain-write capabilities as a hard S1b-c/active prerequisite.
5. Add transaction-only pathway persistence, transition-event, and executor services. Reuse a supplied projector transaction, build frozen stage plans and a private ordered buffer of frozen transition intents before canonical writes, permit idempotent wait evidence without mutable-state change, and flush the command through one batch append session. Derive only internal canonical idempotency namespaces: user `u:<uid>:sha256(operation + raw_key)` without target/payload, sealed system `s:sha256(systemKey + sourceEventId + operation + business_target)` independent of the raw caller key, and child `c:sha256(parent + run + step + rule + child_business_identity)`. Persist no raw client key in pathway, transition, canonical, metadata, or snapshot evidence; same canonical key with a changed target, payload, actor, fingerprint, or sealed lineage conflicts. Prevalidate the full batch, own contiguous ordinals from array order, lock/load replay state and allocate the starting instance sequence once, write each canonical timeline/audit pair before its transition row, fail closed on partial replay, and roll back the whole caller transaction on any mid-batch failure. Persist a frozen result snapshot so replay never substitutes later mutable state. Before returning any replay, require the immutable instance/run/definition/governance/checksum pins, exact approval checksum receipt, and creation/runtime event pins to agree; replay may accept the exact retired pin but may never adopt changed governance. Retain blocking lock-and-replay behavior for a standalone start that owns its transaction, but make every start inside a supplied branded transaction acquire its complete definition/start/episode lock set fail-fast; translate Prisma SQLSTATE `40001` to retryable `PATHWAY_START_SERIALIZATION_BUSY` so inverse child fan-out cannot surface `40P01`. Retain the safe single-event path for atomic pathway start. Ensure interactive transaction callback business errors propagate without consuming or opening the global-per-client database circuit breaker; only positively classified infrastructure failures count.
6. Enforce the non-clinical compiler/runtime ceilings: JSON depth 32, 4,096 visited nodes, and 65,536 serialized UTF-8 bytes; 128 steps; 16 exception targets and 16 child rules per step; a 512-intent static auto-chain estimate; 32 resolved children per rule; 64 per stage and command; 512 aggregate child workflow steps per command; 512 transition intents; and 384 applied plans. Implement blocking, named-owner nonblocking, and informational child semantics; reject ownership-transferring definitions at compile time until D6/S3 supplies destination-acceptance evidence. Propagate the child-step budget through sealed internal capabilities so nesting cannot reset it.
7. Add default-off pathway routes, patient resource resolution, always-enforced patient PHI/access middleware, and direct generic-mutation bypass guards. Keep patient ABAC independent of both care-team rollout mode and pathway `off|shadow|active` execution mode: a denial reaches neither pathway reads nor executor calls, and execution shadow never permits fail-open access. Preserve only audited break-glass with a verified id and reason. Create ABAC resolves only `body.patient_uid` and rejects every request-query patient-selector alias; any resource-resolution failure is authoritatively unresolved and cannot fall back to query or body selectors. Derive user actor, manual source/trigger lineage, owning clinician, and accountable role from authenticated server context; reject manual team/arbitrary-owner assignment and caller-supplied authority/provenance. Require sealed system top-level starts to be event-triggered and derive their source/time from the sealed context. Allow active effects only through the sealed `NODE_ENV=test` capability while production `active` remains rejected. Reject fresh starts/commands in `off` without mutation, but after current PHI authorization allow an exact committed replay to return only its immutable snapshot before current mode evaluation for lost-response recovery; it must not compile, execute effects, or mutate state.
8. Add unit and real-PostgreSQL conformance tests for schema; read-only legacy task/SLA and terminal-receipt preflight; no delete/detach behavior; bounded migration-580 compatibility ancestry without treating `reopen_history` or comments as post-581 authority; exact immutable migration-581 acknowledgement receipts and version-2 closure chains; historical deadline preservation and post-lock race revalidation; two-release alias equality; exact marker-free critical-result/mortuary materialisation; narrow PR #607 compatibility promotion; PR #587 atomic successor materialisation with zero pending generations; route-role parity; the current same-tenant non-`PATIENT` named-owner baseline, valid role-queue ownership, application reassignment, and executor-blocked generic clinical-inbox acknowledgement; deferred identity/receipt/delete dependencies without task↔SLA deadlock; custody, timezone, missing-policy, cold-chain, mortuary, global-rule, and exact-deadline behavior; atomic manual/panel/ORU/ASTM ingestion and authoritative acknowledgement; immutable command lineage, idempotency namespaces, replay snapshots, batch ordering/rollback, and breaker isolation; registry/compiler/runtime budgets; pure human gates and capability-sealed domain-evidence completion; patient ABAC, audited break-glass, shadow suppression, and production-active rejection; child semantics and ownership-transfer rejection; migration-584 governance-existence classification, one-way typed retirement, immutable approval/definition/run/instance/creation/replay pins, retired-existing-run completion, generic approval-domain reservation, inverse-order definition/governance/run/canonical-parent contention returning retryable `40001` rather than `40P01`, and inverse-order supplied-transaction start locks returning retryable `PATHWAY_START_SERIALIZATION_BUSY` rather than `40P01`; and explicit absence of D3-D7/external-policy encoding. Keep exclusive owner resolution and active/route-capable named-owner database tests as an explicit pre-activation follow-up.
9. Add migration 584 as the terminal release-tail governance pin. Backfill only from one exact immutable `pathway_instance_created` event plus the referenced published governance and approval checksum receipt; reject unprovable pins. Make run and companion definition/governance/checksum pins immutable and require the creation event to agree. Classify a run as governed whenever **any** governance row exists for its definition: a generic unpinned run is allowed only when no governance row exists, while every fresh governed run requires the exact active, approved, effective pin and companion. Make approved publication immutable and retirement one-way with typed actor/time/reason/effective-until evidence plus an inactive definition. Permit already-created, exactly pinned approved-or-retired runs to continue to completion and replay; forbid fresh retired starts, re-enablement, repinning, deletion, or approval-receipt mutation. Serialize definition, approval, run-admission, governance-actor, creation-event, and canonical-parent races with transaction-scoped fences: any surface that may already hold a row/fence lock fails fast with retryable SQLSTATE `40001`, governance actor rows use `FOR SHARE NOWAIT`, creation-event admission may wait before parent validation, and inverse ordering must never surface as `40P01`. Encode no D3-D7 or external-policy behavior. Freeze migration 584 at 73,446 bytes with SHA-256 `F799232A9007CB3A69DEA11D7131C96913578E94BB8C62B9C1B6106921C31EB7`.
10. Run focused tests, migration/static gates, ESLint, raw-params, relevant deep suites, and the repository pre-merge gate in proportional chunks.
11. Run independent integration and security reviews; fix all surviving P0/P1/P2 findings.
12. Commit, push one branch, open one PR, and wait for authoritative checks. Stop at the review boundary; do not merge or deploy without separate authorization.

## Exit evidence

- Migrations 580–584 apply in order where pending, the regenerated Prisma schema matches the terminal schema, and the cutover inventory records these frozen SHA-256 values: 580 `A41495FC511BD5238FE548E9185A1461715B47AA54607C7F42FF8AD79EDAA979`; 581 `43AFB83D57E50E738540ADDCC02875C35826884B3D9D4D7B31BBAEBB77B61CB4`; 582 `F0CEA6E6EA63F9CF5932ACBD99EE9508A2E838D3715D09B969AED99E3A0E41F0`; 583 `7D1ABE4238FA95D4BAFBEA9E86052DF8C53CA8361FEFDCF407EA9E44E10919F1`; and 584, 73,446 bytes, `F799232A9007CB3A69DEA11D7131C96913578E94BB8C62B9C1B6106921C31EB7`.
- A read-only per-tenant legacy task/SLA and terminal-receipt report is clean before deployment; every malformed/orphaned or lifecycle-inconsistent row has documented evidence-based clinical reconciliation. Any migration-580 corrected-result compatibility chain is bounded, monotonic, same-tenant/patient/SLA/resource and carries the exact preserved historical clock evidence, but neither `reopen_history` nor comments are treated as post-581 acknowledgement authority. Post-581, every closed generation has the exact immutable acknowledgement receipt and version-2 alert/task/SLA/comment/timeline/audit chain. Unproven no-pointer generations block; the current rearmed SLA never substitutes for historical evidence. Post-lock revalidation closes racing writes. No task/SLA is deleted and no metadata link is detached to satisfy migration 580. Compatibility promotion occurs only for exact recognized old-writer shapes; malformed or mismatched state remains unchanged and blocked. PR #587 rearm is crash-atomic and leaves zero pending successor generations; cleanup is deferred until old-replica drain evidence exists.
- No active pathway definition or production handler exists.
- Production active mode rejects execution until S1b-c evidence and recovery tooling exist; off/shadow semantics and the sealed test-only active capability are tested. `off` rejects fresh starts/commands without mutation, while a currently PHI-authorized exact replay may recover only its immutable committed snapshot before current mode evaluation and performs no compile, effect, or mutation.
- Patient ABAC remains always enforced independently of care-team rollout and pathway execution modes. Denied requests reach neither pathway reads nor the executor even in shadow, and audited break-glass requires a verified id and reason.
- Every accepted command records at least one append-only transition event, with one canonical timeline/audit pair for each deterministic effect ordinal, in the same transaction. Its full frozen intent buffer is prevalidated and flushed in one sequence-allocation/replay session; partial replay and mid-batch failure fail closed. Wait observations may record equal previous/new state without mutating execution state. Full registered domain evidence remains in task/SLA audit records while the canonical pathway completion carries only its compact provenance-bearing reference and fingerprint.
- Replays require the same executor-derived canonical idempotency key and immutable command fingerprint, pass current PHI authorization, and return the immutable committed snapshot without re-running handlers or recompiling changed governance. Before return they prove the instance/run/definition/governance/checksum pins, exact approval checksum receipt, and stored creation/runtime-event pins; an exact retired pin is valid for an existing run, but newer governance cannot be substituted. Actor- or lineage-changing key reuse conflicts. User keys use `u:<uid>:sha256(operation + raw_key)` without target/payload, sealed system keys use `s:sha256(systemKey + sourceEventId + operation + business_target)` independently of the raw caller key, and child keys use `c:sha256(parent + run + step + rule + child_business_identity)`. Raw client keys never persist in pathway, transition, canonical, metadata, or snapshot evidence. System commands derive source/type/time from an identity-sealed signal context separate from projector `sourceEventId`/`causationId`.
- Duplicate commands, episodes, tasks, approvals, and child fan-out are idempotent under concurrency; changed target/payload/fingerprint or sealed lineage under the same canonical key conflicts.
- Same-tenant but wrong-run graph links are rejected in service and database layers.
- At publication, governance approval checks prove distinct currently eligible same-tenant non-patient approver/voter identities, timestamps, quorum, exact-definition linkage, and an immutable matching checksum receipt. That account-eligibility lookup is publication-time only: the receipt remains valid after a voter later changes role. Clinical and operational owners are current duties and continue to be revalidated while governance is approved; after retirement those owner identities are historical evidence and may later deactivate or change role without invalidating the retired record. The receipt does not prove that `approvals.required_role` was held at vote time. Generic approval create/decide remains available only for unreserved domains; `care_pathway_definition_governance` and `credential_privilege_grant` reject with `DOMAIN_OWNED_APPROVAL_KIND`, and pathway-linked approvals reject generic mutation with `PATHWAY_EXECUTOR_REQUIRED`. Migration 584 proves one-way typed retirement and immutable approval/definition/runtime pins. Normalized immutable vote/role evidence remains required before production activation.
- Pathway task/SLA links use the exact `workflow_steps` source binding; critical-result and cold-chain acknowledgement links use the exact task resource pair; mortuary links use an existing same-tenant integer-family death-record owner. Typed task deadlines are selected directly from the SLA row in PostgreSQL and remain exactly equal; caller-originated instants survive non-UTC process timezones through epoch binding and explicit UTC receipt formatting. Deferred commit checks enforce the exact terminal acknowledgement/direct-completion or domain-evidence receipt and prevent task/SLA/source-evidence deletion from stranding a surviving obligation. Critical-result and cold-chain acknowledgement still stop their clocks; cold corrective action acknowledges excursion/task/SLA atomically; mortuary acknowledgement does not stop its clock, while registered release evidence does; cancellation completes no SLA. Missing/disabled policy never fabricates a clock, deadline, or link: critical-result/pathway materialisation fails closed, while cold-chain and mortuary retain only their recognized degraded task/metadata shape. Strict database/materialisation errors still propagate. Migration-352 global SLA rules remain readable under a pinned tenant GUC. Noncritical outward notification taxonomy remains unchanged; generic clinical-task taxonomy/recipient policy awaits owner-approved S1b-c policy.
- Every incomplete human-action SLA has exactly one exact actionable task, but S1b-b does not yet prove exclusive active/route-capable named ownership. The executor may set both `assigned_to_uid` and `assigned_role`; start validation accepts a same-tenant non-`PATIENT` UID, and migration 580 accepts UID-or-role viability, allowing a route role to mask an inactive or non-route-capable named UID. Generic clinical-inbox acknowledgement remains executor-blocked and no active definitions exist. Exclusive owner resolution plus active/route-capable named-owner database tests are required before activation; role queues remain route-capable without requiring an active holder before S1b-c activation evidence.
- Deferred validators keep task/SLA counterpart reads non-locking and take only one-way `FOR SHARE` locks on referenced owner/receipt users and break-glass grants. Reverse dependency and concurrency tests prove user/grant mutation cannot invalidate durable evidence or restore the task↔SLA deadlock.
- New/updated cold-chain alert-role configuration rejects non-servicing roles. Invalid legacy roles cannot route or receive alerts; runtime records degradation, substitutes safe department staff queues, and sends no patient notification.
- The legacy death-record release path rejects while any custody event, active typed/degraded unclaimed task, or mortuary SLA rail exists. Migration 414 rejects custody-event `UPDATE`; migration 580 rejects `DELETE` only for cited initial/completion evidence and does not claim every custody row is globally undeletable.
- Manual, panel, authenticated ORU, and ASTM result ingestion plus corrected/amended sign-off commit their clinical record, criticality decision, exact alert/task/SLA generation, canonical evidence, and durable command/message receipt atomically. Critical-alert acknowledgement is one authoritative alert/task/SLA/comment/canonical transition across both lab-alert and clinical-inbox routes; generic task acknowledgement cannot bypass it. New task, SLA, comment, timeline, and audit evidence carries exact technical `ack_contract_version=2`. Unversioned legacy split state in either direction is a zero-write migration/deployment blocker and is never automatically upgraded without a future owner-approved immutable reconciliation receipt. The existing nullable `read_back_method` remains caller-supplied with no invented default; when present it is preserved exactly in durable/canonical evidence. Notification/realtime and the existing outbound signed-result ORU feed remain post-commit transport only.
- A privileged all-tenant primary read-only migration-580 report, the combined migrations-582–584 ingest-and-governance readiness report, and a fresh PostgreSQL 18 production-clone rehearsal are clean before deployment. The maintenance runbook drains old replicas, accepts only `[580, 581, 582, 583, 584]` or the tracker-proven prerequisite variant `[582, 583, 584]`, runs post-drain migration-581 reconciliation, reruns post-schema audits, and resumes traffic only after they are clean. Order-less ORU/ASTM results remain a hard activation blocker requiring an owner-governed linkage/reconciliation mechanism; they are not described as staff-repairable today. Manual-result clients must send `Idempotency-Key`; the zero in-repo production/UI caller result does not prove external clients absent, so gateway/integration inventory is a cutover gate.
- One exact registry identity/version plus the verified governed-definition checksum, dual sealed system lineage, pure human gates, rolled-back same-transaction evidence loading with transaction-free evaluation, JSON depth 32/node 4,096/serialized UTF-8 byte 65,536 limits, and all compiler/runtime ceilings are proven by conformance tests. Actions are computation-only and receive no transaction/query capability; allowlisted atomic domain-write capabilities remain an S1b-c/active prerequisite, with graph hashing retained as defense-in-depth. A typed domain-evidence task/SLA completes and advances execution atomically only through the sole production orchestration helper using the executor-owned private-`WeakSet` capability; before any await the helper freezes one normalized command envelope and never rereads mutable caller actor, signal, key, or activation capability. It emits canonical task-completion evidence and requires the governed satisfied-evidence postcondition, permits only an evidence-satisfied pending approval, and rolls back blocked/unsatisfied/forward-exception or missing-postcondition outcomes. Condition satisfaction alone never completes it.
- Blocking, named-owner nonblocking, and informational children obey their declared closure semantics; ownership-transfer definitions are rejected pending D6/S3, and nested starts cannot reset the aggregate child-step budget.
- Governance-row existence is the final run classifier: generic unpinned runs are valid only for definitions with no governance row; fresh governed runs require approved/effective pins and one exact companion/creation event. Direct generic run/step/task/approval mutation cannot bypass pathway evidence. A retired definition accepts no fresh run, while an existing exact retired pin may complete and replay without repinning.
- Interactive transaction callback business errors never consume or open the global-per-client database circuit breaker; only classified infrastructure failures do.
- D3-D7 remain unsigned engineering recommendations; no migration, definition, handler, external-policy resolver, recipient rule, clock, threshold, default, or conformance test encodes them, and S1b-c remains an explicit prerequisite for production activation.
- Final focused evidence on frozen migration 584 is: independent P0/P1/P2 review, 0/0/0; a fresh PostgreSQL 17 build through all 568 migrations; full schema-plus-transition conformance, 114/114; focused former-deadlock cases, 2/2 returning `40001` and never `40P01`; runtime deep, 7 suites/72 tests; units, 5 suites/136 tests; and clean schema-drift, Prisma, raw-parameter, ESLint, and diff checks. These focused results do not claim a final whole-program suite or full-CI result; every remaining authoritative local and hosted gate must still be green before review completion.
- Final full-CI exit evidence (2026-07-20), additive to the focused evidence above: a fresh blank PostgreSQL 18.4 database applied all 568 migrations; unseeded and seeded database contracts each passed 13/13; exhaustive seed coverage left 812/812 application tables non-empty. Two consecutive runs of each readiness audit exited 0 with zero blockers; the care-spine report was ready, while both combined lab/pathway reports explicitly retained `care_pathway_production_activation_ready=false`, as required until S1b-c. The final backend gate passed lint, raw-parameter/PHI/tenant/region/secret checks, dependency audit, Prisma generation and schema drift, OpenAPI drift/core sync, Spectral with zero errors, Docker-backed database guardrails, and all 1,152 Jest files in 144/144 chunks; it exited 0 in 4,698.3 seconds. This blank-database rehearsal is clean-install/full-CI evidence only, not a production-clone rehearsal or production-activation evidence.

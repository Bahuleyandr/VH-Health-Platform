# Reprocessable devices platform (dialysis and OT) — design

- Date: 2026-09-05
- Status: **awaits owner design approval (stage 1 of 3, §1.2).** **Revision round 2 (owner review of 2026-09-06) applied — the owner's ten numbered points and the additional release conditions are each answered in the body and indexed in §11 (the revision ledger).** The architecture stands (device identity separate from patient linkage, dialysis dedication, historical evidence retained while decisions come from live data, atomic un-capture); D1–D11 stand as decided; the two-phase carve stands; the resolver contract stands; the canary discipline stands. What changed: a marker-free register for every writer including the late-reactive path (§3.2, §4.3), an explicit HOLD model separate from historical exposure (§5.5), one domain-aware release path (§5.6), load evidence with per-device processing events and one load-outcome handler including load creation (§5.2), three separated clinical functions with a pathogen-specific reuse-eligibility matrix and dialysis release criteria on an approved protocol (§5.3, §5.5, §4.2a), the block-mode escapes closed (§3.4), composite relationship constraints and physical-use preconditions (§4.3, §4.4, §5.1, §5.2), durable exposure delivery through an outbox in the marker's own transaction (§5.7), a lock order and command receipts (§5.8), a release-CI activation gate and a named response validator (§7.3, §6.6), and a Phase 2 census that counts every legacy declaration still deciding anything (§7.6). Owner decisions D1–D9 taken in conversation on 2026-09-05; this document designs within them. Design review round 1 (2026-09-05) applied: the un-capture path for cancelled cases (G1), executor fixes 1–10 and the review follow-ups; the then-new owner item D10 (now decided in §2 and §9). **Carve-out (2026-09-05, decision trio): the dialysis isolation derivation is the Phase 1 lane's (the merge-authority session — dev-1b, formerly signing as dev-ea — migration 767); this document consumes its resolver and ships the Phase 2 census gate (§3.3); the settled carve and the non-blocking Q2–Q3 follow-ups are in §9.** **Contract amendment (2026-09-05):** dev-1b rejected `isolation_class` as an unconditional fifth `Decision` field; it is now **opt-in** behind `includeIsolationClass` (§3.3) and nothing user-facing carries it. **Owner decisions of 2026-09-06, confirmed to dev-1b as the merge authority: D11 = NO** — a routing role may learn *that* a patient is isolated, never *which* marker: `dialysis_machines.isolation_group` becomes the routing key, the class → group mapping lives in the per-tenant isolation settings, `required_class` is **retired** and `required_group` is the live key (§2, §3.4, §9); and **Q1, REVISED by the owner the same day (via dev-1b, who owns migration 767's semantics; this lane consumes the rule and says so): the legacy roster columns are ASYMMETRIC evidence** — a legacy `'positive'` restricts on its own; a legacy `'negative'` is **not evidence** (never an input to `clear`, never corroborated, never ANDed with anything); a roster row with only a `'negative'` and no marker or `dialysis_serology` row resolves `unknown` (§3.3, §7.3, §7.6, §9). Review round 1's Fix-6 is superseded by D11. **Naming:** throughout this document **dev-1b** is the merge-authority session that owns Phase 1, migration 767 and the derivation lane; it signed the earlier rounds as dev-ea
- Base: `github/main` at **`db30fe80b`** (re-based 2026-09-07 from the stale `b27a730d3`; highest migration still 766). Between the two, `main` took **#1015** (the policy-filtered CSSD cycle-type picker and device-label endpoint), **#1016** (cath readiness refresh scheduled off the lab writers' critical path), **#1017** (`reconcileTenant` / `reconcileAllTenants` re-drive the signed-result writer; `exposureHandlerBootstrap.js` explicitly imports handler owners and exposes `exposureHandlerCount`; the API and operator sweep refuse an empty registry), **#1018** (readiness rules/actions/persistence split plus waiver asymmetry), **#1019** (the `TENANT_A` facility fixture), **#1020** (log masking), **#1021** (`ingestMachineObservations` is tenant-scoped), **#1022** (the date-only outside-report correction), **#1024** (`recordSerology` and `addAccess` stamp their own tenant; the serology stamp is load-bearing for §3.3), and **#1025** (`dbClockAsOf` supplies `refreshCaseLabReadiness` with the database clock and the clock-comparison guard covers DATE columns). #1025 changes no Plan 4 contract but is recorded so this document is anchored on the actual head. Every code claim below was re-verified against this base on 2026-09-07 and is cited by **function name**; a `file:line` that survives from the 2026-09-05 scouting pass is secondary (`registerExposureHandler` is at `cathDeviceReuseService.js:1428`; `recordReuseRegister` at `dialysisService.js:943`; `scheduleSession` at `:446`; `cancelSession` at `:631`; the seroconversion checks at `:1130-1132`; the positive-column promotion at `:1160-1170`; `createSterilizationLoad` at `cssdService.js:424`; `transitionSterilizationLoad` at `:588`; `issueSet` at `:731`; `transitionIssue` at `:807`; `cancelIssue` at `:947`). **Migration number: `NNN`** — the next free number at implementation push time. 767 is reserved for the Phase 1 lane whether or not a `767_*` file exists; this lane never infers availability from its absence. Every `NNN` in this document and the plan remains a placeholder until implementation
- Programme: Plan 4 of the cath device-reuse programme. Plans 1–3 are merged (`764_patient_bloodborne_markers`, `765_cath_device_reuse`, `766_cath_lab_readiness`)
- Companions: `2026-09-04-cath-device-reuse-and-bloodborne-markers-design.md` (the register shape, the state machine, the serology resolver and the CSSD queue this platform generalises — cited below as "the cath spec"), `2026-09-04-cath-pre-procedure-lab-readiness-design.md` (the role projection and the disclosure canary a new mount must join)
- Scouting brief with file:line evidence: the read-only pass of 2026-09-05 over `apps/backend/src/migrations/{168,291,418,419,420,421,422,423,473,474,565,765}`, `services/clinical/{dialysisService,dialysisMachineService,cathDeviceReuseService,bloodborneMarkerService,bloodborneMarkerRules,cathLabReadinessProjection}.js`, `services/cssd/cssdService.js`, `routes/{cssd/cssdRoutes,clinical/dialysisRoutes,clinical/cathReprocessingPolicyRoutes}.js`, `lib/prisma.js`, `tests/unit/serologyDisclosureCanary.test.js`

## 1. Problem and scope

The platform holds **four independent reprocessing loops with four incompatible identity models**, built at four different times:

| | cath (Plan 2, mig 765) | dialysis (mig 418) | OT/CSSD (migs 421–423) | linen (migs 473–474) |
|---|---|---|---|---|
| identity | system-minted `device_tag` `RP…` | operator-typed `dialyzer_serial` free text | `set_code` + `barcode`, operator-supplied | none (quantity) |
| grain | one row per physical device | one row **per session** | one row per set + one issue row per OT case | per laundry cycle |
| state machine | 6 states, `DEVICE_ACTIONS` from-lists | 3 flat statuses | 9 on the set, 7 on the issue | 5 on the cycle |
| cycle ceiling | `max_cycles_snapshot`, DB CHECK | `reuse_cycle_count` 0..100, typed, no policy | none | none |
| serology gate | full (`resolveReuseStatus`) | none — parallel `dialysis_patients.*_status` columns, never consulted by the reuse path | none | none |
| sterilisation evidence | manual CSSD mark, no load link | `integrity_test_result` pass/fail, `disinfectant` free text | `sterilization_loads` with BI/CI/mechanical indicators | none |
| per-patient dedication | pooled | recorded (`patient_uid`) but never enforced | pooled | pooled |

Plan 4 is **convergence, not extension**: one register, one policy shape, one state machine, one serology gate and one CSSD queue serving dialysis and OT, with the cath register left where it is. Linen stays quantity-based and is out of scope.

The three defects this closes, in order of clinical weight:

1. **Dialysis has no serology gate and two serology records that disagree.** `dialysis_patients.hbsag_status / hcv_status / hiv_status` (168:45-50) drive `isolation_required` and the today board, but a reactive HBsAg signed off in the lab updates `patient_bloodborne_markers` (Plan 1) and *not* those columns. A patient the platform knows is reactive can sit on the board as non-isolation. The dialyser reuse path (`dialysisService.recordReuseRegister`, :928-999) reads neither record. This defect has two halves and two owners: *which record the roster reads* (the derivation) is the Phase 1 lane's, and *the reuse path reading no record at all* is this lane's — closed by consuming Phase 1's resolver (§3.3).
2. **The dialyser cycle count is whatever the operator typed.** `reuse_cycle_count` is validated only against `dialysis_sessions.reuse_count`, a number the same client wrote on the previous call (:945-957). Nothing links two register rows carrying the same serial; nothing stops one serial appearing under two patients; there is no maximum, no TCV rule and no agent/contact-time record.
3. **OT sets carry no cycle count, no exposure flag and no policy**, and a set used on a reactive patient is returned and re-issued like any other. Sterilisation evidence exists (`sterilization_loads`, 422) but cath devices are not on it, and a failed load does not touch a set already issued.

### 1.1 Consumers and non-goals

**Consumers of this lane:** dialysis (**dialysers only** — bloodlines are **removed from this release**: one usage per session cannot model a bloodline set beside a dialyser, and a bloodline is single-use in every Indian unit this platform serves; a later release models them as slots if a tenant ever needs them, §10) and OT (instrument sets and trays; **procedure packs are tracked but can never be made reprocessable** — a `reprocessable = true` policy row is refused for them by CHECK, §4.2). Cath keeps `cath_reprocessable_devices` untouched (D1b); §2.6 states the seam a later cath migration would use.

### 1.2 Approval model (owner, 2026-09-06) — three stages

Approval of this work is split into three stages, each with its own evidence, and no stage implies the next:

1. **Revised design approval** — this document and the plan, after the owner's review of 2026-09-06 (§11 indexes every point). Nothing is built before it.
2. **Implementation verification** — the code, with the **complete lifecycle and race tests** named in §8 green on a fresh database, twice: every state of both domains through capture → use → return → hold → authorised release → reprocessing → re-issue, the three concurrency tests (§5.8), the durable-delivery crash test (§5.7), the load-outcome discovery pin, the canary over device fields / snapshots / error bodies / audit-derived responses, and the mutation checks. The plan's Task 9 hands back with this evidence and merges nothing.
3. **Tenant clinical activation** — a tenant turns a domain on by creating its policy rows **under an approved protocol** (§4.2a): a nephrology / CSSD / infection-control protocol reviewed and recorded by name, with the pathogen-specific reuse matrix, the TCV threshold, the agent parameters and the surveillance interval it mandates. The release-CI activation gate (§7.3) and the boot assertion refuse a build on which activation is possible while the Phase 1 resolver is missing, and the durable exposure delivery (§5.7) is a precondition of this stage, not a follow-up.

The plan's hand-back task states the stage it is handing back for (stage 2) and what stage 3 requires of the tenant.

**Non-goals:**

- Moving, renaming or viewing `cath_reprocessable_devices`; re-declaring `cath_inventory_authority_assert_contract_753` or `cath_authority_identity_guard_753` (a rename would force a fourth verbatim re-declaration of a ~400-line plpgsql body that no CI gate validates — the mig-952 lesson).
- Implant reuse or implant lifecycle (D5). `surgical_implants` gains only the sterilisation-load evidence FK it lacks. The unmerged `audit/cath-implant-lifecycle` branch (which carries a colliding migration 764) is decided separately by the owner (D5b); nothing here depends on it or touches its tables.
- Billing (D8). Dialysis keeps its flat `DIALYSIS-HD-SESSION` line (mig 420); OT keeps its zero-priced audit line; no reuse-differentiated tariff.
- A dialyser catalogue. Dialysers are free-text strings in three places today; the register records `manufacturer` and `model_name` as text. A catalogue is a follow-up (§10).
- `facility_id` on `dialysis_sessions` and `instrument_sets` (neither has one). The register's `facility_id` is nullable for that reason (§2.3); the upstream backfill is its own decision (§10).
- Linen; `dialysis_machine_qa_logs` (its `machine_no` stays free text; a FK to the new `dialysis_machines` is a follow-up); the `device_registry` (371) and `clinical_ai_biomed_devices` (053) machine tables beyond the one optional FK in §2.7.
- A recall **dashboard**. The minimum failure-response workflow (identify affected devices and patients, hold, notify infection control) is **in scope** (§5.2); a screen that tracks the response is a follow-up (§10).
- Single-use-device reprocessing (a separate regulatory question). Nothing here admits a category that is single-use in the Indian market to a reprocessable policy (§4.2).
- A patient-facing surface.
- Writing `dialysis_patients.hbsag_status / hcv_status / hiv_status`, changing their defaults, adding a marker source, re-pointing `recordSerology`, or backfilling markers from those columns — all Phase 1 (§3.3). This lane reads the roster and never alters it.
- Editing migrations 168, 291, 418–423, 565, 764, 765, 766 or 767 (Phase 1's). All schema change is forward-only under **`NNN`** (§4.11).

## 2. Decisions

The owner's decisions are fixed; the reasons recorded here are the scouting brief's, kept so the migration header can state them inline.

- **D1 — Owner references are typed, nullable per-domain FKs plus `CHECK (num_nonnulls(...) = 1)`.** The pattern is already in production on `surgical_implants` (`565:54-58`, `surgical_implants_source_check`). A `(ref_type, ref_id)` string pair cannot be a foreign key; three sibling registers cannot share a queue. Every arm is a tenant-pinned composite FK (§2.4).
- **D1b — Cath stays in `cath_reprocessable_devices`.** Two registers, one service shape, one queue with a domain filter. The seam for a later cath migration is §2.6.
- **D2 — The register is patient-blind.** Per-patient dedication of a dialyser lives on the dialysis link row (§2.5), never on `reprocessable_devices`. This is what keeps `/api/v1/cssd` free of a PHI logger and keeps the CSSD queue out of the serology disclosure blast radius.
- **D2b — Domain is a fixed enum**, `dialysis` and `ot`, with `cath` reserved. Per-domain rules are code, not configuration; a tenant seeding a "Nephrology 2" department must not create a fourth rule set.
- **D3 — Dialysis machines do not enter the register.** A minimal `dialysis_machines` table (§2.7) carries the routing group (`isolation_group`, D11 = NO); isolation is **warn-only with a recorded override**, mirroring `set_issue_log.warn_only / enforcement_enabled` (423:27-28), with a per-tenant switch to `block` for units that have tagged their machines. **Review 2026-09-06:** `block` has **no escape routes** — in block mode an unregistered machine and an unmapped class are refusals, the switch to `block` is refused until the mapping is validated against active machines, and the emergency exception is an explicit authorised workflow with its own role set and audit (§3.4).
- **D5 — OT scope is instrument sets plus the sterilisation-load evidence FK on `surgical_implants`.** Implant reuse is out of scope.
- **D6 — Dialysis converges: mint device identities for dialysers; `dialyzer_reuse_register` stays as the statutory log and gains `device_id`.** `reuse_cycle_count` becomes derived from the device, which is the defect being closed. Two behaviour changes follow and are flagged in §4.2: a client-typed cycle count that disagrees with the device is refused, and a register row stops being freely re-writable once the device has moved on. **Review 2026-09-06:** the record also carries the **release criteria** (§4.2a, §5.1): a reprocessing record whose mandatory evidence is missing settles the usage but does **not** release the dialyser (`release_status = 'not_established'`), and a settled record is followed by **append-only attempts** (§4.14), never a rewrite.
- **D7 — Sterilisation evidence is load-driven for OT and not for dialysis.** OT sets are already on `sterilization_loads` (`set_ids`, `instrument_sets.last_passed_load_id`, `set_issue_log.sterilization_load_id`); a passed load increments the set's device cycle and a failed load quarantines it. Dialyser reprocessing is a chemical reuse-room process recorded on the statutory row, not an autoclave load. **Review 2026-09-06:** a passed *status* is not a processing *event* — evidence identity is a durable per-device `device_processing_events` row (device × load, exactly once, with its applicability window), written by ONE load-outcome handler that both `createSterilizationLoad` (which can create a load already `passed` or `failed` — verified on `main`: `deriveLoadStatus` returns `passed` when `completed_at` is set and every indicator passed, and the creation path then UPDATEs `instrument_sets` and `set_issue_log` directly) and `transitionSterilizationLoad` call (§5.2). Both sides of the evidence chain are kept: the event that made a set ready **before** a use is pinned immutably on the usage, and the event **after** it is recorded separately.
- **D8 — Billing unchanged.**
- **D9 — External identifiers are accepted alongside the minted tag.** `manufacturer_serial` (dialysers) and `hospital_asset_id` (the OT set barcode is copied here) are unique per tenant and domain when present; the minted `device_tag` is always present.
- **D10 — DECIDED separately per role (review 2026-09-06): D10a `DIALYSIS_TECHNICIAN` = NO; D10b `BLOOD_BANK_STAFF` = NO; D10c `BLOOD_BANK_TECHNICIAN` = NO.** None joins the serology-detail audience in this lane. A dialysis technician receives the three-valued status, non-clinical warning codes and `required_group`, which is sufficient to route without exposing the marker. The two blood-bank roles have no Plan 4 clinical-detail surface, so broadening their platform-wide serology access would exceed this lane. This is three explicit decisions, not a bundled inference from provisioning. `NON_ASSIGNABLE_HUMAN_ROLES` (`rolePolicyGraph.js:343-363` on `main`) contains `DIALYSIS_TECHNICIAN` and `BLOOD_BANK_STAFF` but not `BLOOD_BANK_TECHNICIAN`; non-assignability is not proof that nobody holds a role. `scripts/role-assignment-census.mjs` (§7.6) therefore counts actual active assignments per tenant for all three before implementation hand-back and clinical activation. Any future widening is a separate reviewed change to the canary-pinned allow-list. The D11 routing-payload test uses a fixed role fixture and cannot become vacuous if that future change occurs.
- **D11 — DECIDED — NO (owner, 2026-09-06, via the merge authority). May a routing role learn *which* marker isolates a patient, or only *that* one does? Only *that* one does.** Whoever puts a patient on a machine must know the patient needs an isolation machine; the *reason* is not named to that role. The NO branch **ships and is the design, not an option**: (a) `dialysis_machines.isolation_group` — a tenant-defined label ("Bay 1", "Red side") carrying no clinical meaning, `VARCHAR(40)`, **nullable, where `NULL` is a general machine** — **replaces** `isolation_class` as the machine's routing key in migration `NNN`, in `listMachines` / `createMachine` / `PATCH /machines/:id`, in the OpenAPI `DialysisMachine` schema and in the Admin and Staff machine forms (§4.6, §6.1, §6.7, §6.8). (b) The class → group mapping lives in the per-tenant isolation settings as `reprocessing_domain_settings.isolation_groups` (§4.1), a JSONB object keyed by the four marker classes — e.g. `{ "hbsag": "Bay 1", "hcv": "Bay 2", "hiv": "Bay 2", "isolation_mixed": "Bay 3" }` — written only through `PUT /api/v1/reprocessing/domains/dialysis/settings` under `ISOLATION_GROUP_ADMIN_ROUTE_ROLES` = `REPROCESSING_POLICY_ROUTE_ROLES ∩ {INFECTION_CONTROL_OFFICER, ADMIN, SUPER_ADMIN}`; `QUALITY_OFFICER` (also on the governance mount) reads it and cannot write it, and `DIALYSIS_TECHNICIAN` — which lives on `DIALYSIS_ROUTE_ROLES` and on no governance set — **never reaches the mount at all**. A save naming a group that is on no registered machine **warns**; it does not refuse (a unit may map its bays before it tags them). (c) `assessIsolationTx` / `evaluateIsolationForSessionTx` still call the resolver with `includeIsolationClass: true` and map class → group **server-side** through those settings; the comparison is `required_group` vs `machine.isolation_group` (§3.4). (d) `isolation_warnings[]` entries carry `{ code, machine_id, severity, required_group }`. **`required_group` is the live key and `required_class` is RETIRED**: it is not in `computeIsolationWarnings`' output (the marker class survives only as an unnamed local inside it, used to index the map), not in the internal isolation object, not in the `DialysisIsolation` schema (`additionalProperties: false`, which lists `required_group` and never a class), not in the mismatch or blocked messages, and not in the `dialysis.session.isolation_evaluated` metadata — all of which name the **group** (§7.2). (e) **Review 2026-09-06 — widened to EVERY writer and BOTH domains:** the platform register carries **no marker name anywhere**. `reprocessable_devices` has **no `exposure_markers` column at all** (§4.3): the exposure fact on a device is the boolean `exposure_flag` (historical, never cleared) and the operational restriction is a **hold row** (§5.5); the *which-marker* evidence lives only on the hold's pointer to the audience-gated marker row (`patient_bloodborne_markers`, the existing record behind `roleSeesSerologyDetail`). This closes the contradiction the owner found: the late-reactive handler (§3.2) no longer writes `'Late reactive <marker> …'` into `quarantine_reason`, no longer appends a marker to a device, and its audit metadata carries a hold id, not a marker. `quarantine_reason` becomes a **non-clinical reason code** from a fixed vocabulary (§4.3), and free text goes to the hold row. **The OT arm is marker-free too** — the owner asked for dialysis; making the register uniform costs nothing, removes an asymmetry the canary would otherwise have to special-case, and the OT evidence is the same hold → marker-row pointer (approved as the uniform two-domain representation). The CSSD queue reads the flag and universal precautions apply. (f) Staff renders `required_group`; the `required_class` string key is dropped from the five-locale block (§6.8). (g) **Two** server-side callers pass `includeIsolationClass: true` (review 2026-09-06 changed this from one): `assessIsolationTx` (machine routing, class → group) and `reuseEligibilityTx` (the pathogen-specific reuse matrix, §5.3, which cannot be pathogen-specific without the class). Both are in-process decisions; neither serialises, snapshots or audits the class, and the plan pins the count at exactly two by name (§8). **The resolver contract in §3.3 is unchanged** — the class crosses the Phase 1 seam server-side either way; only what leaves the API differs, so Phase 1 was never blocked by this question. **Costing history, kept so the decision is legible:** YES would have cost nothing — `isolation_warnings` regains `required_class` and the design is what review round 1 settled on as Fix-6; NO costs one column swap and one settings block, and that is what shipped. **The limit the owner priced honestly (dev-1b):** under NO the mapping is opaque in the *data*, not in the *world* — a technician who sees which patients route to "Bay 1" learns the cohort within a week, and "Bay 1 is the hep B bay" becomes ward knowledge whatever the payload says. That is an inference risk, not a disclosure, and it did not argue against the design (routing has to happen somewhere physical): `isolation_group` **reduces the blast radius** — no marker in an API response, a log line, an audit row or an export — without *eliminating the knowledge*, and it was chosen for the former, not in the belief that it achieves the latter. Review round 1's Fix-6 ("do not blank `required_class`") is **SUPERSEDED**: that round reasoned the class was recoverable anyway (`GET /machines` published every machine's class and the two mismatch codes narrowed it by elimination), which is an argument for this relabel rather than for shipping the class — under `isolation_group` the same elimination recovers a bay name and nothing more.

Decisions taken by this spec within those:

- **A captured device is released, not returned, when its case is cancelled before use.** `cancelSession` / `no_show` (dialysis) and `transitionIssue → cancelled` (OT) run an un-capture hook inside the cancel's own transaction: the usage closes with `post_use_disposition = 'cancelled_before_use'`, the device goes `in_case → available` with no cycle change and no reprocessing record, and a cancelled usage does not consume its cycle number (§4.4). Without this a cancelled case left the device `in_case` with an open usage, and the only exits from `in_case` were `return` (a needless reprocessing) or `discard`: the next `issueSet` of the same sterile set failed `RPD_DEVICE_NOT_AVAILABLE`, the next capture of the patient's own dedicated dialyser failed the same way, and "recovering" by recording reprocessing on the cancelled session incremented a cycle that never happened — the D6 defect re-opened from the other side. The un-capture is refused when use was recorded or may have happened (§5.1 Cancel, §5.2), and a refused un-capture fails the cancel: best-effort is not allowed here, because a cancel that succeeds while the device stays `in_case` is exactly the defect.

- **Two policy tables, not one.** The brief sketched a single `reprocessing_domain_policies (tenant × domain × category)` carrying the serology rule. The serology rule is needed *before a category is known*: the late-reactive exposure handler sweeps a whole domain, and the dialysis isolation warning is evaluated per session, not per device. So the three serology fields live on `reprocessing_domain_settings (tenant × domain)` — the exact shape of `cath_reprocessing_settings` with a domain column — and `reprocessing_domain_policies (tenant × domain × category)` carries reprocessable / max cycles / cycle types / function check / TCV threshold, the exact shape of `cath_reprocessing_category_policies` plus two columns. Plan 2's admin and service code shapes carry over unchanged.
- **A third `reactive_patient_rule` value, `quarantine`.** Plan 2's `discard` means "the device is retired at post-use". Retiring a ₹2-lakh orthopaedic tray because it touched a reactive patient is not what any theatre does; the realistic action is to hold it for infection control, who either release it into a fresh cycle or discard it. Rather than make `discard` mean two things, the enum gains `quarantine`. Dialysis defaults to `discard` (a dialyser is a consumable), OT defaults to `quarantine`; either domain may choose either. The shared disposition function gains one branch; a parity test pins that, with no `quarantine` rule in play and **over the input domain the two functions share** (a non-null ceiling, a flag that equals "an active hold"), it answers exactly what cath's `computePostUseOptions` answers for the same inputs (§5.3).
- **`max_cycles` is optional, and its semantics are defined (review 2026-09-06).** Instrument sets have no meaningful cycle ceiling (wear is per instrument, which is not modelled); dialysers do. `max_cycles` is the maximum number of **reprocessing cycles (reuses)**, not of uses: `cycle_count` counts completed reprocessings, a device is usable while `cycle_count <= max_cycles`, and the ceiling is enforced at **reprocessing** — a `reprocessed` request at `cycle_count >= max_cycles` discards with `max_cycles_reached`, so the total number of uses is `max_cycles + 1`. With `max_cycles = 1`: use (cycle 0) → reprocess (cycle 1) → use (cycle 1, the one permitted reuse) → post-use discards. Capture never refuses on the ceiling alone (a device at `cycle_count = max_cycles` is on its last permitted use); `mintDeviceTx` refuses an `initial_cycle_count > max_cycles` and admits `= max_cycles` (one last use). `max_cycles_snapshot` is nullable, the bound CHECK reads `max_cycles_snapshot IS NULL OR cycle_count <= max_cycles_snapshot`, and **every ceiling comparison in the lane carries an explicit non-null guard** — the cath rule at `cathDeviceReuseService.js:141` reads `Number(device.cycle_count) >= Number(device.max_cycles_snapshot)`, and `Number(null)` is `0`, so an unguarded copy would treat every unbounded device as at its ceiling. §8 tests the full lifecycle at `max_cycles` 1, 2 and unlimited.
- **Historical exposure and an operational HOLD are two different records (review 2026-09-06, §5.5).** `exposure_flag` is history: set once, never cleared, read by lookbacks. A `reprocessable_device_holds` row is the restriction: placed by the late-reactive handler, a failed or invalidated load, a `serology_required` return, an inspection failure, a prion event or a person; resolved only by an authorised infection-control decision that records adjudication, the processing protocol required and the evidence. Issue-time and capture-time logic check **holds**, never the historical flag — which is what makes a released, reprocessed set re-issuable.
- **One release path (review 2026-09-06, §5.6).** Every transition to `available` goes through `releaseToAvailableTx`, which dispatches on domain: dialysis applies the release criteria on an approved protocol and writes the statutory row or an append-only attempt; OT requires the applicable processing event. The generic CSSD `reprocessed` command delegates to it and cannot bypass either. Deactivating a policy stops new enrolment and new processing; tracking, holds and settlement of existing devices continue.
- **An approved protocol is a precondition of reprocessability (review 2026-09-06, §4.2a).** A category becomes reprocessable only with a `reprocessing_protocols` row naming the approved basis (manufacturer IFU / guideline), its approver and its parameters — the TCV threshold (floor 80 %), the agent list with contact-time and concentration bounds, the residual-test requirement, the pathogen-specific reuse matrix and the surveillance interval. A catalogue of models is a follow-up; the reference is minimal now and required now.
- **Exposure delivery is durable (review 2026-09-06, §5.7).** The exposure event is written to `bloodborne_exposure_outbox` in the **same transaction** as the marker row; the post-commit handler call is the fast path and the reconciler applies holds idempotently for anything it missed. Required before stage-3 activation.
- **Three decisions, three functions (review 2026-09-06, point 5; §5.3).** *What evidence exists* is the resolver's `Decision` (Phase 1) or `resolveReuseStatus` (OT) — status, evidence kind, evidence date, surveillance currency. *Where a patient dialyses* is `computeIsolationWarnings` (§3.4), which reads `status` and maps the class to a bay. *Whether a device may be reprocessed for this patient* is `reuseEligibility` (§5.3), which reads the tenant protocol's pathogen-specific matrix, the surveillance currency, the device's active holds and the ceiling, and returns a verdict **code**. `computeDispositionOptions` consumes the third and never re-derives the first two. One `Decision.status` no longer carries three jobs.
- **End of use is a precondition of release (review 2026-09-06, point 8).** Nothing makes a device `available` while its patient-use episode is open: the dialysis reprocessing record requires the session to be `completed` (`actual_end_at` is set by `completeSession`) or `cancelled` from `in_progress` (aborted — `actual_start_at` set; the only two exits from a started session under `SESSION_TRANSITIONS`); an OT set is released only after its issue has reached `returned`. A record on an `in_progress` session is refused (`RPD_USE_NOT_ENDED`) — a **behaviour change** from `main`, where `recordReuseRegister` checks no session status (§5.1).
- **Dialysis has one category.** `reprocessing_domain_policies.category` for `dialysis` admits `dialyser` only (bloodlines are out of this release, §1.1); `procedure_pack` is admitted for OT so packs can be tracked, but a `reprocessable = true` row on it is refused by CHECK (§4.2).
- **OT sets get one register row each, linked 1:1 by `instrument_set_id`; `instrument_sets.status` keeps its nine values and the CSSD board keeps reading it.** The register row carries only what the set lacks (cycle count, ceiling, exposure, the six-state reuse status, the last passed load). The two states are kept coherent by four hooks inside `cssdService`'s existing transactions (§5.2) — issue, return, cancel, load transition — and a unit test pins that every `set_issue_log` write path in `cssdService.js` calls the hook. Where they can still disagree (a set edited by a path that does not exist yet) the register's row is the reuse truth and the set's row is the board truth; the device read joins both and exposes `set_status` beside `status`.
- **Dialysis isolation and the dialysis reuse decision consume one resolver, owned by the Phase 1 lane.** This lane writes nothing to `dialysis_patients.*_status`, keeps no sync, no latch and no union read of its own, and reads `resolveDialysisIsolation` (§3.3) for every isolation and post-use decision — live, never from the frozen snapshot. The derivation itself, its migration (767) and its backfill are dev-1b's; the Phase 2 census that gates the eventual DROP is this lane's.
- **The dialysis reprocessing record is one command.** `POST /sessions/:id/reuse-register` (existing) performs post-use and reprocessing together — the reuse room writes one form, and that is the form the statutory row is. Its body gains TCV, agent and contact time; the cycle count becomes read-only.

### 2.6 The cath seam (not built here)

A later migration converging cath would: widen `reprocessable_devices_domain_check` and `reprocessable_device_usages_domain_check` to admit `cath` (drop and re-add by name — the forward pattern 765 used on 564's constraint); add `cath_catalog_item_id` as a third identity arm on the register and `cath_case_id` / `cath_usage_id` as a third owner pair on the usage table with the CHECKs widened to `num_nonnulls(...) = 1` over three; add `legacy_tag VARCHAR(24)` unique per tenant so a migrated device keeps the `RP…` label already affixed to it (the generated `device_tag` cannot be overridden); copy rows; re-declare `cath_inventory_authority_assert_contract_753`'s `reused_device` branch to read the new table; and leave `cath_reprocessable_devices` as a view. Nothing in `NNN` forecloses any of that, and nothing in `NNN` depends on it.

## 3. Serology gate

### 3.1 One resolver

`bloodborneMarkerService.resolveReuseStatus({ tenantId, patientUid, validityDays, asOf, db })` is already domain-free (it is patient-scoped and reads only `patient_bloodborne_markers`). **OT** calls it with the OT `serology_validity_days` from `reprocessing_domain_settings` (`instrument_sets` and `ot_schedules` carry no legacy serology). **Dialysis does not call it at all**: capture, the reprocessing record, the session read and the isolation rule read the Phase 1 resolver's `Decision` (§3.3), whose rule differs from this one on purpose — no validity window, and the retained roster columns as a legacy-declaration input. So "one resolver" is one *per record shape*: the cath resolver for cath and OT, the roster resolver for dialysis; neither lane re-implements the other's. The evidence-freezing rule from the cath spec §7.4 holds, with one sharpening stated in §3.3: `reuse_screen` (capture) and `post_use_screen` (return) on `reprocessable_device_usages` are evidence snapshots, written once and never an input to a later decision; every decision re-resolves live, and a later result changes the device through the exposure handler, never the historical rows. Both resolvers answer only *what evidence exists*; neither decides where a patient dialyses (§3.4) or whether a device may be reprocessed (§5.3) — the three decisions are separate functions (§2).

### 3.2 One exposure handler, two domain arms

`reprocessableDeviceService.quarantineDevicesExposedToPatient(event)` registers with `registerExposureHandler` at module load, exactly as `cathDeviceReuseService.js:1262` does. The registry is already domain-neutral; a second handler is a second `Set` member. For a reactive event `{ tenantId, patientUid, marker, testedOn, markerRowId }`:

- **dialysis arm** — every device whose `reprocessable_device_dialysis_links.dedicated_patient_uid` is the patient and whose status is not `discarded` (no window: a dedicated dialyser is that patient's for life, and the exposure is the patient's own history), plus any dialysis device with an open usage on that patient;
- **OT arm** — every OT device with a usage row for that patient with `captured_at >= tested_on − serology_validity_days` (Asia/Kolkata midnight boundary, the same `AT TIME ZONE` pin as 765), all uses for `cjd_suspected`. **`tested_on` may be NULL** once Phase 1's backfill writes legacy declarations (the hand-over note in §3.3 says NULL, never an invented date; `764:57` declares `tested_on DATE NOT NULL` today, so 767 relaxes it and this handler must not assume a value). A date-window comparison against NULL selects nothing, silently — the exact failure the owner named. **Decision: an undated declaration uses an unbounded lookback with infection-control review.** The predicate is `($3::date IS NULL OR u.captured_at >= …)`; every usage of that patient in the register is a candidate, each hold it places carries `reason_code = 'exposure_undated_declaration'`, and the IC notification says the evidence is undated. Justification: the register's own history bounds the sweep (usages exist only since this platform went live on the tenant, so "unbounded" is a few months, not a decade); a documented cutoff would have to be invented by the platform, which is the thing the hand-over note forbids; and a set used on a known carrier at any recorded time is at least worth an IC officer's look, which is what a hold is. The dialysis arm has no window in any case.
- each candidate is settled in its **own** transaction (the Plan 2 resilience contract: one lock timeout must not roll back nine quarantines), and **every write is marker-free** (D11(e), §2): `available` / `awaiting_reprocessing` / `in_cssd` → a **hold** (`placeHoldTx`, `hold_type = 'bloodborne_exposure'` or `'prion_exposure'` for `cjd_suspected`, `reason_code = 'exposure_late_result'` / `'exposure_undated_declaration'`, `source_marker_row_id = markerRowId`) plus the `quarantine` transition with `quarantine_reason = 'exposure_hold'` (a code, §4.3) and `exposure_flag = true`; `in_case` → the same hold row (status `active`, `pending_return = true`) and the flag, no transition — the hold is what the return path then reads (§5.2, §5.1); already `quarantined` → the hold row is added beside the existing one (one active hold per type per device; a second event of the same type updates `last_seen_marker_row_id`); `discarded` re-checked under the lock and skipped. The device row, the usage row, the audit metadata (`rpd.device.exposure_flagged` carries `{ hold_id, hold_type, reason_code, status, domain }`) and the transition metadata **never name the marker**; the hold row's `source_marker_row_id` is the only pointer, and hold rows are readable only through the governance mount (§6.4) and the CSSD release dialog for the IC role (§6.2);
- **nothing is written to `dialysis_patients`** — the roster derives its state at read time through the Phase 1 resolver (§3.3), so the handler has no sync step;
- then one `cds_alerts` row (`alert_type = 'bloodborne_reuse_exposure'`, `severity = 'high'`, description naming the domain, the device tags and "a reactive blood-borne marker" — **not the marker**: `cds_alerts` is a patient-scoped table with readers outside the serology audience) and one `notification_outbox` row per active `INFECTION_CONTROL_OFFICER` (`type = 'bloodborne_reuse_exposure'`, `sourceEventKey = 'bloodborne-reuse-exposure:rpd:<markerRowId>:<officer uid>'`, body marker-free, deep link `/dashboard/cssd?tab=devices&domain=<domain>&hold=<id>` where the IC officer — an audience role — reads the hold and, through it, the marker row), raised for whatever subset settled even when others failed, and **idempotent under replay** (§5.7): the alert is skipped when a `cds_alerts` row with the same `source_data.marker_row_id` already exists, and the notification's `sourceEventKey` already deduplicates. The existing notification channel is `notificationOutbox.queue` (`utils/notifications/notificationOutbox.js`), exactly as `cathDeviceReuseService.quarantineDevicesExposedToPatient` uses it on `main`.
- **Handler registration is explicit.** `exposureHandlerBootstrap.js` (#1016, on `main`) imports `cathDeviceReuseService.js` for its side effect and states that a new handler owner is added there; this lane adds `import './reprocessableDeviceService.js';` to it, and `exposureHandlerCount()` is asserted to be **2** in the platform's own unit test and in the reconciliation script's pre-flight (`reconcile-bloodborne-markers.mjs` refuses `--apply` at zero; it now also prints the count and the plan expects 2).

**Durable delivery (review 2026-09-06, §5.7).** The crash window between the marker commit and the handler call is no longer a notification problem: patient A's set is used, the reactive result commits, the process dies before the handler runs, and patient B — clear — is assessed later; re-resolving B cannot discover A's missed exposure, because the missing information is the device's hold. So the exposure event is written to **`bloodborne_exposure_outbox` in the same transaction as the marker row** (`enqueueExposureEventsTx`, called by `bloodborneMarkerService.recordMarkers` and `recordMarkersFromSignedResults` inside their `setTenantTx` before commit — the two writers on `main` that call `notifyExposureHandlers` post-commit), the post-commit fan-out is the fast path that marks the row `delivered` per handler, and a reconciler re-drives every undelivered row idempotently. It is a table of its own, modelled on `event_outbox`'s lease / backoff / dead-letter shape (mig 588) rather than written to it — `event_outbox`'s only consumer is the webhook fan-out (`completeClaimedEventFanout` writes `webhook_deliveries`), so a row there would be claimed by the wrong consumer and marked delivered with nothing done. The design is in §5.7; it is a precondition of stage-3 activation (§1.2).

### 3.3 Isolation derivation — dependency on the derivation lane

**What changed and why.** The first draft of this section derived `dialysis_patients.hbsag_status / hcv_status / hiv_status` from the marker record inside this lane: a service-side sync with a no-downgrade latch, a union read of the two records during transition, a `dialysis_surveillance` marker source, an enrol-body guard and a one-shot backfill script. That collided head-on with the **dialysis isolation derivation lane** (dev-1b), which owns the same three columns, the same enrolment default and the same roster surfaces. A decision trio (advocate / challenger / supervisor, 2026-09-05) ruled **two-phase**, and this document now treats the carve as settled: Phase 1 owns derivation, resolver and migration 767; Plan 4 consumes the resolver and owns the Phase 2 census.

**Phase 1 — the derivation lane (dev-1b; migration 767).** Not built here; stated so this lane's dependency is explicit:

- A **read-time resolver is the only input to any isolation decision**. The three `dialysis_patients.*_status` columns are **retained, read-only**, as legacy-declaration inputs to a union read inside the resolver. There is **no sync write** onto them and **no no-downgrade latch**: a column is a declaration made by a person on a date, and the resolver weighs it as one input beside the marker rows.
- The isolation rule is **distinct from `computeReuseStatus`** (the cath resolver): a non-voided reactive marker → `restricted`; any non-voided non-reactive → `clear` **regardless of age**; nothing → `unknown`. The cath rule's 90-day `clear` window is wrong for a roster: with zero markers cluster-wide today, a window would leave every marker-less roster `unknown` forever. **The legacy-column arm is asymmetric (Q1 as revised, below):** a `'positive'` column restricts on its own; a `'negative'` column is not evidence of anything.
- `unknown` **never isolates and never blocks**. Enrolment writes `'unknown'`, not `'negative'` (the `'negative'` default was a claim nobody made). Amber renders as a **roster chip only**; no per-session warning fires for it — `DIALYSIS_SEROLOGY_UNKNOWN` **does not exist** (§3.4). Unknown-isolates would isolate every patient on day one.
- Invariant (d): a voided reactive with **no other evidence** falls back to the **column**, not to `clear` — a void un-isolates only when the marker was the sole evidence.

**The resolver contract this lane consumes** (agreed with dev-1b in full, 2026-09-05 — there is no open point left):

```
resolveDialysisIsolation({ tenantId, patientUids, db,
                           includeMarkers = false, includeIsolationClass = false })
  → Map<patientUid, Decision>            // batch-first; callable inside a tx with the tx client
Decision = {
  status:   'restricted' | 'unknown' | 'clear',
  asOf,                                  // when the resolver evaluated
  reasons:  [],                          // human text; empty unless the caller is in the audience
  evidence: 'marker' | 'legacy_declaration' | 'none',
  isolation_class?: 'hbsag' | 'hcv' | 'hiv' | 'isolation_mixed' | null,
                                         // ONLY when includeIsolationClass === true;
                                         // null unless status === 'restricted'
  markers?: [...]                        // ONLY when includeMarkers === true
}
isolated === (status === 'restricted')   // derived, never stored as a second flag
```

- A thin single-uid wrapper exists for the per-session paths; this lane binds to both through one adapter (`dialysisReuseService.isolationDecisionTx`), so a Phase 1 rename is a one-line change here.
- **No boolean pair.** `{ isolated, unknown }` admits an illegal fourth state; `status` is the one field.
- **Marker detail is opt-in and default off**, so a caller that forgets gets the safe shape. **No caller in this lane passes `includeMarkers: true`.** The one surface that may — the roster / patient-detail read behind `roleSeesSerologyDetail` — is Phase 1's roster work; this lane does not attach a resolver call to the existing `GET /patients/:id` (an existing read must not start answering 503 on a build without Phase 1). The adapter carries the flag so that surface has one seam to use; the session evaluator (`assessIsolationTx`), capture, the reprocessing record, the session read and the CSSD paths never forward it.
- **`isolation_class` is opt-in on the same footing as marker detail, and this lane's ask is settled that way.** Plan 4 first asked for it as an unconditional fifth field. dev-1b refused, and was right: a value of `'hiv'` **is** serology whatever the caller calls it, and "it is a routing instruction, not a marker value" is the same reframing that produced the two earlier disclosures this canary exists to catch. So the field is **present only when `includeIsolationClass: true`**, and **`null` unless `status === 'restricted'`** — a caller that forgets gets the safe shape, exactly as with `includeMarkers`. Phase 1 computes it from the rule this lane hands over: one reactive core marker → its class; two or more → `isolation_mixed`; `restricted` for a marker with no machine class (`cjd_suspected`, other) → `isolation_mixed`; not restricted → `null`.
- **Who may pass `includeIsolationClass: true`.** Under D11 = NO, **exactly two** server-side paths (review 2026-09-06 raised this from one): the **session-evaluation path** — `assessIsolationTx`, which does the read, and `evaluateIsolationForSessionTx`, which wraps it for start and reassign (§3.4) — where the class is mapped to the tenant's routing group through `reprocessing_domain_settings.isolation_groups` (§4.1); and the **reuse-eligibility path** — `reuseEligibilityTx`, called by the dialysis post-use record (§5.1) and by the OT return hook for an OT patient's `Decision`-shaped restriction only when the tenant protocol's matrix is pathogen-specific (§5.3) — where the class indexes the protocol's reuse matrix (HBsAg-reactive → no reuse; HIV-reactive → no reuse; HCV per protocol). Both are in-process decisions: the class is **never rendered, never serialised, never snapshotted and never audited** by either; what leaves each frame is a group label or an eligibility verdict code. The roster, the today board, the case / session lists, patient detail, capture, the session read and every CSSD path **never pass it**, so their `Decision` has no `isolation_class` key at all. The plan pins the asker count at **two, by function name** (§8).
- **Null-class semantics are defined per status and per flag (review 2026-09-06, point 7).** With `includeIsolationClass: true`: `status = 'restricted'` **requires** a class in the four-value set (absent or `null` → `RPD_ISOLATION_DECISION_INVALID`); `status = 'clear'` or `'unknown'` **requires `null`** (a non-null class on a non-restricted Decision is a resolver defect → `RPD_ISOLATION_DECISION_INVALID`). With the flag **off**: the key is normally **absent** and that is the valid shape; a key that is *present* is tolerated only when `null`, and a present non-null class the caller never asked for is refused too — an unrequested class is exactly the disclosure the opt-in contract exists to prevent, and failing closed there costs nothing. §8 tests all three statuses under both flag settings; the adapter's `assertDecisionShape(decision, askedForClass)` is the one place the rule lives.
- **The adapter's fail-closed check keys on the ask, not on presence.** `RPD_ISOLATION_DECISION_INVALID` (500) fires on the table above — a real invariant, because a wrong shape must not route a patient or admit a dialyser to reuse. Asserting presence unconditionally would only re-create the rejected contract inside our own code.

**Q1 as revised — the legacy columns are ASYMMETRIC evidence (owner, 2026-09-06, via dev-1b; INPUT to the Phase 1 lane's contract, which owns migration 767 and this rule; #1014 consumes it and says so).** Q1 was first answered on 2026-09-06 as "a legacy `'negative'` means `clear`" and **revised the same day** once the schema fact was in front of the owner: `168` declares each column `DEFAULT 'negative'` and `enrolPatient` writes `COALESCE($8, 'negative'), COALESCE($9, 'negative'), COALESCE($10, 'negative')` when serology is omitted, so a bare `'negative'` is indistinguishable from *nobody having said anything*, and the literal rule would have resolved every never-tested patient as `clear` — fail-open on blood-borne isolation. The precise rule, written here exactly as dev-1b stated it so the next reader cannot soften it:

- a legacy **`'positive'`** contributes **`restricted` on its own**, with no corroboration required — it is never the default and is written only by explicit enrolment input or by `recordSerology`'s promotion `UPDATE` (`dialysisService.js` ≈1160-1170), so it has provenance. Applying a corroboration requirement to this arm would **un-isolate declared positives** — also fail-open — which is why the rule is asymmetric rather than a symmetric "corroborate both";
- a legacy **`'negative'`** contributes **nothing**. It is **not an input to `clear`** — not a weaker clear, not an AND with corroborating evidence: when a `dialysis_serology` row or a `patient_bloodborne_markers` row exists for the analyte, **that evidence decides by itself** and the column never changes an outcome. Write it as "the negative column is not evidence", never as "negative + evidence = clear" — an AND keeps a meaningless column load-bearing, and the next reader will not know it is inert;
- only a `'negative'` column and **no** marker row and **no** `dialysis_serology` row for the analyte → **`unknown`**;
- invariant (d) is intact and is a **positive** fallback: a voided reactive with no other evidence falls back to the column, which can only re-restrict (a `'positive'`) and can never clear.

**Per-marker semantics** (an input to Phase 1, so the roster rule matches `computeReuseStatus` on this one point while keeping its window-less semantics — `bloodborneMarkerRules.js`, `CORE_MARKERS.every(...)`): `clear` requires **each of the three core analytes** to be cleared by a non-voided non-reactive `patient_bloodborne_markers` row or a non-reactive `dialysis_serology` value (`hbsag`, `anti_hcv`, `hiv`; `168`); one cleared analyte never clears an untested one. A `dialysis_serology` row is human-entered, dated (`test_date`), attributed (`reported_by`), and since #1024 stamped with its own tenant. The legacy negative is not what corroborates that result: the evidence row decides by itself. `evidence: 'legacy_declaration'` therefore arises only from a **positive** roster column; a negative-only roster value with no evidence row remains `evidence: 'none'` and resolves `unknown`.

**Evidence date and surveillance currency stay separate from cohort status.** The `Decision` gains `evidence_dated_on` (the latest per-analyte date; `null` when any deciding evidence is undated) and `surveillance_overdue: boolean` against the tenant protocol's surveillance interval (`168`'s header states baseline plus three-monthly HBsAg/HCV and annual HIV; the interval is configured on the protocol, §4.2a). Routing reads `status` alone. Reuse eligibility (§5.3) reads `surveillance_overdue` and treats overdue evidence as unknown for reprocessing when the protocol says so (default `true`). Neither decision reads a legacy negative. Pre-#1024 `dialysis_serology` rows can carry the default tenant id, so migration 767 must either repair that tenant stamp or resolve them through the in-tenant `dialysis_patient_id`; and `dialysis_serology` admits both `'positive'` and `'reactive'` while `recordSerology` promotes only `'positive'`. `dialysis_patients` has no `updated_by`, so the roster row cannot prove provenance.

**Consequences for this lane:** day-one amber is higher than the literal Q1 predicted — every roster row whose three columns are only the enrolment default resolves `unknown` — and under `unknown_serology_rule = 'warn'` each such patient's first dialyser reprocessing needs an acknowledgement. The census in §7.6 reports declared-and-evidence-backed negative observations separately from defaulted negative-shaped roster values. Because today's roster schema cannot distinguish a deliberately entered `'negative'` from its default, the honest column-only declared-negative count is **zero** unless a serology row backs the observation; a large number is a census bug, not a finding.

**Snapshot versus decision** (dev-1b's fourth point). `reuse_screen`, frozen on the usage row at dialyser capture, is an **evidence snapshot**: "what was known when this dialyser was captured". It is written once, is never re-read as current state, and is never an input to any decision. Both decisions this lane makes for dialysis **re-resolve live** through the resolver: schedule / start / reassign machine routing (§3.4) and the post-use reprocess / quarantine / discard decision (§5.1). The snapshot exists so an audit can see what the capturing user saw; a marker recorded between capture and start must reach the machine assignment, and one recorded between capture and the reprocessing record must reach the disposition. `post_use_screen` is the same kind of object: the `Decision` the reprocessing record acted on, frozen after the fact. **Neither snapshot carries marker detail or `isolation_class`**. `assessIsolationTx` consumes the class for class-to-group routing and `reuseEligibilityTx` consumes it for the server-side matrix before any response or snapshot is built; the adapter's `snapshotOf` has no `keepClass` form. On response, `projectUsageForRole` also empties `reasons` and removes marker/class keys recursively from both frozen screens for non-audience roles (§3.5).

**Exposure on a register device (D11 = NO, widened by review 2026-09-06: the flag and a hold, never a marker — both domains, every writer).** `reprocessable_devices` has **no `exposure_markers` column** (§4.3). `returnDeviceTx` sets `exposure_flag = true` when the live restriction is `restricted` (the dialysis `Decision` or the OT `resolveReuseStatus` object — it reads `status` only) and, when the disposition is `quarantine`, places a hold (§5.5) whose `source_marker_row_id` points at the reactive marker row the restriction was resolved from when the resolver exposes one to a server-side caller (`includeMarkers` is never forwarded by this lane; for OT `resolveReuseStatus` carries `markers` server-side and the hook reads the reactive row's id there, never its name). The rules helper is `exposureStamp(restriction)` → `{ exposure_flag }`; there is **no** class → marker map, no marker array and no `metadata.exposure_isolation_class` anywhere in the lane. The **CSSD queue reads the flag and the hold status**, and universal precautions apply to a flagged device whatever isolated the patient — which is all a queue with no patient identity (D2) could act on anyway. The late-reactive handler (§3.2) writes the same shape. The earlier text made the OT arm the exception ("still stamps from `resolveReuseStatus`'s markers"); that exception is **withdrawn** — a uniform marker-free register is the design, and the OT marker evidence is the hold → marker-row pointer like everything else.

**Phase 2 gate — this lane's deliverable.** Before any migration drops the three columns, a per-tenant census must read zero on the two counts below **and** show zero pre/post differences on the third (§7.6) in every production tenant:

1. **Orphan `patient_uid` count** — `dialysis_patients` rows whose `(tenant_id, patient_uid)` has no `users` row. `168_dialysis_unit.sql:30` declares `patient_uid UUID NOT NULL` with **no FK**, while `764:69` (`fk_patient_bloodborne_markers_patient`) requires one; such a row can therefore **never hold a marker**, and the resolver can never see anything but its column. Until every orphan is re-pointed or retired, the column is that patient's only record.
2. **Un-backfilled positive count** — roster rows with a `'positive'` column and no non-voided reactive marker for that marker, computed with `bool_or` **across every roster row of the patient** (a re-enrolled patient has more than one; `ORDER BY id DESC LIMIT 1` would miss the older one).
3. **Decision-bearing declarations and the pre/post comparison (review 2026-09-06, point 10).** Every legacy declaration that still changes a decision under the asymmetric rule — therefore every `'positive'` column — is counted with provenance. Negative-shaped roster values are split into those accompanied by independent non-reactive evidence and those that are only defaults; the evidence, never the negative column, decides the first group. For **every** roster patient the resolver's `status` **and** routing `required_group` are computed with the columns and with the columns simulated absent; the DROP requires **zero differences**. Defaulted negatives resolve `unknown` before and after and are reported for planning, never gated.

`apps/backend/scripts/dialysis-isolation-census.mjs` (§7.6) prints all three per tenant; the third (`--compare-drop`) needs the Phase 1 resolver, the first two do not. The census gates the **DROP** — a **future migration outside Plan 4** — and never the additive migration `NNN`. Blast radius for whoever writes it: the `dialysis_today` view selects `p.isolation_required` (`000_baseline.sql:8081`), a generated column over the three; the partial index the roster uses; and a **second, unrelated** `isolation_required` on `infection_cases` (`000_baseline.sql:10259`, declared by mig 078:91) — a name-match in that migration is the wrong-referent trap, and a grep that stops at the first hit will find the wrong table.

**Removed from this lane and handed over** (one-paragraph pointer; the design lives in dev-1b's lane): the union read implementation, `syncDialysisPatientSerology`, the marker void → sync hook and its registry, the `@default("negative") → "unknown"` flip in `schema.prisma:9107-9109`, the enrol-body guard (`DIALYSIS_SEROLOGY_FIELDS_NOT_ALLOWED`), `scripts/backfill-dialysis-markers.mjs`, the `dialysis_surveillance` source value and the 764 CHECK-pair change, `recordSerology`'s re-pointing, and every test of those. Hand-over notes dev-1b should have:

- A legacy `'positive'` becomes a `clinical_declaration` marker under a **named actor** (a SQL backfill cannot supply `recorded_by`); `tested_on` is **NULL for a legacy row**, not `updated_at::date` — `updated_at` moves on any edit (a dry-weight change), and a date the platform did not know must not be invented.
- `recordMarkers` fires the exposure handlers post-commit (`bloodborneMarkerService.js:262`), so a one-shot backfill quarantines **every legacy-positive patient's dedicated dialyser** (and flags the `in_case` ones) and every OT set used on them inside the window, through both this lane's handler (§3.2) and cath's, in one burst. The dry run must print the **device count** and the run must be planned with infection control.
- The census's `bool_or` over the roster is the read shape for the legacy declaration inside the union read too.
- Under Q1 as revised the negative column is **inert**: the union read must not consult it for `clear`; a `dialysis_serology` non-reactive value is the only clearing evidence a declaration can supply, and `evidence: 'legacy_declaration'` is reserved for a positive column.
- `dialysis_serology` admits both `'positive'` and `'reactive'` (`168:236-240`), but the seroconversion test at `dialysisService.js:1115-1117` and the column promotion at `:1135` read only `'positive'` — a `'reactive'` entry is silently not promoted. That disjointness is dev-1b's, with the columns.
- The negatives-lag and the no-correction-path follow-ups recorded by review round 1 were properties of the sync; with no sync they become Phase 1's questions (Q2 and Q3, §9).

**Kept in this lane (unblocked, additive):** `dialysis_machines.isolation_group` and the class → group mapping on `reprocessing_domain_settings` (§4.1, §4.6); `evaluateIsolationForSessionTx` / `assessIsolationTx` (warn-only, per-tenant `block`), calling the resolver with `includeIsolationClass: true` and mapping to a group server-side; the warning payload keyed on `required_group` with **no** class key (§3.4); dialyser identities, the register and the 418 grain; the OT / CSSD arms; the reprocessable-devices tables; the un-capture (G1). The dialysis-arm tasks that call the resolver are marked **DEPENDS ON Phase 1 landing on `main`**: a named interface stub is acceptable for unit tests; the dialysis deep suite runs only once the resolver is on `main`, behind a named guard, never against a stub.

### 3.4 The dialysis isolation WARN rule

Evaluated in `startSession` and `scheduleSession` (both existing) for the session's machine and patient, and re-evaluated by `PATCH /sessions/:id/machine` (new, §6.1). Every evaluation reads a **live `Decision`** from the Phase 1 resolver (§3.3) — never the frozen `reuse_screen` — so a marker recorded between capture and start reaches the machine assignment. **DEPENDS ON Phase 1.** In `scheduleSession` the evaluation runs **before** the `INSERT`, inside one `setTenantTx` (today's `scheduleSession` inserts with the bare client outside any transaction — `dialysisService.js:466-481` — so a `block` refusal raised after the insert would leave an orphan `scheduled` row): `assessIsolationTx` reads and decides without writing, the `INSERT` itself carries the isolation columns, and a `DIALYSIS_ISOLATION_MACHINE_BLOCKED` at scheduling leaves no row (a deep test asserts the count). `startSession` and the reassign route call `evaluateIsolationForSessionTx`, which is `assessIsolationTx` plus the `UPDATE` onto the existing row. Pure function `computeIsolationWarnings({ decision, machine, isolationGroups, enforcement })` — `isolationGroups` is the tenant’s class → group map from `reprocessing_domain_settings` (§4.1), so the mapping is data the caller supplies and never a table the rules module owns:

| Condition | Code | Severity under `warn` | Under `block` (review 2026-09-06: no escapes) |
|---|---|---|---|
| decision `restricted` and no `dialysis_machines` row for `machine_no` | `DIALYSIS_MACHINE_UNREGISTERED` | warn | **block** — a restricted patient cannot be started on a machine the unit has not registered |
| decision `restricted`, machine registered, machine's `isolation_group` does not satisfy the patient's `required_group` (rule below) | `DIALYSIS_ISOLATION_MACHINE_MISMATCH` (the marker class stays server-side; the payload names the **group**) | warn | block |
| decision `restricted`, the tenant has **not mapped** the class (`required_group` null) | `DIALYSIS_ISOLATION_GROUP_UNMAPPED` (new) | warn — any non-null group satisfies, as before | **block** — an unmapped class is a refusal; block mode is not entered with an incomplete map (precondition below) |
| decision `unknown` | **nothing** — `unknown` never isolates and never blocks (§3.3); the amber state is a roster chip owned by Phase 1, not a session warning | — | — |
| decision `clear`, machine registered, `isolation_group IS NOT NULL` | `DIALYSIS_GENERAL_PATIENT_ON_ISOLATION_MACHINE` | warn | warn |

A clear patient on an unregistered machine produces nothing — otherwise every session on day one warns, and a warning nobody reads is not a warning; an `unknown` patient produces nothing for the same reason multiplied by a marker-less cluster. **The required group** is `settings.isolation_groups[Decision.isolation_class]`: Phase 1 computes the class by the rule handed over in §3.3 (`hbsag → hbsag`, `hcv → hcv`, `hiv → hiv`; two reactive markers → `isolation_mixed`), and this lane maps it to the tenant's own label server-side, deriving neither from markers. **Satisfaction rule:** a machine with `isolation_group IS NULL` is a general machine and never satisfies a restricted patient; when the tenant has mapped the class the two labels must be equal; when it has **not** (`required_group` is `null` — the day-one state) any non-null group satisfies **under `warn` only**, because the patient is at least on an isolation machine and the platform must not invent a bay name. There is no `isolation_mixed` machine any more: a bay that takes two marker classes is one label both classes map to (§4.1). When the code list is non-empty, `startSession` requires `isolation_override_reason` (400 `DIALYSIS_ISOLATION_OVERRIDE_REQUIRED`) and records it with actor and time; under `block`, `UNREGISTERED`, `MISMATCH` and `UNMAPPED` all refuse with 409 `DIALYSIS_ISOLATION_MACHINE_BLOCKED` (carrying `codes`) regardless of reason. The codes, the required group, the mode pair and the override are persisted on `dialysis_sessions` (§4.7) and audited as `dialysis.session.isolation_overridden`.

**Preconditions for `block` (review 2026-09-06).** `PUT /domains/dialysis/settings` with `isolation_enforcement = 'block'` is refused with 409 `RPD_ISOLATION_BLOCK_PRECONDITIONS` (carrying `unmapped_classes[]`, `groups_on_no_active_machine[]`, `active_machines`) unless: every one of the four classes is mapped; every mapped group is carried by at least one **active** `dialysis_machines` row; and the tenant has at least one active machine. A later machine retirement or a mapping edit that would break these while `block` is on is refused with the same code — the mapping stays validated for as long as it is enforced. **Emergency exception — an explicit authorised workflow, not a loophole.** `POST /api/v1/dialysis/sessions/:id/isolation-emergency-override` `{ reason }` (scope `dialysis_isolation_emergency`, roles `DIALYSIS_EMERGENCY_OVERRIDE_ROUTE_ROLES = DIALYSIS_ROUTE_ROLES ∩ {NURSING_INCHARGE, IP_INCHARGE, ICU_INCHARGE, CONSULTANT, ADMIN, SUPER_ADMIN}` — an intersection, asserted as a strict subset in the wiring test, approved membership) records `isolation_override_kind = 'emergency'` on **this session only**, writes a `medication_safety_reviews` row (`finding_code = 'ISOLATION_EMERGENCY_OVERRIDE'`, §5.4), audits `dialysis.session.isolation_emergency_override`, and queues an IC notification; only then does `startSession` on that session proceed past a block, and the payload's `override` names the kind. A dialysis technician cannot invoke it.

**The user-facing payload (D11 = NO, §2).** `computeIsolationWarnings` returns `{ codes, required_group, blocked }`. The marker class is an **internal intermediate only**: a local inside that function, read off the live `Decision` to index `settings.isolation_groups`, and a key of nothing — not the return value, not the internal isolation object, not an audit row, not a response. What the API emits:

```
isolation: {
  codes:              ['DIALYSIS_ISOLATION_MACHINE_MISMATCH', …],       // the persisted flat list
  isolation_warnings: [{ code, machine_id, severity, required_group }], // one entry per code; GROUP, never class
  warn_only, enforcement_enabled, override
}
```

`machine_id` is the `dialysis_machines.id` the warning is about and is `null` for `DIALYSIS_MACHINE_UNREGISTERED`; `severity` is `'warn'` or `'block'` from the table above; `required_group` is the tenant's label for the bay this patient belongs in, and is `null` when the patient is not restricted or the tenant has not mapped the class. The `DIALYSIS_ISOLATION_MACHINE_BLOCKED` 409 carries `{ codes, required_group }` and names the group in its message ("… must be dialysed on a machine in isolation group Bay 1", degrading to "… on an isolation machine" when `required_group` is `null`). `required_class` **does not exist** on any of these. `DialysisIsolation`'s OpenAPI schema is `additionalProperties: false` (§6.6) and lists `required_group` and never a class, so a payload that starts carrying a class fails schema validation before it reaches a reviewer — the shape is enforced, not merely documented.

### 3.5 Projection for non-audience roles

`DIALYSIS_ROUTE_ROLES` admits three roles outside `roleSeesSerologyDetail` (`CLINICAL_STAFF_ROUTE_ROLES`): `BLOOD_BANK_TECHNICIAN`, `BLOOD_BANK_STAFF` and **`DIALYSIS_TECHNICIAN`**. `CSSD_ROUTE_ROLES` admits six (`STORES_PURCHASE_INCHARGE`, `HR_STAFF`, `QUALITY_OFFICER`, `INFECTION_CONTROL_OFFICER`, `DATA_PROTECTION_OFFICER`, `COMPLIANCE_OFFICER`). `THEATRE_ROUTE_ROLES` admits none. Every read surface added or changed by this lane projects by role through the **same predicate** (`cathDeviceReuseService.roleSeesSerologyDetail`, deliberately not a second list), keys blanked never dropped:

- `reuse_restriction` on every payload: `projectReuseRestrictionForRole` (existing) — for OT the cath shape: `status`, `validity_days`, `evaluated_at` survive; `reasons` and `markers` empty. For dialysis the payload is the resolver's `Decision` (§3.3): `status`, `asOf`, `evidence` survive; `reasons` emptied; `isolation_class` **blanked to `null`** for a non-audience role (keys blanked, never dropped) — belt and braces, because no read surface in this lane passes `includeIsolationClass`, so the key is absent on these payloads to begin with. `markers` is never present on these surfaces (no caller in this lane forwards `includeMarkers`; the projection drops the key anyway, and the canary's poisoned Decision carries one so a forwarding route fails).
- **The frozen `reuse_screen` and `post_use_screen` are sanitised ON RESPONSE (review 2026-09-06, point 1).** They are stored complete — `reasons` included, because the audit purpose of a snapshot is to show what the capturing user saw — but they are **never returned raw**. Every surface that returns a usage row (`GET /sessions/:id/dialyser`, the capture response, `GET /reprocessing/devices/:id/history`, the theatre sets read) passes the usage through `projectUsageForRole(usage, role)`, which applies `sanitizeScreenForRole` to both screens: for a non-audience role `markers` is dropped, `isolation_class` is blanked to `null`, and **`reasons` is emptied** — removing the marker keys does not make free text like "HBsAg reactive 2026-08-12" safe. For an audience role the screens pass unchanged. The `ReprocessableDeviceUsage` schema declares both screens as `DialysisIsolationDecision`-or-`BloodborneReuseStatus` shapes with `additionalProperties: false`, and the canary poisons the stored screen's `reasons` with the sentinel so an unprojected usage fails (§3.6). A usage row is therefore the fourth projected object beside the restriction, the roster row and the serology row, through the same predicate.
- `isolation` / `isolation_warnings[]`: **still not projected — because there is nothing in them to project.** `codes`, `machine_id`, `severity` and `required_group` reach every dialysis role: whoever puts the patient on a machine must know that a warning fired, which machine it is about and which bay the patient belongs in. `required_group` is a tenant's label for a room, not a clinical value — that is the whole point of D11 = NO — while the marker class does not reach them and does not exist in the payload at all (§3.4). Review round 1's Fix-6 — "do not blank `required_class`; it is a routing instruction, and blanking protects nothing because `GET /machines` publishes every machine's class and the two mismatch codes narrow it by elimination in at most three attempts" — is **SUPERSEDED** by D11: the recoverability argument was a reason to relabel the machine layer, which is what the owner chose, and not a reason to publish the class outright; the same "it is a routing instruction, not a marker value" reframing is what dev-1b rejected on the resolver contract. Under the relabel that elimination recovers a bay name and stops there. The canary pins the *value*, not the key (§3.6).
- `dialysis_patients` rows on roster, today board and patient detail: `hbsag_status`, `hcv_status`, `hiv_status` → `null`; `isolation_required` survives (the advisory). `dialysis_serology` rows in patient detail: `hbsag`, `hbs_titre`, `anti_hcv`, `hcv_pcr`, `hiv` → `null`, `is_seroconversion` → `false`; `test_date` survives.
- The CSSD queue carries no patient identity and therefore nothing to project; the canary proves it (§3.6).

**D10 consequence (decided per role; D11 is also decided).** The dialysis technician sees the restriction status, isolation codes, machine, and required **group** — enough to route — but not marker values, reasons, lab rows, or a marker class. D10a/b/c are each NO in this lane (§2): neither the technician nor either blood-bank role joins the serology-detail audience. The actual-assignment census still runs because provisioning policy cannot prove that no account already holds a role. A future audience widening would be a separate reviewed change and would not put a class into a routing payload.

### 3.6 The canary gains four mounts

`serologyDisclosureCanary.test.js` enumerates exactly three mounts today (`/api/v1/cath-lab`, `/api/v1/stemi-pathway`, `/api/v1/cath-reprocessing`). It gains `/api/v1/dialysis` (`DIALYSIS_ROUTE_ROLES`), `/api/v1/cssd` (`CSSD_ROUTE_ROLES`), `/api/v1/theatre` for the one sets read, and the new `/api/v1/reprocessing` (§6.4). The stubbed persistence layer poisons the dialysis roster row with `hbsag_status = SENTINEL`, a `dialysis_serology` row with `hbsag = SENTINEL`, and both resolvers' stubs — `resolveReuseStatus` for OT and the Phase 1 `Decision` for dialysis — return sentinel-bearing `reasons`, so every unprojected `reuse_restriction` would leak. **Coverage widened (review 2026-09-06, point 1) to the four places a marker could travel that are not a restriction object:** (i) **device fields** — the stubbed device row carries `quarantine_reason: SENTINEL` and `discard_note: SENTINEL`, and the predicate fails any device-shaped node (a node with `device_tag`) whose `quarantine_reason` is not in `QUARANTINE_REASON_CODES` or whose free-text fields carry a marker word; (ii) **snapshots** — the stubbed usage's `reuse_screen.reasons` and `post_use_screen.reasons` carry the sentinel, so an unprojected usage on any surface fails; (iii) **error bodies** — every 4xx/409 the sweep provokes (a `RPD_DISPOSITION_NOT_ALLOWED`, a `DIALYSIS_ISOLATION_MACHINE_BLOCKED`, a `CSSD_SET_QUARANTINED`) is walked with the same predicate, and the stub makes the underlying `AppError.details` carry the sentinel so a route that relays a service detail unprojected fails; (iv) **audit-derived responses** — `GET /reprocessing/devices/:id/history` returns `transitions[].metadata`, the stub poisons one metadata object with `marker: SENTINEL`, and the walk fails it for every role, audience or not, because audit metadata reachable through an API is a routing payload under D11 whatever the reader's audience. (A marker word in `metadata` written by the platform's own handler would also fail the deep suite's audit assertion, §8.) The disclosure predicate gains **three** checks beside the existing ones: any object carrying a non-null `hbsag_status` / `hcv_status` / `hiv_status` (or a non-null `hbsag` / `anti_hcv` / `hiv` / `hbs_titre` / `hcv_pcr`, or `is_seroconversion === true`); any `reuse_restriction` (or `restriction`) with a non-empty `reasons` or `markers`; and — the D11 sentinel — any node carrying an `isolation_class` or `required_class` whose **value** is one of `hbsag` / `hcv` / `hiv` / `isolation_mixed`. Now that D11 is decided **NO**, that sentinel is no longer a pending question parked in a test: **any** marker-class value under either key, in any routing payload reaching a non-entitled role, is an unambiguous **defect** — `required_class` is retired and nothing in the lane computes one, so a hit means a regression put it back. The third mirrors the existing `critical_items` check exactly: a **value** test against a named set (`SEROLOGY_CODES` plus `isolation_mixed`), not a key-presence test, so the key may be absent or `null` and only a marker-class string fails. It carries the same shape guard `serologyItemLeaks` uses: a node with a `machine_no` is a `dialysis_machines` row. Its `isolation_group` is *equipment* data that `GET /machines` publishes to every dialysis role — a bay label, not a marker class — so it is not in the value set and could not trip the sentinel even without the guard; say so rather than deleting the guard, which **stays** to keep a future machine column (or a fixture that revives `isolation_class` on a machine row) from being read as a patient's class. The reachable-set snapshot is regenerated deliberately (`CANARY_WRITE_SNAPSHOT=1`, which rewrites and then fails) and the diff read: every new line is a GROWTH, which is expected for four new mounts and nothing else. **The D11 routing-payload test runs over a FIXED population, regardless of D10 (review 2026-09-06).** The earlier text sized the class-value walk as "reachable − ENTITLED", so a YES to any of D10a–c would have shrunk its population — to zero if all three roles joined the audience. That is the wrong population: under D11 the class must not appear in a **routing payload** for **any** role, audience or not — an entitled nephrologist may read the marker in `reuse_restriction.reasons`, and still must never find `required_class` in `isolation_warnings[]`. So the D11 sentinel is scoped to **routing nodes** (the `isolation` envelope, `isolation_warnings[]` entries, device-shaped nodes, the two frozen screens, audit-derived `transitions[].metadata`) and runs for **every role in `D11_FIXTURE_ROLES`** — a list pinned in the test to the current `DIALYSIS_ROUTE_ROLES` and `CSSD_ROUTE_ROLES` unions, so it is a fixture, not a derivation from the audience set. A D10 widening changes that list by zero. **Population (liveness):** because no response legitimately carries a patient's class, the D11 value walk has a population of zero by design and proves only absence; the suite therefore also asserts that the sweep VISITED at least one `isolation_warnings` entry (`code` + `machine_id` + `severity`) and at least one Decision-shaped node (`status` + `evidence` + `asOf`), and - the only one of the three that proves the POISON is present rather than that the walker ran - that an entitled role's body still carries the poisoned `reuse_restriction.reasons` sentinel and the fixture envelope's `machine_id` (7); any of the three failing reads INERT FIXTURE instead of passing over nothing (the empty-population class). Attribution matters and the plan tabulates it: the two shape counters prove the WALKER ran; the `reasons` pin proves the DECISION fixture is live and says nothing about the isolation envelope; the `machine_id` control is the only in-suite proof the ISOLATION fixture reached a body; and mutation (12) is the only proof the value sentinel bites on that entry - none of the four covers another, so none may be deleted as redundant. Its mutations: drop `POISONED_ISOLATION` from the mock (population red, sentinel green); replace the poison with benign values (positive control red, shape counters and sentinel green) - a shape-only check passes the second and is therefore not enough; and mutation (12), which adds `required_class: 'hbsag'` beside the fixture's resting `required_group: 'Bay 1'` and must go RED **for every role in `D11_FIXTURE_ROLES`** (not for the non-entitled subset), while that resting group value must stay GREEN. **The deep-test contradiction is fixed:** the earlier dialysis deep suite asserted `exposure_markers: ['hcv']` on a late-flagged dialyser while the mutation check demanded that no class → marker map survive — both cannot hold. With no `exposure_markers` column (§4.3) the deep assertion is `{ exposure_flag: true }` plus one active hold row of type `bloodborne_exposure` whose `source_marker_row_id` is the marker just recorded, and `JSON.stringify(deviceRow)` matches no marker word (§8).

## 4. Data model

All new tables: `tenant_id UUID NOT NULL` with the GUC default used by 765, `REFERENCES tenants(id) ON DELETE CASCADE`, `ENABLE` and `FORCE ROW LEVEL SECURITY`, the `tenant_isolation` policy verbatim from 765, a role-guarded GRANT block (SELECT, INSERT, UPDATE; DELETE and TRUNCATE revoked; sequence USAGE and SELECT only), registration in `src/lib/prisma.js`'s `runtime_mutable_no_delete_relations` and `runtime_nextval_sequences`, `prismaCoverage.test.js` pins, and `TABLE_COLUMN_SEED_OVERRIDES` entries. Every CHECK is named. Every FK into an existing table is a tenant-pinned composite; composite `SET NULL` actions carry the column list. Only the two patient-keyed FKs are `DEFERRABLE INITIALLY DEFERRED` (the patient-merge sweep re-points `patient_uid` under `SET CONSTRAINTS ALL DEFERRED`). No relation fields are added to `schema.prisma` for the new FKs (`check:prisma-relations` budget); the mirror carries scalars, uniques and indexes only.

None of the **eleven** new tables (§4.1, §4.2, §4.2a, §4.3–§4.6, §4.12–§4.15) is baseline-owned. Nine are updated in place and sit in `prisma.js`'s `runtime_mutable_no_delete_relations`; two are **append-only** — `device_processing_events` (§4.13) and `dialyser_reprocessing_attempts` (§4.14) — and sit in its existing `runtime_append_only_relations` (SELECT + INSERT; UPDATE, DELETE and TRUNCATE revoked by the migration and re-narrowed on every boot). Of the tables `NNN` alters, `dialysis_sessions`, `ot_schedules`, `surgical_implants` and `clinical_ai_biomed_devices` **are** baseline-owned: every change to them is `ALTER TABLE … ADD COLUMN` / `ADD CONSTRAINT` / `CREATE UNIQUE INDEX`, never an inline re-declaration, so the census manifest and its `expectedAbsentCount` (411) do not change. `dialyzer_reuse_register`, `instrument_sets`, `sterilization_loads` and `set_issue_log` are not baseline-owned and are altered the same way. `dialysis_patients` and `patient_bloodborne_markers` are **not altered** by this lane (§3.3, §4.7, §4.10) — which is why the two pointers from this lane to a marker row (§4.12, §4.15) are its only **single-column** FKs: their backing `(tenant_id, id)` unique would be a change to a Phase 1 table (764 declares only `ux_patient_bloodborne_markers_lab_result`).

### 4.1 `reprocessing_domain_settings` (tenant × domain)

| Column | Type | Notes |
|---|---|---|
| tenant_id | UUID NOT NULL | GUC default; FK `tenants(id)` |
| domain | VARCHAR(16) NOT NULL | CHECK `reprocessing_domain_settings_domain_check`: `dialysis`, `ot` (`cath` reserved — widened by the cath migration, never by a tenant) |
| reactive_patient_rule | VARCHAR(24) NOT NULL | CHECK `reprocessing_domain_settings_reactive_rule_check`: `discard`, `quarantine`, `override_allowed`. **No column default**: the honest default differs per domain (dialysis `discard`, OT `quarantine`) and is supplied by the service's read path when no row exists, so "no row" and "row with defaults" cannot disagree |
| unknown_serology_rule | VARCHAR(24) NOT NULL DEFAULT 'warn' | CHECK `…_unknown_rule_check`: `warn`, `block_return` |
| serology_validity_days | INTEGER NOT NULL DEFAULT 90 | CHECK `…_validity_check`: 1..365. Consumed by the OT arm (`resolveReuseStatus`, the exposure lookback). **Inert for dialysis** while the Phase 1 rule applies no window (§3.3); kept on the row for the shared shape and a future window rule, and shown in the admin form with that note (§6.7) |
| isolation_enforcement | VARCHAR(8) NOT NULL DEFAULT 'warn' | CHECK `…_isolation_enforcement_check`: `warn`, `block`; CHECK `…_isolation_domain_check`: `domain = 'dialysis' OR isolation_enforcement = 'warn'` |
| isolation_groups | JSONB NOT NULL DEFAULT '{}'::jsonb | **D11 = NO** (§2): the tenant's marker-class → routing-group map, e.g. `{ "hbsag": "Bay 1", "hcv": "Bay 2", "hiv": "Bay 2", "isolation_mixed": "Bay 3" }`. Values are the tenant's own `dialysis_machines.isolation_group` labels and carry no clinical meaning. CHECK `…_isolation_groups_shape_check`: `jsonb_typeof(isolation_groups) = 'object'`; CHECK `…_isolation_groups_keys_check`: `isolation_groups - ARRAY['hbsag','hcv','hiv','isolation_mixed'] = '{}'::jsonb` (no other key may be stored); CHECK `…_isolation_groups_domain_check`: `domain = 'dialysis' OR isolation_groups = '{}'::jsonb`. The service validates the **values** (non-empty text, ≤ 40 chars — the `isolation_group` column width) and **warns, never refuses**, when a group named here is on no registered machine: a unit may map its bays before it tags them |
| approved_isolation_groups | TEXT[] NOT NULL DEFAULT '{}' | **Review 2026-09-06 (point 1): the approved, non-clinical bay vocabulary.** A `dialysis_machines.isolation_group` may only be a member of this list (`RPD_ISOLATION_GROUP_NOT_APPROVED`), every value of `isolation_groups` must be a member, and every member is validated against **`ISOLATION_GROUP_DENY_WORDS`** — a case-insensitive, token-split deny-list (`hiv`, `hbv`, `hbs`, `hbsag`, `hcv`, `hep`, `hepatitis`, `aids`, `positive`, `reactive`, `sero`, `infect`, `cjd`, `prion`) — refusing `"HIV bay"` with 400 `RPD_ISOLATION_GROUP_LABEL_CLINICAL` (token-split so `"Ship"` does not trip `hip`). Written only by `ISOLATION_GROUP_ADMIN_ROUTE_ROLES`: infection control approves the words before a machine can carry one. CHECK `…_approved_groups_domain_check`: `domain = 'dialysis' OR approved_isolation_groups = '{}'`. **A deny-list does not prove a label is non-clinical** — "Bay 1" becomes ward knowledge (§2 D11's residual inference risk); it stops the obvious failure the earlier validator accepted |
| reviewed_by, reviewed_at, updated_by, created_at, updated_at | | |

PK `(tenant_id, domain)`. Read path with no row: `{ dialysis: { discard, warn, 90, warn, {} }, ot: { quarantine, warn, 90, warn, {} } }`, `approved_isolation_groups: []`, `configured: false` — an unmapped tenant routes by "any isolation machine" under `warn` (§3.4) and never by an invented label; with no approved vocabulary no machine can be tagged, so `block` is unreachable until infection control has approved the bays (the `block` preconditions, §3.4).

**Who may write the mapping.** `isolation_groups` is written only through `PUT /api/v1/reprocessing/domains/dialysis/settings` (§6.4), whose mount audience is `REPROCESSING_POLICY_ROUTE_ROLES` — an alias of `CATH_REPROCESSING_POLICY_ROUTE_ROLES` (`QUALITY_OFFICER`, `INFECTION_CONTROL_OFFICER`, `ADMIN`, `SUPER_ADMIN`) — narrowed for this one field to `ISOLATION_GROUP_ADMIN_ROUTE_ROLES = REPROCESSING_POLICY_ROUTE_ROLES ∩ {INFECTION_CONTROL_OFFICER, ADMIN, SUPER_ADMIN}`. It is an intersection, so the gate can never be dead, and it is asserted as a strict subset in `reprocessableDeviceRouteWiring.test.js` alongside the other three (§7.4). A body carrying `isolation_groups` from any other role on the mount is refused 403 `RPD_ISOLATION_GROUPS_FORBIDDEN` and the rest of the settings body is unaffected; `QUALITY_OFFICER` therefore reads the mapping and cannot change it. **A technician can never reach it**: `DIALYSIS_TECHNICIAN` is on `DIALYSIS_ROUTE_ROLES` and on no governance set, so the mount itself refuses before any handler runs.

### 4.2 `reprocessing_domain_policies` (tenant × domain × category)

| Column | Type | Notes |
|---|---|---|
| tenant_id, domain | | as above; CHECK `reprocessing_domain_policies_domain_check` |
| category | VARCHAR(40) NOT NULL | CHECK `…_category_check`: `(domain = 'dialysis' AND category = 'dialyser') OR (domain = 'ot' AND category IN ('instrument_set', 'tray', 'implant_set', 'procedure_pack', 'other'))` — the OT values are `instrument_sets_type_check`'s vocabulary (421) so an OT device's category is its set's `set_type`; **`bloodline` is gone** (§1.1) and **`procedure_pack` is tracked but never reprocessable**: CHECK `…_procedure_pack_check`: `NOT (category = 'procedure_pack' AND reprocessable)` (review 2026-09-06, point 6) |
| reprocessable | BOOLEAN NOT NULL DEFAULT false | |
| max_cycles | INTEGER | CHECK `…_max_cycles_check`: 1..100 (418's ceiling); NULL = no ceiling |
| allowed_cycle_types | TEXT[] NOT NULL DEFAULT '{}' | CHECK `…_cycle_types_check`: `<@ ARRAY['steam','eto','plasma','dry_heat','chemical','other']`; CHECK `…_dialysis_cycle_type_check`: `domain <> 'dialysis' OR allowed_cycle_types <@ ARRAY['chemical','other']` (a dialyser is never autoclaved) |
| function_check_required | BOOLEAN NOT NULL DEFAULT false | CHECK `…_ot_function_check_check`: `domain <> 'ot' OR function_check_required = false` — OT's function check is the load release; no per-set check exists in the load flow |
| tcv_min_pct | INTEGER | CHECK `…_tcv_check`: NULL, or `domain = 'dialysis' AND tcv_min_pct BETWEEN 80 AND 100` — **the floor is 80 %** (review 2026-09-06, point 6; Indian guidance's discard threshold), protocol-configurable **upward** only; the service writes 80 for the `dialyser` category when omitted, and the protocol row (§4.2a) may raise it, never lower it |
| protocol_id | INTEGER | composite FK `(tenant_id, protocol_id) → reprocessing_protocols (tenant_id, id)`; CHECK `…_protocol_check`: `reprocessable = false OR protocol_id IS NOT NULL` — **a category becomes reprocessable only under an approved protocol (§4.2a)**; the referenced protocol must be `active` and its `domain` / `category` must match (service-checked, deep-tested) |
| updated_by, created_at, updated_at | | |

PK `(tenant_id, domain, category)`. CHECK `…_complete_check`: `reprocessable = false OR cardinality(allowed_cycle_types) >= 1` (max cycles optional — the one deliberate departure from 765's completeness rule). Dark by default: with no rows nothing is minted, the OT hooks are inert, and the dialysis reuse-register path behaves exactly as today.

**Dark versus deactivated (review 2026-09-06, point 2).** *Dark* is a category with no reprocessable policy **and no register rows** — nothing is minted, the hooks return early, the dialysis reuse-register route runs its legacy path. *Deactivated* is a category whose policy has been set back to `reprocessable = false`, or whose protocol was retired, **after** devices were enrolled: enrolment and minting stop (`RPD_POLICY_DEACTIVATED`), no processing event is recorded and nothing is released to `available` (`releaseToAvailableTx` refuses, §5.6), but **tracking and settlement continue** — capture of an already-enrolled dialyser is refused, an open usage still returns, holds are still placed and released, the cancel path still un-captures, the load-failure response still runs, and `discard` remains available. The legacy dialysis path is **not** re-entered for a session whose usage exists: a returned-but-unreleasable dialyser sits `awaiting_reprocessing` until the policy is re-activated or it is discarded. The service decides dark vs deactivated by `EXISTS (register row for the tenant × domain × category)`, never by the policy row alone.

### 4.2a `reprocessing_protocols` (tenant × domain; the approved processing basis — review 2026-09-06, points 5 and 6)

A category is reprocessable only under a protocol row. This is the **minimal approved model / protocol reference** the review asked for now; the per-model catalogue (manufacturer, model, nominal TCV, IFU document) is a follow-up (§10) that will hang off this table.

| Column | Type | Notes |
|---|---|---|
| id | SERIAL PK; UNIQUE `(tenant_id, id)` | |
| tenant_id, domain | | as §4.1; CHECK `reprocessing_protocols_domain_check` |
| category | VARCHAR(40) | NULL = domain-wide; else the §4.2 vocabulary for the domain (CHECK `…_category_check`) |
| name | VARCHAR(160) NOT NULL | the protocol as the unit names it |
| basis | VARCHAR(24) NOT NULL | CHECK `…_basis_check`: `manufacturer_ifu`, `national_guideline`, `institutional` — what the validation rests on (a chemical name and a contact time are documentation, not effectiveness proof; the basis is) |
| reference | TEXT NOT NULL | the IFU / guideline / document the unit cites |
| approved_by, approved_role, approved_at | UUID NOT NULL; VARCHAR(50) NOT NULL; TIMESTAMPTZ NOT NULL | the nephrology / CSSD / infection-control approver recorded by name and role (stage 3, §1.2); CHECK `…_approved_role_check`: `INFECTION_CONTROL_OFFICER`, `CONSULTANT`, `ADMIN`, `SUPER_ADMIN` |
| status, retired_at, retired_by | VARCHAR(16) NOT NULL DEFAULT 'active' | CHECK: `active`, `retired` |
| tcv_min_pct | INTEGER | dialysis only; CHECK `…_tcv_check`: NULL or `BETWEEN 80 AND 100` — the **floor is 80 %** and a protocol may only raise it |
| baseline_tcv_required | BOOLEAN NOT NULL DEFAULT true | a dialyser is not released without a recorded baseline |
| mid_life_enrolment_rule | VARCHAR(40) NOT NULL DEFAULT 'refuse' | CHECK: `refuse` (a dialyser with an unknown baseline is never released — the conservative default), `first_measured_with_ic_signoff` (the first measured TCV becomes the baseline, recorded as such with the approver on the link row, §4.5), `manufacturer_nominal` (the protocol's `nominal_tcv_ml` for the model is the baseline). The **explicitly approved alternative** the review requires for mid-life enrolment |
| nominal_tcv_ml | NUMERIC(6,1) | required when `mid_life_enrolment_rule = 'manufacturer_nominal'` (CHECK) |
| residual_test_required | BOOLEAN NOT NULL DEFAULT true | a negative residual-disinfectant test is required **at capture** of a reprocessed dialyser (§5.1) |
| integrity_test_required | BOOLEAN NOT NULL DEFAULT true | `integrity_test_result = 'pass'` required for release |
| agents | JSONB NOT NULL DEFAULT '[]' | `[{ agent, min_concentration_pct, max_concentration_pct, min_contact_minutes }]`, `agent` ∈ the 418 vocabulary; CHECK `jsonb_typeof(agents) = 'array'`, element validation in the service — a release records an agent **in this list** with parameters inside its bounds |
| reuse_matrix | JSONB NOT NULL | the **pathogen-specific reuse matrix** keyed by the four classes. CHECK `…_matrix_keys_check`: keys ⊆ `{hbsag, hcv, hiv, isolation_mixed}`; CHECK `…_matrix_conservative_check`: `reuse_matrix->>'hbsag' = 'no_reuse' AND reuse_matrix->>'hiv' = 'no_reuse' AND reuse_matrix->>'isolation_mixed' = 'no_reuse'` — **HBsAg-reactive: no reuse; HIV-reactive: no reuse; mixed: no reuse — enforced by the database, not configurable by a tenant** (CDC advises against dialyser reuse in HBsAg-positive patients, reprocessing being an occupational exposure; Indian guidance avoids reuse for HBV- and HIV-positive patients); `hcv` ∈ `{ no_reuse, dedicated_reuse }` with the service default **`no_reuse`** — "HCV per protocol" means a unit may choose `dedicated_reuse` (same-patient dedication, the D2 link row) and must say so in an approved protocol. Relaxing the three locked cells is an owner decision (§9), not configuration |
| surveillance_intervals_days | JSONB NOT NULL DEFAULT '{"hbsag":90,"hcv":90,"hiv":365}' | per-marker currency the resolver's `surveillance_overdue` is computed against (§3.3) |
| surveillance_overdue_blocks_reuse | BOOLEAN NOT NULL DEFAULT true | an overdue patient is `unknown` for reuse eligibility (§5.3) |
| prion_rule | VARCHAR(16) NOT NULL DEFAULT 'discard' | CHECK: `discard`, `ic_pathway` — a `prion_exposure` hold (§4.12) is never released by routine processing; under `discard` the only disposition is discard, under `ic_pathway` an infection-control release naming this protocol may authorise the unit's own CJD pathway |
| notes, created_by, created_at, updated_by, updated_at | | |

Uniques: `(tenant_id, id)`; partial `(tenant_id, domain, COALESCE(category, '*')) WHERE status = 'active'` (one active protocol per category; a NULL category is the domain-wide row). OT: sterility evidence is the load's indicators on the processing event; mechanical inspection / function is the return's `return_condition` and the `inspection_failed` hold — never conflated (§5.2). Written through `GET` / `PUT /api/v1/reprocessing/domains/:domain/protocols` (§6.4) under `ISOLATION_GROUP_ADMIN_ROUTE_ROLES` — the same infection-control / admin narrowing as the isolation map; `QUALITY_OFFICER` reads. Retiring the active protocol **deactivates** its categories (§4.2) rather than deleting anything. The admin form renders the matrix with the three locked cells read-only and the `hcv` cell as a choice (§6.7).

### 4.3 `reprocessable_devices` (the register)

One row per physical device. **No patient identity.**

| Column | Type | Notes |
|---|---|---|
| id | BIGSERIAL PK | |
| tenant_id | UUID NOT NULL | GUC default; FK `tenants(id)` |
| domain | VARCHAR(16) NOT NULL | CHECK `reprocessable_devices_domain_check`: `dialysis`, `ot` |
| category | VARCHAR(40) NOT NULL | CHECK `…_category_check`: same pairs as §4.2 |
| facility_id | INTEGER | composite FK `(tenant_id, facility_id) → facilities (tenant_id, id)`; **nullable** because neither `dialysis_sessions` nor `instrument_sets` carries a facility today (§1.1) |
| device_tag | VARCHAR(24) GENERATED ALWAYS AS (`'RD' \|\| lpad(id::text, GREATEST(8, length(id::text)), '0')`) STORED | prefix `RD`, distinct from cath's `RP`, so a tag can never resolve against the wrong register; `DEVICE_TAG_PATTERN = /^RD[0-9]{8,19}$/` |
| manufacturer_serial | VARCHAR(120) | D9; unique partial `(tenant_id, domain, manufacturer_serial) WHERE NOT NULL`; **required for dialysis** |
| hospital_asset_id | VARCHAR(120) | D9; unique partial `(tenant_id, domain, hospital_asset_id) WHERE NOT NULL`; for OT the set's `barcode` is copied here at enrolment |
| manufacturer, model_name | VARCHAR(120) | free text (no catalogue, §1.1) |
| instrument_set_id | BIGINT | composite FK `(tenant_id, instrument_set_id) → instrument_sets (tenant_id, id) ON DELETE RESTRICT`; unique partial `(tenant_id, instrument_set_id) WHERE NOT NULL` |
| enrolled_via | VARCHAR(24) NOT NULL | CHECK `…_enrolled_via_check`: `session_capture`, `set_issue`, `console` |
| cycle_count | INTEGER NOT NULL DEFAULT 0 | CHECK `…_cycle_check`: ≥ 0 |
| max_cycles_snapshot | INTEGER | CHECK `…_max_cycles_check`: NULL or ≥ 1; CHECK `…_cycle_bound_check`: `max_cycles_snapshot IS NULL OR cycle_count <= max_cycles_snapshot` |
| status | VARCHAR(32) NOT NULL DEFAULT 'available' | CHECK `…_status_check`: `awaiting_reprocessing`, `in_cssd`, `available`, `in_case`, `quarantined`, `discarded` (765's six). Default `available`, not 765's `awaiting_reprocessing`: a dialyser is minted at first capture (new, sterile from the manufacturer) and a set at issue (already sterile from a passed load) |
| current_usage_id | BIGINT | composite FK **`(tenant_id, current_usage_id, id) → reprocessable_device_usages (tenant_id, id, device_id)`** — the usage must belong to **this** device (review 2026-09-06, point 8: the earlier two-column FK validated the tuple, not the relationship), added after §4.4 exists, `ON DELETE SET NULL (current_usage_id)`; CHECK `…_in_case_check`: `(status = 'in_case') = (current_usage_id IS NOT NULL)`. "Open" cannot be a CHECK (cross-row): `captureDeviceTx` sets it in the same transaction that inserts the usage, every close path nulls it in the same transaction, and the census's `--integrity` mode counts a pointer at a closed usage (must be zero) |
| exposure_flag | BOOLEAN NOT NULL DEFAULT false | **historical exposure: set once, never cleared** (§5.5). **There is no `exposure_markers` column** (D11(e), review 2026-09-06): the which-marker evidence lives only on a hold's pointer to the marker row (§4.12); no lookback, queue row, audit row or export can name a marker from this table |
| last_reprocessed_at, last_reprocessed_by | | |
| last_cycle_type | VARCHAR(20) | CHECK `…_cycle_type_check`: the six |
| last_function_check | VARCHAR(16) | CHECK `…_function_check_check`: `not_required`, `pass`, `fail` |
| last_sterilization_load_id | BIGINT | composite FK `(tenant_id, last_sterilization_load_id) → sterilization_loads (tenant_id, id) ON DELETE SET NULL (last_sterilization_load_id)`; CHECK `…_load_domain_check`: `domain = 'ot' OR last_sterilization_load_id IS NULL` (D7) |
| last_processing_event_id | BIGINT | composite FK `(tenant_id, last_processing_event_id) → device_processing_events (tenant_id, id)` (added after §4.13 exists) — the event that made this device ready **now**; copied onto the next usage's `ready_processing_event_id` at capture (§4.4, §4.13) |
| residual_test_pending | BOOLEAN NOT NULL DEFAULT false | dialysis: set by a release under a protocol with `residual_test_required`; cleared by the capture that records a negative pre-use residual test (§5.1) |
| version | INTEGER NOT NULL DEFAULT 0 | **the lifecycle version** (review 2026-09-06, additional conditions): incremented in the same `UPDATE` as every transition; commands carry `expected_version` and a stale one is refused (`RPD_VERSION_CONFLICT`); the receipt every command returns is `{ version, audit_id, action }` (§5.8) |
| quarantine_reason, quarantined_at | VARCHAR(40); TIMESTAMPTZ | **a code, not free text** (review 2026-09-06, point 1): CHECK `…_quarantine_reason_check` over `QUARANTINE_REASON_CODES` = `exposure_hold`, `prion_hold`, `sterilization_failed`, `load_invalidated`, `serology_required`, `post_issue_restriction`, `cycle_type_not_allowed`, `inspection_failed`, `release_pending_processing`, `manual`; the human note lives on the hold row (§4.12). Both cleared by `release` (the transition, after every active hold is released) and by `reprocessed` |
| discard_reason | VARCHAR(40) | CHECK `…_discard_reason_check`: 765's nine plus `tcv_below_threshold`, `integrity_test_failed`, `set_retired`; CHECK `…_discarded_check`: `status <> 'discarded' OR (discard_reason IS NOT NULL AND discarded_at IS NOT NULL)` |
| discard_note, discarded_at, discarded_by, created_by (NOT NULL), created_at, updated_at, metadata | | |

CHECK `…_identity_check`: `((domain = 'ot') = (instrument_set_id IS NOT NULL)) AND (domain <> 'dialysis' OR manufacturer_serial IS NOT NULL)`. Uniques: `(tenant_id, device_tag)`; `(tenant_id, id)`; `(tenant_id, id, domain)` — the target that lets child rows pin the domain by FK rather than by trigger; `(tenant_id, id, instrument_set_id)` — the target that pins an OT usage's set to its device (§4.4). Indexes: `(tenant_id, domain, status)`, `(tenant_id, facility_id, status) WHERE facility_id IS NOT NULL`.

Transitions are 765's table (`DEVICE_ACTIONS`: receive, reprocessed, quarantine, release, discard, capture, return, with the same `from` lists) plus one action 765 did not need — **`uncapture`: `in_case → available`**, no cycle change, no reprocessing field touched, no reprocessing record: the case was cancelled before use (§5.1 Cancel, §5.2). Because two actions now land on `available`, the transition `UPDATE` keys cycle and reprocessing columns on the **action** (`cycle_count + 1` only on `reprocessed`), never on the target state, so `uncapture` cannot count a cycle. The dialysis reprocessing command applies `return` then `reprocessed` in one transaction (§5.1); every other path is one action per command. **Review 2026-09-06:** `quarantine`'s from-list is 765's — `awaiting_reprocessing`, `in_cssd`, `available` — and **never `in_case`**: a device in a case is returned first (`return`, which closes the usage) and quarantined second, in one transaction (§5.1, §5.2); the late-reactive handler places a hold on an `in_case` device and leaves the transition to the return. `release` (`quarantined → awaiting_reprocessing`) is refused while any hold on the device is `active` (`RPD_HOLD_ACTIVE`) — the hold is released by an authorised decision first (§5.5), the transition follows in the same transaction. Every transition **to `available`** — `reprocessed` and `uncapture` — goes through `releaseToAvailableTx` (§5.6). Every transition bumps `version` and writes one audit row whose id is the command's receipt (§5.8).

### 4.4 `reprocessable_device_usages` (patient linkage; owner pair per D1)

One row per device per case.

| Column | Type | Notes |
|---|---|---|
| id | BIGSERIAL PK | |
| tenant_id | UUID NOT NULL | GUC default |
| domain | VARCHAR(16) NOT NULL | CHECK `reprocessable_device_usages_domain_check`: `dialysis`, `ot` |
| device_id | BIGINT NOT NULL | composite FK `(tenant_id, device_id, domain) → reprocessable_devices (tenant_id, id, domain) ON DELETE RESTRICT` — three columns, so a usage row's domain can never disagree with its device's |
| patient_uid | UUID NOT NULL | composite FK `(tenant_id, patient_uid) → users (tenant_id, uid) ON DELETE NO ACTION DEFERRABLE INITIALLY DEFERRED` |
| dialysis_session_id | INTEGER | composite FK `(tenant_id, dialysis_session_id) → dialysis_sessions (tenant_id, id) ON DELETE RESTRICT` |
| ot_schedule_id | INTEGER | composite FK `(tenant_id, ot_schedule_id) → ot_schedules (tenant_id, id) ON DELETE RESTRICT`; **and** composite FK `(tenant_id, ot_schedule_id, patient_uid) → ot_schedules (tenant_id, id, patient_uid)` — the usage's patient must be the case's patient (review 2026-09-06, point 8). A dialysis session carries `dialysis_patient_id`, not `patient_uid`, so the dialysis pair is checked in the capture transaction under the session lock and counted by the census's `--integrity` mode, never trusted to the client |
| instrument_set_id | BIGINT | OT only (denormalised on purpose): composite FK `(tenant_id, device_id, instrument_set_id) → reprocessable_devices (tenant_id, id, instrument_set_id)` **and** composite FK `(tenant_id, set_issue_log_id, instrument_set_id) → set_issue_log (tenant_id, id, instrument_set_id)` — the issue, the set and the device must agree |
| set_issue_log_id | BIGINT | composite FK `(tenant_id, set_issue_log_id) → set_issue_log (tenant_id, id) ON DELETE SET NULL (set_issue_log_id)`; **and** composite FK `(tenant_id, set_issue_log_id, ot_schedule_id) → set_issue_log (tenant_id, id, ot_schedule_id)` — the issue must be this case's |
| ready_processing_event_id | BIGINT | composite FK `(tenant_id, ready_processing_event_id) → device_processing_events (tenant_id, id)`; **write-once at capture** from `device.last_processing_event_id` — the load / reprocessing that made the device ready **before** this use (review 2026-09-06, point 4: both sides of the evidence chain). NULL only for the first use of a new dialyser or a set enrolled mid-life (`metadata.enrolled_from_existing_set`), and the row says which |
| post_use_processing_event_id | BIGINT | composite FK to the same target; **write-once** by the load-outcome handler or the dialysis release — the event that reprocessed the device **after** this use. `stampUsagePostUseEventTx` writes it only `WHERE post_use_processing_event_id IS NULL`; a later processing never overwrites it. (The existing `set_issue_log.sterilization_load_id` path *does* overwrite on later processing — that column stays cssd's board convenience and is no longer evidence) |
| reuse_cycle | INTEGER NOT NULL | CHECK `…_reuse_cycle_check`: ≥ 0 — the device's `cycle_count` at capture; 0 is a first use (unlike 765, first use is a captured event here) |
| captured_at, captured_by | TIMESTAMPTZ NOT NULL DEFAULT now(); UUID NOT NULL | `captured_by` composite FK to `users (tenant_id, uid)` |
| capture_source | VARCHAR(24) NOT NULL | CHECK `…_capture_source_check`: `staff_app`, `admin_console`, `cssd_issue`, `system` |
| capture_provenance | VARCHAR(16) NOT NULL DEFAULT 'live' | CHECK `…_provenance_check`: `live`, `retrospective` — `retrospective` is the implicit capture at reprocessing time (§5.1): the association was recorded after the use, `captured_at` is the recording time (never back-dated), and `reuse_screen` is the `Decision` at recording time, flagged so it is not read as what a capturing user saw (review 2026-09-06, point 8) |
| pre_use_residual_test | VARCHAR(16) | dialysis: CHECK `negative`, `not_required`; recorded at capture of a reprocessed dialyser, required when the protocol says so (§5.1) |
| unopened_confirmation | VARCHAR(24) | written by an un-capture: CHECK `sealed_unopened` (an OT pack with its seal intact; a dialyser still in its manufacturer packaging, never primed), `not_connected` (dialysis: never connected to the circuit). **Required** — an un-capture with no affirmative condition is refused (`RPD_UNOPENED_CONFIRMATION_REQUIRED`; §5.1 Cancel, §5.2) |
| reuse_screen, post_use_screen | JSONB | frozen resolver output — **stored complete, sanitised on response** (§3.5) |
| post_use_disposition | VARCHAR(40) | CHECK `…_post_use_check`: `sent_for_reprocessing`, `released_not_established` (the dialysis record settled the usage but the release criteria were not met — the device is `awaiting_reprocessing`, §5.1), `quarantined_bloodborne_exposure`, **`quarantined_serology_required`**, **`quarantined_post_issue_restriction`**, **`quarantined_inspection_failed`**, **`quarantined_other`** (review 2026-09-06, point 7: a `serology_required` hold is never recorded as a blood-borne exposure), `discarded_bloodborne_exposure`, `discarded_max_cycles`, `discarded_integrity_failed`, `discarded_tcv_below_threshold`, `discarded_other`, **`cancelled_before_use`** (the case was cancelled before use and the device was released, §5.1 Cancel / §5.2 — no post-use screen, no cycle consumed) |
| returned_at, returned_by | | CHECK `…_returned_check`: `(post_use_disposition IS NULL) = (returned_at IS NULL)`. The table has one close timestamp: an un-capture writes `returned_at = now()` too (the review's "ended_at" is this column) |
| acknowledgement_reason | TEXT | the override text, also written to `medication_safety_reviews` (§5.4) |
| metadata, created_at, updated_at | | |

CHECK `…_owner_check`: `num_nonnulls(dialysis_session_id, ot_schedule_id) = 1` (D1). CHECK `…_owner_domain_check`: `((domain = 'dialysis') = (dialysis_session_id IS NOT NULL)) AND ((domain = 'ot') = (ot_schedule_id IS NOT NULL))`. CHECK `…_ot_links_check`: `domain = 'ot' OR (set_issue_log_id IS NULL AND instrument_set_id IS NULL)`; CHECK `…_ot_set_check`: `domain <> 'ot' OR instrument_set_id IS NOT NULL`.

Uniques: `(tenant_id, id)`; partial `(tenant_id, device_id, reuse_cycle) WHERE post_use_disposition IS DISTINCT FROM 'cancelled_before_use'` — each cycle is used at most once, the constraint 765 as built could not express; a cancelled usage does not consume its cycle, so the next capture of the same device at the unchanged `cycle_count` is legal (the OT re-issue after a cancel, §5.2); partial `(tenant_id, device_id) WHERE returned_at IS NULL` — at most one open usage per device; partial `(tenant_id, dialysis_session_id) WHERE dialysis_session_id IS NOT NULL` — one dialyser per session; partial `(tenant_id, set_issue_log_id) WHERE set_issue_log_id IS NOT NULL`; `(tenant_id, id, device_id)` and `(tenant_id, id, dialysis_session_id)` — the targets for the register's `current_usage_id` FK, the holds' and attempts' usage FKs and 418's pair (§4.3, §4.8, §4.12, §4.14). Indexes: `(tenant_id, patient_uid, captured_at DESC)`, `(tenant_id, ot_schedule_id) WHERE NOT NULL`, `(tenant_id, ready_processing_event_id) WHERE NOT NULL` (the failure response's affected-usage read, §5.2).

### 4.5 `reprocessable_device_dialysis_links` (per-patient dedication; D2)

One row per dialysis-domain device, created at first capture.

| Column | Type | Notes |
|---|---|---|
| device_id | BIGINT PK | |
| tenant_id | UUID NOT NULL | GUC default |
| domain | VARCHAR(16) NOT NULL DEFAULT 'dialysis' | CHECK `…_domain_check`: `domain = 'dialysis'`; composite FK `(tenant_id, device_id, domain) → reprocessable_devices (tenant_id, id, domain) ON DELETE CASCADE` — an OT device can never acquire a link row |
| dedicated_patient_uid | UUID NOT NULL | composite FK `(tenant_id, dedicated_patient_uid) → users (tenant_id, uid) ON DELETE NO ACTION DEFERRABLE INITIALLY DEFERRED` |
| dedicated_at, dedicated_by | TIMESTAMPTZ NOT NULL DEFAULT now(); UUID NOT NULL | |
| baseline_tcv_ml | NUMERIC(6,1) | CHECK `…_baseline_tcv_check`: NULL or > 0; measured on the new dialyser before first use |
| baseline_tcv_measured_at | TIMESTAMPTZ | |
| baseline_tcv_source | VARCHAR(40) | CHECK `…_baseline_source_check`: `measured_new` (on the new dialyser before first use), `manufacturer_nominal`, `first_measured_mid_life` — the last two are the protocol's approved mid-life alternatives (§4.2a); NULL while no baseline exists |
| baseline_approved_by, baseline_approved_at | UUID; TIMESTAMPTZ | CHECK `…_baseline_approval_check`: required when `baseline_tcv_source = 'first_measured_mid_life'` — the sign-off the alternative demands |
| created_at, updated_at | | |

Index `(tenant_id, dedicated_patient_uid)`. The CSSD queue never joins this table.

**Where TCV lives.** No TCV, priming-volume or fibre-bundle-volume field exists anywhere in the repository today. Baseline TCV is a device-level fact and lives here; each reprocessing's measured TCV is a per-event fact and lives on the statutory row (§4.8), with `tcv_pct_of_baseline` computed by the service at write time (a generated column cannot read another table). Priming volume is not stored: no clinical rule in this lane hangs on it (§10). **A missing baseline no longer passes the TCV verdict** — it is `not_established` (§5.1; review 2026-09-06, point 6).

### 4.6 `dialysis_machines` (minimal; D3)

| Column | Type | Notes |
|---|---|---|
| id | SERIAL PK | |
| tenant_id | UUID NOT NULL | GUC default; FK `tenants(id)` |
| facility_id | INTEGER | composite FK to `facilities (tenant_id, id)` |
| machine_no | VARCHAR(40) NOT NULL | the value `dialysis_sessions.machine_no` carries; UNIQUE `(tenant_id, machine_no)` |
| display_name | VARCHAR(120) | |
| biomed_device_id | INTEGER | composite FK `(tenant_id, biomed_device_id) → clinical_ai_biomed_devices (tenant_id, id) ON DELETE SET NULL (biomed_device_id)` — asset identity stays in 053 |
| isolation_group | VARCHAR(40) | **D11 = NO** (§2): the tenant's own routing label — "Bay 1", "Red side". **Nullable; `NULL` is a general machine.** No enum CHECK, because the vocabulary is the tenant's; only CHECK `dialysis_machines_isolation_group_check`: `isolation_group IS NULL OR btrim(isolation_group) <> ''`. It carries no clinical meaning and is equipment data — `GET /machines` publishes it to every dialysis role. **The service additionally requires the label to be in `reprocessing_domain_settings.approved_isolation_groups`** (§4.1): infection control approves the vocabulary before a machine can carry a word from it |
| status | VARCHAR(20) NOT NULL DEFAULT 'active' | CHECK `…_status_check`: `active`, `out_of_service`, `retired` |
| notes, created_by, updated_by, created_at, updated_at | | |

Index `(tenant_id, isolation_group, status)`. No cycle count, no queue, no discard: a machine is not a device. There is no `isolation_mixed` machine: a bay that takes two marker classes is one label that both classes map to (§4.1).

### 4.7 Changes to `dialysis_sessions` (baseline-owned; ALTER only)

`dialysis_sessions` gains `isolation_warning_codes TEXT[] NOT NULL DEFAULT '{}'`, `isolation_required_group VARCHAR(40)` (**D11 = NO**: the routing group the evaluation decided, persisted beside the codes so the session read can re-serialise the warning without asking the resolver for a marker class — a bay label, publishable; there is deliberately no `isolation_required_class` column), `isolation_warn_only BOOLEAN NOT NULL DEFAULT true`, `isolation_enforcement_enabled BOOLEAN NOT NULL DEFAULT false`, `isolation_override_reason TEXT`, `isolation_override_by UUID`, `isolation_override_at TIMESTAMPTZ(6)`, `isolation_override_kind VARCHAR(16)` (CHECK `dialysis_sessions_isolation_override_kind_check`: NULL, `reason`, `emergency` — the emergency exception of §3.4), `isolation_evaluated_at TIMESTAMPTZ(6)`; CHECK `dialysis_sessions_isolation_override_check`: `num_nonnulls(isolation_override_reason, isolation_override_by, isolation_override_at) IN (0, 3)`. The mode pair mirrors 423's names so a reader of one recognises the other. `machine_no`, `dialyser` and `reuse_count` are unchanged; the last two become derived at capture (§5.1).

`dialysis_patients`: **untouched by `NNN`**. The default flip to `'unknown'` and every other change to the three serology columns belong to Phase 1 (§3.3); this lane reads the table and never alters or writes it. `isolation_required` keeps its generated expression until the Phase 2 DROP, which is outside this lane.

Backing uniques for the composite FKs above: `ux_dialysis_sessions_tenant_id (tenant_id, id)`, `ux_ot_schedules_tenant_id`, `ux_instrument_sets_tenant_id`, `ux_sterilization_loads_tenant_id`, `ux_set_issue_log_tenant_id`, `ux_clinical_ai_biomed_devices_tenant_id`. Each is a non-concurrent unique index build inside the migration transaction — the same deploy note 765/766 carry for `lab_results`; `ot_schedules` is the largest of these.

### 4.8 Changes to `dialyzer_reuse_register` (mig 418; D6)

Added columns: `device_id BIGINT`, `device_usage_id BIGINT`, `measured_tcv_ml NUMERIC(6,1)`, `baseline_tcv_ml NUMERIC(6,1)` (the baseline as it stood, so the percentage is auditable without the link row), `tcv_pct_of_baseline NUMERIC(5,1)`, `reprocessing_agent VARCHAR(24)`, `disinfectant_contact_minutes INTEGER`, `disinfectant_concentration_pct NUMERIC(5,2)`, `release_status VARCHAR(24) NOT NULL DEFAULT 'legacy'` (CHECK `chk_dialyzer_reuse_release_status`: `legacy` — a pre-`NNN` or dark-path row — `released`, `not_established`, `quarantined`, `discarded`), `missing_evidence TEXT[] NOT NULL DEFAULT '{}'` (the release criteria items a `not_established` row lacks, §5.1), `capture_provenance VARCHAR(16)` (CHECK: `live`, `retrospective`), `protocol_id INTEGER`, `processing_event_id BIGINT`. Constraints (all `ALTER TABLE … ADD CONSTRAINT`, named): `fk_dialyzer_reuse_register_device` `(tenant_id, device_id) → reprocessable_devices (tenant_id, id) ON DELETE SET NULL (device_id)`; **`fk_dialyzer_reuse_register_device_usage` `(tenant_id, device_usage_id, device_id) → reprocessable_device_usages (tenant_id, id, device_id)`** and **`fk_dialyzer_reuse_register_usage_session` `(tenant_id, device_usage_id, session_id) → reprocessable_device_usages (tenant_id, id, dialysis_session_id)`** — the statutory row's device, usage and session must be one triple (review 2026-09-06, point 8; the earlier two independent FKs validated two tuples and no relationship); `fk_dialyzer_reuse_register_protocol` `(tenant_id, protocol_id) → reprocessing_protocols (tenant_id, id)`; `fk_dialyzer_reuse_register_processing_event` `(tenant_id, processing_event_id) → device_processing_events (tenant_id, id)`; `chk_dialyzer_reuse_device_pair`: `(device_id IS NULL) = (device_usage_id IS NULL)`; `chk_dialyzer_reuse_agent`: NULL or `peracetic_acid`, `formaldehyde`, `glutaraldehyde`, `renalin`, `other`; `chk_dialyzer_reuse_contact_minutes`: NULL or 0..1440; `chk_dialyzer_reuse_measured_tcv`: NULL or > 0; `chk_dialyzer_reuse_tcv_pct`: NULL or 0..200; `chk_dialyzer_reuse_release_event`: `release_status <> 'released' OR processing_event_id IS NOT NULL`. New unique `ux_dialyzer_reuse_register_tenant_id (tenant_id, id)` — the target for the attempts and events FKs. Existing columns, CHECKs and the per-session unique are untouched; `register_format_status` stays `format_pending` on every row (the statutory form is still owed, §9). `disinfectant VARCHAR(80)` stays as free-text detail beside the new enum. **A settled row accepts two later writes and no others:** `notes`, and `release_status → 'released'` (with `processing_event_id`) written by the attempt that releases (§4.14); every later legitimate reprocessing is an **append-only attempt** linked to this row, never a rewrite (review 2026-09-06, point 2).

### 4.9 Changes to `surgical_implants` (baseline-owned; D5)

`sterilization_load_id BIGINT` with `fk_surgical_implants_sterilization_load` `(tenant_id, sterilization_load_id) → sterilization_loads (tenant_id, id) ON DELETE SET NULL (sterilization_load_id)`, via `ALTER TABLE … ADD CONSTRAINT` (the table is baseline-owned; an inline re-declaration would trip the census gate). `sterilization_lot VARCHAR(120)` stays. The `POST /api/v1/surgical/implants` handler accepts the new field when the load exists in the tenant; nothing else about implants changes.

### 4.10 `patient_bloodborne_markers` (mig 764) — no change

The `dialysis_surveillance` source, the widening of `patient_bloodborne_markers_lab_link_check` and the matching change to `recordMarkerTx`'s guards moved to Phase 1 with `recordSerology` (§3.3). `NNN` does not touch the table. (If Phase 1 lands first, 767 carries them and this lane's `bloodborneMarkers.mjs` overlay picks them up on regeneration; if not, the overlay is unchanged.)

### 4.11 Migration `NNN` outline and the 753/758 hazard

`NNN_reprocessable_devices_platform.sql` (`NNN` = the next free migration number at push time; 767 is the Phase 1 lane's, §3.3), one transaction, `lock_timeout 5s` / `statement_timeout 300s`, in this order: (1) the **ten** backing uniques — the six of the earlier draft plus `ux_ot_schedules_tenant_id_patient (tenant_id, id, patient_uid)`, `ux_set_issue_log_tenant_id_set (tenant_id, id, instrument_set_id)`, `ux_set_issue_log_tenant_id_schedule (tenant_id, id, ot_schedule_id)` and `ux_dialyzer_reuse_register_tenant_id (tenant_id, id)`; (2) `reprocessing_domain_settings`; (3) `reprocessing_protocols`; (4) `reprocessing_domain_policies` (with its protocol FK); (5) `reprocessable_devices` without its usage and event FKs; (6) `reprocessable_device_usages` with its device / owner FKs and without its event FKs; (7) `device_processing_events`; (8) the deferred FKs — `reprocessable_devices.current_usage_id` (three-column), `reprocessable_devices.last_processing_event_id`, the usages' two event FKs; (9) `reprocessable_device_dialysis_links`; (10) `dialysis_machines`; (11) `reprocessable_device_holds`; (12) `dialyser_reprocessing_attempts`; (13) `bloodborne_exposure_outbox`; (14) `dialysis_sessions` columns; (15) `dialyzer_reuse_register` columns and constraints; (16) `surgical_implants` column and FK; (17) RLS on the eleven new tables; (18) the GRANT block — nine tables SELECT + INSERT + UPDATE, the two append-only tables SELECT + INSERT only. Its header is self-contained: it states D1–D9 and D11 inline, the two policy tables, the protocol precondition, the `quarantine` rule, optional `max_cycles` and its semantics, the hold model, the marker-free register (no `exposure_markers`), that `dialysis_patients` and `patient_bloodborne_markers` are deliberately **not** touched (Phase 1, migration 767 — and why the two marker-row pointers are single-column FKs), and that `cath_reprocessable_devices` is not touched. **It is additive throughout**: no column is dropped or narrowed, and every new NOT NULL column carries a default.

**753/758 hazard assessment.** `cath_inventory_authority_assert_contract_753(uuid, bigint)` is bound to `cath_case_consumable_usage` and names `public.cath_reprocessable_devices` directly in its `reused_device` branch (765:551); `cath_authority_identity_guard_753()` dispatches on `TG_TABLE_NAME` over `cath_lab_cases`, `cath_consumable_catalog`, `cath_case_consumable_usage`, `tasks`, `workflow_sla_instances`, `notification_outbox` and `pharmacy_stock_movements`. None of the tables `NNN` creates or alters is on either list, and `NNN` does not insert into any of them. **Confirmed: a new generic table trips neither assert, and no plpgsql body is re-declared by this migration.** The scouting brief's conclusion stands.

### 4.12 `reprocessable_device_holds` (the operational restriction — review 2026-09-06, points 1, 3 and 7)

One row per hold placed on a device. Historical exposure stays on the device as `exposure_flag`; this table is what issue, capture, release and reprocessing check (§5.5).

| Column | Type | Notes |
|---|---|---|
| id | BIGSERIAL PK; UNIQUE `(tenant_id, id)` | |
| tenant_id, domain | | GUC default; CHECK on domain |
| device_id | BIGINT NOT NULL | composite FK `(tenant_id, device_id, domain) → reprocessable_devices (tenant_id, id, domain) ON DELETE RESTRICT` |
| hold_type | VARCHAR(32) NOT NULL | CHECK `…_type_check`: `bloodborne_exposure`, `prion_exposure`, `sterilization_failed`, `load_invalidated`, `serology_required`, `post_issue_restriction`, `cycle_type_not_allowed`, `inspection_failed`, `manual` |
| reason_code | VARCHAR(40) NOT NULL | CHECK `…_reason_check` over `HOLD_REASON_CODES`: `exposure_late_result`, `exposure_undated_declaration`, `exposure_at_return`, `load_failed`, `load_invalidated_after_release`, `serology_unknown_block_return`, `restricted_after_issue`, `cycle_type_forbidden`, `return_condition_damaged`, `manual_ic`, `manual_cssd` — codes, never text |
| status | VARCHAR(16) NOT NULL DEFAULT 'active' | CHECK: `active`, `released`, `satisfied`, `superseded` |
| pending_return | BOOLEAN NOT NULL DEFAULT false | placed while the device was `in_case`; the return path reads it and quarantines |
| source_marker_row_id | BIGINT | **the lane's single-column FK** `REFERENCES patient_bloodborne_markers(id) ON DELETE SET NULL` (764 has no `(tenant_id, id)` unique and this lane does not alter that table; the hold's tenant is pinned by its device FK and the writer checks the marker row's tenant inside the transaction). The **only** pointer from the register to a marker; readable by audience roles only (§3.5, §6.2) |
| last_seen_marker_row_id | BIGINT | the same FK; a second event of the same type on the same device updates it instead of adding a hold |
| source_load_id | BIGINT | composite FK to `sterilization_loads (tenant_id, id)` — for load holds |
| source_usage_id | BIGINT | composite FK `(tenant_id, source_usage_id, device_id) → reprocessable_device_usages (tenant_id, id, device_id)` — the use the hold arose from |
| note | TEXT | **free text lives here**, never on the device; shown to the release dialog only |
| placed_at, placed_by, placed_via | TIMESTAMPTZ NOT NULL; UUID (NULL = system); VARCHAR(24) | CHECK on `placed_via`: `exposure_handler`, `reconciler`, `load_outcome`, `return`, `issue`, `manual` |
| released_at, released_by, released_role | | the authorised decision — CHECK `…_released_check`: `status NOT IN ('released', 'satisfied') OR (released_at IS NOT NULL AND released_by IS NOT NULL AND release_adjudication IS NOT NULL)` |
| release_adjudication | TEXT | what infection control (or CSSD, per type) decided and why |
| release_protocol_id | INTEGER | composite FK to `reprocessing_protocols (tenant_id, id)` — the processing protocol the release requires; CHECK `…_prion_check`: `hold_type <> 'prion_exposure' OR status = 'active' OR release_protocol_id IS NOT NULL` |
| release_evidence | JSONB NOT NULL DEFAULT '{}' | references: IC case, lab, document |
| release_requires_processing | BOOLEAN NOT NULL DEFAULT true | the device may not become `available` until a processing event satisfies the release; `false` records an adjudication that the hold was placed in error |
| satisfied_by_processing_event_id, satisfied_at | BIGINT; TIMESTAMPTZ | composite FK to `device_processing_events (tenant_id, id)`; CHECK `…_satisfied_check`: `status <> 'satisfied' OR satisfied_by_processing_event_id IS NOT NULL` |
| metadata, created_at, updated_at | | |

Uniques: partial `(tenant_id, device_id, hold_type) WHERE status = 'active'` — one active hold per type per device (the idempotency the reconciler relies on, §5.7). Indexes: `(tenant_id, device_id, status)`, `(tenant_id, status, hold_type)`. Release authority per hold type is in §5.5.

### 4.13 `device_processing_events` (append-only; the evidence identity — review 2026-09-06, point 4)

A passed *status* is not a processing *event*. One row per device per processing occurrence, written by `releaseToAvailableTx` (§5.6) and never updated.

| Column | Type | Notes |
|---|---|---|
| id | BIGSERIAL PK; UNIQUE `(tenant_id, id)` | |
| tenant_id, domain, device_id | | composite three-column FK to the register |
| kind | VARCHAR(24) NOT NULL | CHECK: `sterilization_load`, `chemical_reprocessing`, `manual_reprocessed` |
| sterilization_load_id | BIGINT | composite FK to `sterilization_loads (tenant_id, id)`; CHECK `…_load_kind_check`: `(kind = 'sterilization_load') = (sterilization_load_id IS NOT NULL)` |
| dialyzer_reuse_register_id | BIGINT | composite FK `(tenant_id, dialyzer_reuse_register_id) → dialyzer_reuse_register (tenant_id, id)` |
| attempt_id | BIGINT | composite FK to `dialyser_reprocessing_attempts (tenant_id, id)` (added after §4.14 exists) |
| protocol_id | INTEGER | composite FK to `reprocessing_protocols (tenant_id, id)` |
| cycle_type | VARCHAR(20) NOT NULL | the six |
| outcome | VARCHAR(16) NOT NULL | CHECK: `passed`, `failed`, `invalidated`, `not_established` |
| sterility_evidence | JSONB NOT NULL DEFAULT '{}' | OT: the three indicator results and `released_at`; dialysis: agent, concentration, contact minutes, TCV set |
| function_evidence | JSONB NOT NULL DEFAULT '{}' | OT: `return_condition`, inspection outcome; dialysis: integrity result — **sterility and function recorded apart** (point 6) |
| device_last_returned_at | TIMESTAMPTZ | the device's most recent `returned_at` when the event was recorded — the temporal-applicability witness: a load whose `completed_at` precedes this is **not applicable** and gets no event (§5.2) |
| cycle_before, cycle_after, device_version_before, device_version_after | INTEGER | |
| recorded_at, recorded_by, recorded_via | TIMESTAMPTZ NOT NULL; UUID (NULL = system); VARCHAR(24) | CHECK on `recorded_via`: `load_created`, `load_transitioned`, `dialysis_record`, `dialysis_attempt`, `manual_reprocessed` |
| metadata | JSONB | |

Uniques: partial `(tenant_id, device_id, sterilization_load_id) WHERE sterilization_load_id IS NOT NULL` — **exactly once per device × load**; partial `(tenant_id, dialyzer_reuse_register_id) WHERE dialyzer_reuse_register_id IS NOT NULL AND attempt_id IS NULL`; partial `(tenant_id, attempt_id) WHERE attempt_id IS NOT NULL`. Append-only (`runtime_append_only_relations`; UPDATE revoked). On `main` a metadata-only PATCH on an already-passed load re-derives `passed` through `deriveLoadStatus` and re-runs `transitionSterilizationLoad`'s set UPDATEs; the exactly-once unique makes the platform's handler a no-op for a `(device, load)` it has already counted, and the applicability witness refuses a load completed before the device's last return.

### 4.14 `dialyser_reprocessing_attempts` (append-only; recovery after settlement — review 2026-09-06, point 2)

| Column | Type | Notes |
|---|---|---|
| id | BIGSERIAL PK; UNIQUE `(tenant_id, id)` | |
| tenant_id, device_id | | composite FK to the register (dialysis domain pinned by the three-column FK) |
| device_usage_id | BIGINT NOT NULL | composite FK `(tenant_id, device_usage_id, device_id) → reprocessable_device_usages (tenant_id, id, device_id)` |
| dialyzer_reuse_register_id | BIGINT NOT NULL | composite FK to the settled statutory row this attempt follows |
| attempt_no | INTEGER NOT NULL | UNIQUE `(tenant_id, dialyzer_reuse_register_id, attempt_no)` |
| authorising_hold_id | BIGINT | composite FK to `reprocessable_device_holds (tenant_id, id)` — required when the device was quarantined (the released hold this attempt satisfies) |
| protocol_id | INTEGER NOT NULL | composite FK |
| integrity_test_result, measured_tcv_ml, baseline_tcv_ml, tcv_pct_of_baseline, reprocessing_agent, disinfectant_concentration_pct, disinfectant_contact_minutes, process_parameters | | the release-criteria evidence, same CHECKs as §4.8 |
| verdict | VARCHAR(24) NOT NULL | CHECK: `released`, `not_established`, `discarded` |
| missing_evidence TEXT[] NOT NULL DEFAULT '{}'; verdict_reason VARCHAR(40) | | |
| processing_event_id | BIGINT | composite FK to §4.13; CHECK: `verdict <> 'released' OR processing_event_id IS NOT NULL` |
| recorded_at, recorded_by, notes | TIMESTAMPTZ NOT NULL; UUID NOT NULL; TEXT | |

Why append-only: after a usage is settled as quarantined or `not_established` its statutory row cannot be rewritten (`DIALYZER_REUSE_REGISTER_SETTLED`); a later legitimate reprocessing is a **new attempt linked to the original device and use**, so the trail reads *settled → held → released by IC → attempt 1 not established (no baseline) → attempt 2 released*. The 418 row's `status` stays what the settlement wrote; `release_status` moves to `released` by the attempt that releases (§4.8).

### 4.15 `bloodborne_exposure_outbox` (durable exposure delivery — review 2026-09-06, point 9)

| Column | Type | Notes |
|---|---|---|
| id | BIGSERIAL PK; UNIQUE `(tenant_id, id)` | |
| tenant_id | | GUC default |
| marker_row_id | BIGINT NOT NULL | single-column FK `REFERENCES patient_bloodborne_markers(id) ON DELETE CASCADE` (the same deviation as §4.12, same reason); UNIQUE `(tenant_id, marker_row_id)` — one row per reactive marker row, ever |
| patient_uid, marker, tested_on | UUID NOT NULL; VARCHAR(20) NOT NULL; DATE | `tested_on` NULL for an undated legacy declaration (§3.2) |
| event | JSONB NOT NULL | the exact `{ tenantId, patientUid, marker, testedOn, markerRowId }` `notifyExposureHandlers` receives |
| status | VARCHAR(16) NOT NULL DEFAULT 'pending' | CHECK: `pending`, `processing`, `delivered`, `failed` — mig 588's vocabulary |
| delivered_handlers | TEXT[] NOT NULL DEFAULT '{}' | handler names that have acknowledged; `delivered` when every registered handler has |
| attempts, available_at, lease_owner, lease_expires_at, last_error, delivered_at, created_at | | 588's lease / backoff shape, copied not shared (§5.7) |

Indexes: `(tenant_id, status, available_at)`. Mutable-no-delete. Written by `enqueueExposureEventsTx` **inside** the marker writer's transaction (§5.7).

## 5. Flows

### 5.1 Dialysis

**Capture** — `POST /api/v1/dialysis/sessions/:id/dialyser`, idempotency scope `dialysis_dialyser_capture`, session guard before the claim. Body `{ manufacturer_serial?, device_tag?, model_name?, manufacturer?, hospital_asset_id?, baseline_tcv_ml?, initial_cycle_count?, exposure_acknowledgement?: { reason } }` — exactly one of `manufacturer_serial` / `device_tag`. Inside one tenant transaction, session locked `FOR UPDATE`:

1. Session status ∈ {`scheduled`, `in_progress`}, no open usage on the session → else 409 `DIALYSER_ALREADY_CAPTURED`. The `dialyser` category policy must be reprocessable → else 409 `RPD_POLICY_NOT_REPROCESSABLE` (dark by default: the capture route refuses until a policy exists, and the legacy free-text `dialyser` column keeps working as today).
2. Resolve the device `FOR UPDATE` by tag, or by `(tenant, 'dialysis', manufacturer_serial)`; a serial nobody has seen mints a row (`enrolled_via = 'session_capture'`, `status = 'available'`, `cycle_count = initial_cycle_count ?? 0` with `metadata.enrolled_mid_life = true` when non-zero, `max_cycles_snapshot` from policy) and a link row dedicated to the session's patient with `baseline_tcv_ml` if given. A tag unknown in the tenant → 404 `RPD_DEVICE_NOT_FOUND`; a tag whose device is not `dialysis` → 409 `RPD_DOMAIN_MISMATCH`.
3. **Dedication**: link row exists and `dedicated_patient_uid <> session patient` → 409 `DIALYSER_DEDICATED_TO_ANOTHER_PATIENT`. No override exists for this; a dedicated dialyser on another patient is a never event.
4. Device status must be `available` → else 409 `RPD_DEVICE_NOT_AVAILABLE` with the status. **No active hold** on the device → else 409 `RPD_HOLD_ACTIVE` (§5.5; the historical `exposure_flag` is never a capture-time input). The ceiling **never refuses a capture**: a device at `cycle_count = max_cycles_snapshot` is on its last permitted use (§2, `max_cycles` semantics); `RPD_MAX_CYCLES_REACHED` is a reprocessing-time and mint-time refusal only. **Residual test (review 2026-09-06, point 6):** when the device's `residual_test_pending` is set (a reprocessed dialyser under a protocol with `residual_test_required`), the body must carry `pre_use_residual_test: 'negative'` → else 409 `RPD_RESIDUAL_TEST_REQUIRED`; the value is written on the usage and the device flag cleared in the same statement. A deactivated policy (§4.2) refuses (`RPD_POLICY_DEACTIVATED`).
5. The resolver's live `Decision` (§3.3; **DEPENDS ON Phase 1**) is frozen as `reuse_screen` — the evidence snapshot of what the capturing user saw, never re-read for a later decision. **Reuse eligibility is decided here too** (`reuseEligibilityTx`, §5.3) for a device that has already been reprocessed (`cycle_count > 0`): a dialyser dedicated to a patient whom the protocol's matrix marks `no_reuse` (HBsAg-reactive, HIV-reactive, mixed; HCV when the protocol says so) cannot be **captured as a reprocessed device** — 409 `RPD_REUSE_NOT_ELIGIBLE` carrying the verdict code and never a class; its *first* use is permitted (a new dialyser), and the post-use record will discard it. **The former same-patient exception is withdrawn** (review 2026-09-06, point 5): dedication does not make a dialyser reusable on an HBsAg- or HIV-reactive patient; it survives only as the HCV `dedicated_reuse` option under an approved protocol. Under `override_allowed` the acknowledgement path applies only to verdicts the matrix leaves open (`unknown` under `warn`; HCV `dedicated_reuse`); a matrix `no_reuse` cannot be acknowledged away, and a safety review is written for every acknowledgement (§5.4).
6. Write the usage row (`dialysis_session_id`, `patient_uid` — read from the locked session's roster row, never the body — `reuse_cycle = device.cycle_count`, `capture_source`, `capture_provenance = 'live'`, `ready_processing_event_id = device.last_processing_event_id`, `pre_use_residual_test`), device → `in_case` (`version + 1`), `dialysis_sessions.dialyser = COALESCE(model_name, manufacturer_serial)` and `reuse_count = reuse_cycle` (the legacy columns become derived, so the today board and the completion billing path keep working unchanged). Audit `rpd.device.captured`; realtime `dialyser-captured`; the response carries the command receipt (§5.8).

**Isolation warnings** at `startSession` / `scheduleSession` / `PATCH /sessions/:id/machine` per §3.4 — a live `Decision` each time (DEPENDS ON Phase 1).

**Reprocessing record** — `POST /api/v1/dialysis/sessions/:id/reuse-register` (existing route; now claims a key: scope `dialysis_reuse_register`, `retainOnServerError: true` because a per-session upsert has no self-blocking `from` list — scouting risk 8). Body: 418's fields plus `measured_tcv_ml`, `reprocessing_agent`, `disinfectant_contact_minutes`, `disinfectant_concentration_pct`, `baseline_tcv_ml?` (mid-life enrolment only), `acknowledgement?: { reason }`, `expected_version?`. Behaviour, in order:

- **Dark** (no policy for `dialyser` **and** no register row for the category, §4.2) → the legacy path, byte for byte (free-text serial, typed count, upsert; `release_status = 'legacy'`). **Deactivated** (a policy that was on; devices exist) → the platform path below, which settles the usage and cannot release.
- **End of use is a precondition (review 2026-09-06, point 8).** The session must be `completed` (`actual_end_at` set by `completeSession`) or `cancelled` from `in_progress` (aborted; `actual_start_at` set — the only two exits from a started session under `SESSION_TRANSITIONS`) → else 409 `RPD_USE_NOT_ENDED`. A `scheduled` session has no use to record: its dialyser leaves by un-capture (Cancel, below). **Behaviour change:** `recordReuseRegister` on `main` checks no session status.
- **Policy on, session has an open usage** → the device is the usage's device; `reuse_cycle_count` in the body must equal `usage.reuse_cycle` or be absent → else 409 `DIALYZER_REUSE_CYCLE_DERIVED` (behaviour change; the admin modal renders the count read-only).
- **Policy on, no usage, `dialyzer_serial` present** → a **retrospective capture** (steps 2–6 above with `capture_source = 'system'`, `capture_provenance = 'retrospective'`, `captured_at = now()`, the `reuse_screen` flagged as recorded after the fact, and no acknowledgement path: a retrospective capture on a restricted or ineligible patient refuses with the capture code rather than guessing), then continue. The statutory row carries `capture_provenance = 'retrospective'` and the census counts them per tenant (§7.6) — an honest record of an association made after the use (review 2026-09-06, point 8).
- **Three decisions, re-resolved live inside the transaction** (§5.3; the frozen `reuse_screen` is not consulted, so a marker recorded since capture reaches this record): (1) the `Decision` (evidence); (2) nothing — routing is not a post-use question; (3) `reuseEligibilityTx` — may this dialyser be reprocessed for this patient: the protocol matrix over the class (in-process), `surveillance_overdue`, the device's active holds, the ceiling. `computeDispositionOptions` turns the verdict and the domain rule into the allowed set; the `Decision` acted on is frozen as `post_use_screen`. Body `status = 'in_use'` asks for `reprocess`; `discarded` asks for `discard`; `quarantined` asks for `quarantine`. A request outside the allowed set → 409 `RPD_DISPOSITION_NOT_ALLOWED` carrying `allowed`, `discard_reason`, `blocked_code` and the eligibility `verdict` code (never a class or a marker). `reprocess` under `unknown + warn` requires `acknowledgement.reason`; a matrix `no_reuse` cannot be acknowledged.
- **`quarantine` closes the usage first (review 2026-09-06, point 7):** device `return` (`in_case → awaiting_reprocessing`; the usage closes with the quarantine disposition), then `quarantine` (`awaiting_reprocessing → quarantined`, `quarantine_reason = 'exposure_hold'`), one transaction, plus a hold row (§5.5: `hold_type = 'bloodborne_exposure'`, `reason_code = 'exposure_at_return'`, `source_usage_id`) and `exposure_flag = true`. Nothing names the marker. The 418 row is written with `status = 'quarantined'`, `release_status = 'quarantined'`.
- **`discard`**: device `return` then `discard` (reason from the verdict — `bloodborne_exposure`, `max_cycles_reached`, or `other`), usage disposition accordingly; `exposure_flag` set when the reason is exposure; no marker anywhere.
- **`reprocess` — the release criteria (review 2026-09-06, point 6; §4.2a).** The device goes `return` (→ `awaiting_reprocessing`) and is released to `available` by `releaseToAvailableTx` (§5.6) **only when every mandatory item is present and passing**: (a) the eligibility verdict is `eligible` or `eligible_with_acknowledgement` (with the acknowledgement); (b) `integrity_test_result = 'pass'` when the protocol requires it (`pending` / `not_done` are not passes); (c) a **baseline TCV is recorded** on the link row — for a mid-life enrolment the protocol's approved alternative decides (`refuse`; `first_measured_with_ic_signoff` writes this measurement as the baseline with the approver; `manufacturer_nominal` uses the protocol's nominal), and **a missing baseline never passes** (`baseline_tcv` is named in `missing_evidence`); (d) `measured_tcv_ml` present and `measured / baseline ≥ tcv_min_pct` (the protocol's, default 80 %, floor 80 %); (e) `reprocessing_agent` in the protocol's agent list with `disinfectant_concentration_pct` inside its bounds and `disinfectant_contact_minutes ≥` its minimum; (f) not at the ceiling. Then: `reprocessed` (`cycle_count + 1`, `last_cycle_type = 'chemical'`, `last_function_check = 'pass'`, → `available`, `residual_test_pending = protocol.residual_test_required`, `version + 1`), a `device_processing_events` row (`kind = 'chemical_reprocessing'`, `outcome = 'passed'`, the sterility and function evidence sets), the usage's `post_use_processing_event_id`, the 418 row's `release_status = 'released'` with `processing_event_id`. **Failures split three ways:** (d) below threshold → `discard` with `tcv_below_threshold` (a physical fact about the fibre bundle); (b) `= 'fail'` → `discard` with `integrity_test_failed`; **everything else missing or out of bounds → `not_established`**: the usage settles (`released_not_established`), the 418 row is written with `release_status = 'not_established'` and `missing_evidence[]` naming each item, the device **stays `awaiting_reprocessing`** (never `available`), and the response is 200 with `release: { status: 'not_established', missing: [...] }` — the reuse room completes the evidence through an **attempt** (below). "Missing evidence treated as passing" is gone from the lane.
- **Attempts — `POST /api/v1/dialysis/dialysers/:deviceId/reprocessing-attempts`** (scope `dialysis_reprocessing_attempt`; roles `DIALYSIS_ROUTE_ROLES`; the device is the subject — the route writes the same per-patient PHI access rows the history read writes, §6.4). Body: the release-criteria fields plus `expected_version`. Preconditions: the device is `awaiting_reprocessing` or `in_cssd`, its most recent usage is settled, **no active hold** (a *released* hold with `release_requires_processing` is what this attempt satisfies — `authorising_hold_id`), the policy is active → else 409 `RPD_ATTEMPT_NOT_ALLOWED`. Writes an append-only `dialyser_reprocessing_attempts` row (`attempt_no` = previous + 1) with the verdict; a `released` verdict runs the same `releaseToAvailableTx`, records the processing event, marks the authorising hold `satisfied`, and moves the 418 row's `release_status` to `released` (the one status the settled row still accepts, §4.8). This is the recovery path the review asked for: *settled as quarantined → hold released by IC → attempt → released*, with nothing rewritten.
- The 418 row is upserted as today with `device_id`, `device_usage_id`, `reuse_cycle_count = usage.reuse_cycle`, `session_reuse_count = usage.reuse_cycle`, the TCV / agent / protocol / provenance fields; **a second write for a session whose usage is already returned** answers the stored row with 409 `DIALYZER_REUSE_REGISTER_SETTLED` unless the body is identical (`notes` may still be edited). Audit `dialysis.reuse_register.recorded` (metadata: disposition, verdict code, `release_status`, `tcv_pct`, `decision.status` — never reasons, markers or a class); realtime `reuse-register-updated`; the response carries the command receipt (§5.8).

**Read** — `GET /api/v1/dialysis/sessions/:id/dialyser` returns `{ usage (projected, §3.5), device (no marker field exists on it), link: { dedicated_patient_uid, baseline_tcv_ml, baseline_tcv_source }, holds: [{ id, hold_type, reason_code, status, placed_at }] (the marker pointer only for audience roles), reuse_restriction (the live Decision, projected), reuse_eligibility: { verdict } (a code), allowed_dispositions, release: { residual_test_pending, last_release_status }, isolation: { codes, isolation_warnings, warn_only, enforcement_enabled, override } }` or `{ usage: null, … }` when nothing is captured. `isolation` is not projected because it carries no class to project (§3.4, §3.5): this read does **not** pass `includeIsolationClass`, so its `Decision` has no class to publish, and the warnings — `required_group` included — are read back from the session row and re-serialised through the one serialiser. DEPENDS ON Phase 1. The existing `GET /sessions/:id/reuse-register` is unchanged in shape and gains the new columns.

**Cancel** — `POST /api/v1/dialysis/sessions/:id/cancel` (existing route, existing guard). `cancelSession` moves from the bare client into `setTenantTx` with the session locked `FOR UPDATE`; after the status walk is checked and **before** the status `UPDATE`, `dialysisReuseService.onSessionCancelledTx(tx, { session, target, context })` runs:

1. No usage row for the session → nothing (nothing was captured, or the feature is dark). The cancel proceeds as today.
2. The session's usage is already closed (`returned_at` set), **or** a `dialyzer_reuse_register` row references `device_usage_id = usage.id` → 409 `RPD_USAGE_NOT_CANCELLABLE`. Use was recorded on this session: the statutory row says the dialyser was used here, and `cancelled` would contradict it. Such a session ends by `complete`, not `cancel`.
3. The session is `in_progress` (`actual_start_at` set) → 409 `RPD_RETURN_REQUIRED`. Blood contact happened or cannot be ruled out; the reprocessing record must be written first (which closes the usage and lands the device in `available`, `quarantined` or `discarded` by the rule) — and once it is written, rule 2 applies. Patient-contact uncertainty means reprocess, never release. The usage table carries no contact-time column — 418's `disinfectant_contact_minutes` is a reprocessing-time field, not patient contact — so the owner's state is the contact signal.
4. Only a `scheduled` session (`→ cancelled` or `→ no_show`) un-captures, and only on an **affirmative unopened condition** (review 2026-09-06, point 8): the cancel body must carry `dialyser_condition: 'sealed_unopened' | 'not_connected'` → else 409 `RPD_UNOPENED_CONFIRMATION_REQUIRED` — "scheduled" is not proof that nobody primed the dialyser, so the person cancelling says so and is recorded (`unopened_confirmation` on the usage). Then `reprocessableDeviceService.uncaptureDeviceTx` closes the usage (`post_use_disposition = 'cancelled_before_use'`, `returned_at = now()`, `returned_by = actor`, `metadata.cancel_target`), applies `uncapture` through `releaseToAvailableTx`'s un-capture arm (device `in_case → available`, `current_usage_id` cleared, `cycle_count` unchanged, no reprocessing field written, `version + 1`), audits `rpd.device.uncaptured`, and nulls `dialysis_sessions.reuse_count` (it claimed a cycle that never ran; the `dialyser` text is left — nothing reads it on a cancelled session). **A hold that arrived while the device was captured is respected:** if any hold on the device is `active` (`pending_return`, placed by the late-reactive handler on a marker of the dedicated patient), the un-capture lands the device in **`quarantined`** in the same transaction (`available` then `quarantine`, `quarantine_reason = 'exposure_hold'`), never in `available` — the sealed pack is still the pack a hold names.

Every refusal runs before the cancel writes anything, and a refused un-capture fails the cancel — there is no best-effort branch here, because a cancel that succeeds while the device stays `in_case` is the defect this closes. The released dialyser is captured again on the patient's next session at the same `reuse_cycle` (§4.4).

**Machines** — `GET /api/v1/dialysis/machines`, `POST /machines`, `PATCH /machines/:id` (scope `dialysis_machine`), roles `DIALYSIS_MACHINE_ADMIN_ROUTE_ROLES` = `DIALYSIS_ROUTE_ROLES ∩ {NURSING_INCHARGE, IP_INCHARGE, ICU_INCHARGE, ADMIN, SUPER_ADMIN}` (an intersection, so the gate can never be dead). `POST /machines` and `PATCH /machines/:id` accept `isolation_group` — a label from the tenant's **approved** vocabulary (`reprocessing_domain_settings.approved_isolation_groups`, §4.1), `null` for a general machine; a label outside it is refused (`RPD_ISOLATION_GROUP_NOT_APPROVED`), and no isolation class is accepted anywhere. Retiring a machine while `block` is on and it is the last active carrier of a mapped group is refused (`RPD_ISOLATION_BLOCK_PRECONDITIONS`, §3.4). `PATCH /sessions/:id/machine` `{ machine_no, isolation_override_reason? }` re-evaluates §3.4 and is on the session guard.

### 5.2 OT (hooks inside `cssdService`'s transactions; ONE load-outcome handler; dark without a policy)

- **`issueSet`**: after the set is locked and the existing refusals have run, `cssdReuseHooks.onSetIssuedTx(tx, { set, issue, otSchedule, context })`: policy for `set.set_type` reprocessable → resolve or mint the set's register row (`enrolled_via = 'set_issue'`, `hospital_asset_id = set.barcode`, `status = 'available'`, `cycle_count = 0` with `metadata.enrolled_from_existing_set = true` — such a set has no `ready_processing_event_id`, and the usage says so); device must be `available` (a register-`quarantined` set refuses issue with 409 `CSSD_SET_QUARANTINED` naming the **reason code**, never a note); **no active hold** (`RPD_HOLD_ACTIVE` — the historical `exposure_flag` is not an issue-time input; review 2026-09-06, point 3: a released, reprocessed set is re-issuable); a deactivated policy refuses (`RPD_POLICY_DEACTIVATED`); `expected_version` when the caller sends one; serology screen for `ot_schedules.patient_uid` → `reuse_screen`; `reuseEligibilityTx` for the OT patient (the matrix applies to OT only when the tenant's OT protocol says so — the default OT protocol has no pathogen-specific arm, so a restricted patient's set is issued and the return rule decides; where a set is issued to a restricted patient under `override_allowed` an acknowledgement is taken and written on the usage — the return path reads whether one exists); usage row (`ot_schedule_id`, `set_issue_log_id`, `instrument_set_id`, `patient_uid` from the schedule row, `reuse_cycle`, `capture_source = 'cssd_issue'`, `ready_processing_event_id = device.last_processing_event_id`); device `capture` → `in_case`. `POST /api/v1/cssd/issues` now claims a key (scope `cssd_set_issue`; the admin `createCssdIssue` caller moves to `core.ts` with a key).
- **`transitionIssue` → `returned`**: `onSetReturnedTx`: post-use screen and eligibility verdict re-resolved live; `computeDispositionOptions` for the OT domain — there is no operator disposition at return, the rule decides: `clear` → device `return` (→ `awaiting_reprocessing`), usage `sent_for_reprocessing`; `restricted` under `quarantine` → device `return` then `quarantine` (`quarantine_reason = 'exposure_hold'`) with a hold (`bloodborne_exposure`, `reason_code = 'exposure_at_return'`, `source_marker_row_id` from the reactive row `resolveReuseStatus` carries server-side — its id, never its name) and `exposure_flag = true`, usage `quarantined_bloodborne_exposure`; under `discard` → `return` then `discard` (reason `bloodborne_exposure`) and `instrument_sets` set `usable = false, requires_reprocessing = true`; under `override_allowed` → **the acknowledgement point is defined (review 2026-09-06, point 7):** if the usage carries an issue-time acknowledgement (`acknowledgement_reason` set — the patient was restricted at issue and a person accepted it) → `return` with the flag stamped and no second acknowledgement; if it carries **none** (the patient became restricted after issue, or the result arrived post-issue) → nobody has accepted anything, so the set is **held**: `return` then `quarantine` with `hold_type = 'post_issue_restriction'`, `reason_code = 'restricted_after_issue'`, usage `quarantined_post_issue_restriction`, and the infection-control release (§5.5) is where the acknowledgement is obtained; `unknown` under `warn` → `return`; under `block_return` → `return` then `quarantine` with `hold_type = 'serology_required'`, `quarantine_reason = 'serology_required'`, usage **`quarantined_serology_required`** — never the exposure disposition. **Function versus sterility (point 6):** `return_condition = 'damaged'` additionally places an `inspection_failed` hold (`reason_code = 'return_condition_damaged'`) that CSSD resolves by release after inspection or repair — a passed load is sterility evidence and never clears it; `'contaminated'` stamps `metadata.return_condition` and changes nothing else (universal precautions; decontamination is the normal path).
- **`transitionIssue` → `cancelled`** (reachable only from `issued` — `ISSUE_TRANSITIONS.in_theatre` does not admit it; `cancelIssue` is a one-line wrapper over `transitionIssue`, so there is one path): `onIssueCancelledTx(tx, { issue, patch, context })` runs after the transition check and **before** the shared `set_issue_log` UPDATE. No register row for the set, or no usage for this issue → nothing. The usage already closed → 409 `RPD_USAGE_NOT_CANCELLABLE`. `issue.theatre_use_started_at` set, **or** the cancel body declares `pack_condition: 'opened'` → 409 `RPD_RETURN_REQUIRED`: the set is returned and the return rule decides. **An omitted condition does not mean "definitely unopened" (review 2026-09-06, point 8):** the body must carry `pack_condition: 'sealed_unopened'` → else 409 `RPD_UNOPENED_CONFIRMATION_REQUIRED`; the affirmation is written on the usage. Otherwise `uncaptureDeviceTx`: usage `cancelled_before_use`, device `in_case → available` — or **`in_case → available → quarantined` in the same transaction when an active hold arrived while the set was out** (a late reactive result on the previous patient), cycle unchanged, `rpd.device.uncaptured`; the existing cancel branch then restores `instrument_sets` exactly as today, and the next `issueSet` of the same set captures at the same `reuse_cycle` (the cycle unique is partial on `cancelled_before_use`, §4.4). A refused un-capture fails the cancel.
- **ONE load-outcome handler — `cssdReuseHooks.applyLoadOutcomeTx(tx, { load, status, setIds, source, context })` — for creation and for transitions (review 2026-09-06, point 4).** On `main` two paths write a load outcome: `createSterilizationLoad` (derives the status from the body through `deriveLoadStatus`, can create a load already `passed` or `failed`, then UPDATEs `instrument_sets` and `set_issue_log` directly) and `transitionSterilizationLoad` (re-derives the status from the patch, so a metadata-only PATCH on an already-passed load re-runs the same set UPDATEs). Both now call `applyLoadOutcomeTx` after their `sterilization_loads` write and before their audit row, with `source = 'load_created' | 'load_transitioned'`. **The call-site test discovers, it does not enumerate:** `cssdLoadOutcomeCallSites.test.js` finds every statement in `cssdService.js` that writes a load outcome onto a set or issue (regex over `UPDATE instrument_sets … last_passed_load_id`, `UPDATE instrument_sets … status = 'unusable'`, `UPDATE set_issue_log … sterilization_load_id`, `UPDATE set_issue_log … 'sterilization_failed'`), asserts each sits in a function that calls `applyLoadOutcomeTx`, **and asserts the discovered population equals a pinned snapshot count** (today: two functions, seven statements) so a write site reformatted out of the regex fails loudly rather than shrinking the population (the empty-population class). For every set in `set_ids` with a register row the handler decides per device, in ascending device id, each under the device lock (§5.8):
  - **Applicability and exactly-once first.** A `device_processing_events` row for `(device, load)` already exists → **no-op** (`outcome: 'already_counted'`) — a metadata edit to an old passed load increments nothing and releases nothing. The load's `completed_at` precedes the device's most recent `returned_at` → **not applicable** (`outcome: 'not_applicable'`: the load ran before this cycle's return; reported in `affected_devices[]`, nothing else changes). The device is `in_case` → a passed load is not applicable either (the set left CSSD before the load completed — reported), and a **failed** load is not ignored for it: see the failure response.
  - `passed` → if `load.cycle_type ∈ policy.allowed_cycle_types`, the device is `awaiting_reprocessing` or `in_cssd`, **no hold is active** and the policy is active: `releaseToAvailableTx` (§5.6) records the processing event (`kind = 'sterilization_load'`, `outcome = 'passed'`, `sterility_evidence` = the three indicator results and `released_at`, `device_last_returned_at`), applies `reprocessed` (`cycle_count + 1`, `last_cycle_type = load.cycle_type`, `last_sterilization_load_id`, `last_processing_event_id`, `last_function_check = 'not_required'`, → `available`), writes the most recent settled usage's `post_use_processing_event_id` (write-once), and marks any released hold whose `release_requires_processing` this event satisfies (`satisfied`). A cycle type the policy forbids → `quarantine` with `quarantine_reason = 'cycle_type_not_allowed'` and a `cycle_type_not_allowed` hold. A device with an **active** hold of any type is left where it is (`outcome: 'held'`) — a passed load never clears a hold, exposure, `sterilization_failed`, `inspection_failed` or otherwise; the hold is released by its authority (§5.5) and the **next** applicable load satisfies it. At the ceiling a passed load discards (`max_cycles_reached`).
  - `failed` (a fresh failure, or a **passed load later moved to failed** by a late indicator read — `deriveLoadStatus` returns `failed` whenever an indicator is `failed`, whatever `status` says) → **the minimum failure-response workflow runs now, not in a follow-up (point 4):** (i) every device with a processing event on this load — **whatever its current state**: `available`, `in_case`, already re-issued and returned — gets a `load_invalidated` hold (`reason_code = 'load_invalidated_after_release'`; `pending_return` when `in_case`) and, when it is `available` / `awaiting_reprocessing` / `in_cssd`, the `quarantine` transition (`quarantine_reason = 'load_invalidated'`); devices in the load's `set_ids` that are `awaiting_reprocessing` / `in_cssd` with no event yet get a `sterilization_failed` hold and quarantine; (ii) **affected patients are identified**: every usage whose `ready_processing_event_id` is an event of this load — the set was used after this load made it "ready" — is listed with its patient and case, and one `cds_alerts` row per patient (`alert_type = 'sterilization_failure_exposure'`, `severity = 'high'`, naming the load code and the set, never a marker) is raised; (iii) **further issue is prevented** by the holds; (iv) **infection control is notified**: one `notification_outbox` row per active officer (`type = 'sterilization_failure'`, `sourceEventKey = 'sterilization-failure:<load id>:<officer uid>'`) with the device and patient counts and the deep link; (v) the response carries `affected_devices[]` and `affected_usages[]`, and the audit event `rpd.load.failure_response` records both lists. The CDC distinction (indicator type, method, implant involvement) is **infection control's adjudication on release**, not a reason to skip the hold; a screen that tracks the response is the deferred dashboard (§10).
  - `cancelled` / `running` / `completed` / `planned` → nothing.
- **CSSD `discard` on an OT device** also sets `instrument_sets.status = 'retired'`, `retired_at`, `retired_by`, `retirement_reason` (the register's discard reason code) — the one place the register writes the set's status.

What this lane does **not** add for OT (recorded, not hidden): a BI-to-patient lookback across loads *earlier* than the one that failed (the register now has the data — `ready_processing_event_id` per usage — and the failure response covers the failed load's own devices and patients; a wider lookback is §10); there is no single-use flag on any OT item; the WHO sign-in strip is a Staff surface in §6.8 reading the same `reuse_restriction`, not a gate.


### 5.3 Three decisions, and the shared disposition rule

**Decision 1 — what evidence exists.** The Phase 1 `Decision` for dialysis (§3.3) and `resolveReuseStatus` for OT: `status`, the evidence kind, `evidence_dated_on`, `surveillance_overdue`. Neither function knows a device or a machine.

**Decision 2 — where a patient dialyses.** `computeIsolationWarnings` (§3.4): reads `status` and, in-process, the class → group map. It never reads a device.

**Decision 3 — may this device be reprocessed for this patient.** `reprocessableDeviceRules.reuseEligibility({ decision, protocol, device, holds, policy, dedicatedToPatient })` — pure; `reuseEligibilityTx` wraps it with the reads and is the second and last caller of `includeIsolationClass: true` (§3.3). It returns a **verdict code**, never a class: `eligible`; `eligible_with_acknowledgement` (`unknown` under `warn`; HCV `dedicated_reuse` when the device is dedicated to this patient); `not_eligible_matrix` (the class's cell is `no_reuse` — HBsAg, HIV and mixed always; HCV unless `dedicated_reuse`; a `restricted` Decision with no class is read as mixed); `not_eligible_hold` (an active hold — carries the hold types); `not_eligible_ceiling`; `not_established_surveillance` (`surveillance_overdue` and the protocol blocks on it — treated as `unknown` under the domain's unknown rule); `not_eligible_policy` (deactivated / no active protocol). The dialysis `reactive_patient_rule` is applied **after** the matrix: `override_allowed` can open only what the matrix leaves open; `discard` and `quarantine` decide what happens to a device the matrix closes.

**The shared disposition rule.** `reprocessableDeviceRules.computeDispositionOptions({ domain, usage, policy, settings, restriction, device, eligibility })` is `cathDeviceReuseService.computePostUseOptions` with the implant clause removed (no OT or dialysis category is an implant), `units_max` fixed at 1, the `quarantine` branch added, and the eligibility verdict consumed instead of re-deriving it from `restriction.status` alone: `not_eligible_matrix` under `discard` → discard only (`bloodborne_exposure`); under `quarantine` → `['quarantine', 'discard']` with `quarantine_reason = 'exposure_hold'`; `not_eligible_hold` → `['quarantine', 'discard']` naming the hold type; `not_eligible_ceiling` → discard (`max_cycles_reached`); `eligible_with_acknowledgement` → `['reprocess', 'discard']` with `requires_acknowledgement`; `eligible` → `['reprocess', 'discard']`; `unknown` under `block_return` → discard only with `blocked_code = 'RPD_SEROLOGY_REQUIRED'` (and, at an OT return, the `serology_required` hold — never the exposure disposition). The evaluation order is 765's: the device's own **active holds** (which replace the raw flag — the flag is history), then the ceiling **behind an explicit non-null guard** (`cathDeviceReuseService.computePostUseOptions` evaluates `Number(device.cycle_count) >= Number(device.max_cycles_snapshot)` and `Number(null)` is `0`, so an unguarded copy would treat every unbounded device as at its ceiling — review 2026-09-06, point 7), then the patient's verdict. **Parity with cath is asserted over the input domain the two functions share:** a non-null ceiling, a device whose "flag" means "has an active hold", no `quarantine` rule, and a verdict cath's tree would reach from the same `restriction.status` — and outside that domain (a null ceiling, a hold with no flag, the matrix's HCV cell) the unit test asserts *our* answer explicitly rather than cath's.


### 5.4 Overrides

Every `acknowledgement.reason` and `exposure_acknowledgement.reason` is written to `medication_safety_reviews` with `review_type = 'reprocessable_device_reuse'`, `finding_code` ∈ `BLOODBORNE_RESTRICTED_OVERRIDE` (HCV `dedicated_reuse` acknowledged), `SEROLOGY_UNKNOWN_ACKNOWLEDGED`, `EXPOSED_DEVICE_REUSED` (an OT set issued to a restricted patient under `override_allowed`), `ISOLATION_EMERGENCY_OVERRIDE` (§3.4), `override_required = true`, `payload.domain`, and the actor — the cath spec §7.5 shape with a domain field. `review_type` is `VARCHAR(80)` with no CHECK (269:196), so no migration is needed for the new value.

### 5.5 Historical exposure versus an unresolved exposure hold (review 2026-09-06, point 3)

Two records, never one flag doing both jobs:

- **`exposure_flag`** is **history**: set the first time a device touched a restricted patient or a hold of an exposure type was placed; never cleared — not by release, not by reprocessing, not by discard. Lookbacks, the CSSD queue's exposure column and audits read it. **Nothing refuses on it.**
- **`reprocessable_device_holds`** (§4.12) is the **operational restriction**: an active hold makes a device un-issuable (`CSSD_SET_QUARANTINED` / `RPD_HOLD_ACTIVE`), un-capturable, un-releasable and un-reprocessable. It is placed by the late-reactive handler (`bloodborne_exposure` / `prion_exposure`), by a return under the rule (`bloodborne_exposure`, `serology_required`, `post_issue_restriction`, `inspection_failed`), by the load-outcome handler (`sterilization_failed`, `load_invalidated`, `cycle_type_not_allowed`) or by a person (`manual`). It is resolved only by an **authorised release decision**.

**The release decision.** `POST /api/v1/cssd/reprocessable-devices/:id/holds/:holdId/release` `{ adjudication, protocol_id?, evidence?, requires_processing = true, expected_version }` records `released_by / released_role / released_at`, `release_adjudication` (what was decided and why — free text lives here), the processing protocol the release requires and the supporting evidence. **Authority per hold type**, each an intersection with `CSSD_DEVICE_ROUTE_ROLES` and asserted as a strict subset (§7.4): `bloodborne_exposure`, `prion_exposure`, `post_issue_restriction`, `serology_required`, `load_invalidated` → `HOLD_RELEASE_IC_ROUTE_ROLES = CSSD_DEVICE_ROUTE_ROLES ∩ {INFECTION_CONTROL_OFFICER, ADMIN, SUPER_ADMIN}`; `sterilization_failed`, `cycle_type_not_allowed`, `inspection_failed`, `manual` → `HOLD_RELEASE_CSSD_ROUTE_ROLES = CSSD_DEVICE_ROUTE_ROLES ∩ {CSSD_INCHARGE, INFECTION_CONTROL_OFFICER, ADMIN, SUPER_ADMIN}` (approved membership). A `prion_exposure` hold is released only under a protocol whose `prion_rule = 'ic_pathway'` (`RPD_PRION_HOLD_DISCARD_ONLY` otherwise) — routine OT blood-borne exposure and prion exposure never share a release rule (point 5). When the last active hold on a `quarantined` device is released, the same transaction applies `release` (`quarantined → awaiting_reprocessing`) with `quarantine_reason = 'release_pending_processing'` when `requires_processing`; the device becomes `available` only when a processing event satisfies the release (§5.6) — **reprocessing satisfies the release condition, it does not replace the decision**. `requires_processing = false` (an adjudication that the hold was placed in error — a wrong-patient marker later voided) releases straight to `available` through the same path, recorded as such. Audit `rpd.hold.placed`, `rpd.hold.released`, `rpd.hold.satisfied`.

**Acceptance test (§8):** exposure at return → `quarantined` with an active hold → issue / capture refused → authorised release with adjudication and protocol → device `awaiting_reprocessing` → applicable passed load (OT) / released attempt (dialysis) → hold `satisfied`, device `available`, `exposure_flag` still `true` → **successful re-issue / re-capture**. The earlier design could not pass this test: release cleared the quarantine fields and issue refused on the flag.

### 5.6 One release path (review 2026-09-06, point 2)

Every transition **to `available`** — the load-outcome handler's `reprocessed`, the dialysis record's `reprocessed`, an attempt's `reprocessed`, the manual CSSD `reprocessed`, and the un-capture — goes through `reprocessableDeviceService.releaseToAvailableTx(tx, { device, via, evidence, expectedVersion, context })`, which dispatches on domain and refuses before it writes:

- **common:** the device is `awaiting_reprocessing` / `in_cssd` (or `in_case` for the un-capture arm); **no active hold**; the policy is active (`RPD_POLICY_DEACTIVATED`); the ceiling (`RPD_MAX_CYCLES_REACHED` → the discard branch); `expected_version` matches when given;
- **dialysis** (`via = 'dialysis_record' | 'dialysis_attempt' | 'generic_reprocessed'`): the release criteria of §5.1 over the protocol — missing or failing evidence returns `{ released: false, status: 'not_established', missing }` and the device stays. The generic `reprocessed` command is **not a second CSSD shortcut**: it requires `device_usage_id`, takes the same statutory evidence and domain role gate, and delegates to `recordDialyserReprocessing` while the 418 row is unsettled or `recordDialyserReprocessingAttempt` after settlement. It never calls the transition directly and never rewrites the statutory row;
- **OT** (`via = 'load_outcome' | 'manual_reprocessed'`): **a processing event with the applicable load is required** — for the manual command `sterilization_load_id` is **mandatory** (`RPD_LOAD_REQUIRED`), must be `passed`, must list this device's set in `set_ids` (`RPD_LOAD_SET_NOT_IN_LOAD`), must have completed after the device's last return (`RPD_LOAD_NOT_APPLICABLE`), must carry a `cycle_type` the policy allows, and must not already have an event for this device (exactly once); "a passed load in this tenant" is no longer evidence. The manual command exists for the case where the load hook did not run (a set added to a load after its outcome; a repaired hook); it delegates and cannot bypass.

Inside the path, in order: the `device_processing_events` insert, the `reprocessed` transition (or `uncapture`), `stampUsagePostUseEventTx` (write-once), `last_processing_event_id`, hold satisfaction, `residual_test_pending` (dialysis), and the audit row. The generic `POST /reprocessable-devices/:id/reprocessed` first resolves the device domain, applies that domain's role and request contract, then delegates to the dialysis record/attempt command or the OT applicable-load command; it contains no release logic of its own. A unit pin (`releaseToAvailableCallSites.test.js`) asserts that **no statement in the lane sets `status = 'available'` outside `applyDeviceTransitionTx`, every `reprocessed` / `uncapture` call is inside `releaseToAvailableTx`, and the generic controller calls only the two domain commands**. **Policy deactivation** does not reopen the legacy bypass (§4.2): `releaseToAvailableTx` refuses, the hooks keep settling, and the dialysis route never re-enters its legacy branch for a session whose usage exists.

### 5.7 Durable exposure delivery (review 2026-09-06, point 9)

**The failure.** Patient A's set is used and returned clear; A's reactive result commits; the process dies between the commit and `notifyExposureHandlers`; patient B (clear) is assessed for the same set. B's resolver call is correct and cannot know about A — the missing information is the **device's hold**, and no read of B can create it. The pull-style resolver protects a patient whose own evidence changed; it does not protect a device from an exposure the push never delivered.

**The design.** `bloodborneMarkerService.recordMarkers` and `recordMarkersFromSignedResults` — the two writers on `main` that call `notifyExposureHandlers` after their `setTenantTx` — gain one call **inside** that transaction, after the marker rows are written and before commit: `exposureOutboxService.enqueueExposureEventsTx(tx, events)` inserts one `bloodborne_exposure_outbox` row per reactive marker row (§4.15; `UNIQUE (tenant_id, marker_row_id)` makes a replayed write a no-op). The row and the marker commit together or not at all. After commit the existing fan-out runs as the **fast path** and, per handler that returns, `markExposureDeliveredTx(rowId, handlerName)`; when every registered handler has acknowledged, the row is `delivered`. Anything else — a crash, a handler that threw, a lock timeout on one device — leaves the row `pending`.

**The reconciler.** `exposureOutboxService.drainExposureOutbox({ limit })` claims `pending` rows past `available_at` with a lease (`FOR UPDATE SKIP LOCKED`, `lease_owner`, `lease_expires_at` — mig 588's shape, copied not shared: `event_outbox`'s consumer is the webhook fan-out), calls **every registered handler not yet in `delivered_handlers`**, records success per handler, and on failure backs off (588's schedule; 7 attempts, then `failed` with `last_error` for operator redrive). It runs from three places: `utils/scheduler.js` every 60 s in the API process (beside the event-outbox drain, with `reapStaleExposureClaims` beside the reaper); **first** in `scripts/reconcile-bloodborne-markers.mjs` (#1017's operator sweep), which already refuses `--apply` when `exposureHandlerCount()` is zero and now drains the outbox before any repair and prints `pending / failed / drained`; and on demand through `POST /api/v1/reprocessing/exposure-outbox/drain` on the governance mount. **Idempotency is the handlers' contract:** the platform handler places at most one active hold per `(device, hold_type)` (the partial unique), stamps a flag that is already idempotent, skips a `cds_alerts` row whose `source_data.marker_row_id` exists, and relies on `notification_outbox`'s `sourceEventKey`; cath's handler uses `exposure_flag OR …` and `DISTINCT` markers. A drain run twice changes nothing the second time, and the deep test proves it. The `tested_on = NULL` lookback of §3.2 applies to a drained event exactly as to the fast path (the event carries `testedOn: null`).

**Test (§8):** stub `notifyExposureHandlers` to throw after the marker commit; assert the marker row exists, the outbox row is `pending`, and patient B's assessment shows no hold on the device; run `drainExposureOutbox`; assert the hold, the flag, the alert and the notification exist exactly once; run it again; assert the counts are unchanged. **Stage 3 (§1.2) is not entered without this.**

### 5.8 Lock order, command receipts and the three race tests (review 2026-09-06, additional conditions)

**Lock order — every transaction in the lane acquires rows in this order and never backwards:** (1) the owner row — `dialysis_sessions` or `set_issue_log` (`FOR UPDATE`); (2) `sterilization_loads` (when the command names a load: the load-outcome handler enters here, and the manual `reprocessed` locks its load **before** the device for that reason); (3) `instrument_sets` (OT); (4) `reprocessable_devices`, ascending id when several (`FOR UPDATE OF d`); (5) `reprocessable_device_usages`; (6) `reprocessable_device_holds`; (7) `reprocessable_device_dialysis_links`; (8) `dialyzer_reuse_register`; appends (`device_processing_events`, `dialyser_reprocessing_attempts`, `bloodborne_exposure_outbox`, `audit_logs`) take no row locks. The exposure handler settles each device in its own transaction starting at (4). The order is written once as `LOCK_ORDER` in `reprocessableDeviceService.js`, and a unit pin (`lockOrder.test.js`) asserts every `FOR UPDATE` in the lane's modules names a table at a rank not lower than the previous one in the same function.

**Receipts.** A from-list is not permanent idempotency: a device returns to `available`, and a stale retry of `capture` with a fresh key would succeed again. So every command binds to the **lifecycle version**: `reprocessable_devices.version` is incremented in the same `UPDATE` as every transition; commands from the CSSD queue and the Admin console send `expected_version` (**required** on the queue commands — the row shows it; optional on capture / issue, which are already bound to a session or an issue row), and a mismatch is 409 `RPD_VERSION_CONFLICT` carrying `current_version`. Every command's response carries **`receipt: { version, audit_id, action }`** — the audit row is append-only (`audit_logs`' trigger) and is the durable operation receipt; the idempotency middleware's stored response replays the same receipt for the same key. A client that lost a reply reads the device, compares `version`, and knows whether its command landed.

**The three concurrency tests** (deep; two connections; a barrier implemented as the second transaction blocking on the first's row lock — the `moneyLockIdempotency.deep` pattern; each **amplified 20×** in a loop, because a race that passes once proves nothing):

1. **Concurrent capture of an unseen serial:** two sessions capture `manufacturer_serial = 'X'` for the first time at once. The partial unique on `(tenant_id, domain, manufacturer_serial)` makes one `INSERT` fail; the loser must **re-lock by serial and continue** (dedication then decides — `DIALYSER_DEDICATED_TO_ANOTHER_PATIENT` for a different patient, `RPD_DEVICE_NOT_AVAILABLE` for the same). Assert: exactly one device row, exactly one open usage, one 409 with the right code, never two devices for one serial.
2. **Exposure versus release:** the late-reactive handler placing a hold on a device concurrently with an infection-control release plus the reprocessing that would satisfy it. Whichever wins the device lock, assert the end state is never `available` with an active hold, and that a hold placed after the release is `active` on an `awaiting_reprocessing` device (the next release must see it).
3. **Issue versus load outcome:** `issueSet` capturing a set concurrently with `applyLoadOutcomeTx` passing the load that contains it. Assert: either the load's event applied first and the set was captured `available` with `ready_processing_event_id` = that event, or the capture won and the load reports `not_applicable` for that device — never a processing event on an `in_case` device, never a usage whose `ready_processing_event_id` post-dates its `captured_at`.


## 6. Routes, OpenAPI, Admin and Staff

All mutations claim an `Idempotency-Key`; every guard that can refuse runs **before** the claim so a request that can never succeed burns no key. Route role sets are intersections with the mount audience and a unit test asserts the subset for each (§8).

### 6.1 Dialysis (`/api/v1/dialysis`, `DIALYSIS_ROUTE_ROLES`, `phiAccessLogger('DIALYSIS')`)

| Method and path | Guard | Scope | Purpose |
|---|---|---|---|
| `POST /sessions/:id/dialyser` | `guardDialysisSessionParam` | `dialysis_dialyser_capture` | §5.1 capture (DEPENDS ON Phase 1: the frozen `reuse_screen` is the resolver's `Decision`) |
| `GET /sessions/:id/dialyser` | `guardDialysisSessionParam` | — | usage (projected), device, holds (pointer for audience roles only), restriction (live `Decision`, projected), eligibility verdict, allowed dispositions, release state, isolation (DEPENDS ON Phase 1) |
| `POST /dialysers/:deviceId/reprocessing-attempts` | device-subject PHI rows (the history read's shape, §6.4) | `dialysis_reprocessing_attempt` | §5.1 attempts — append-only; releases through §5.6 (DEPENDS ON Phase 1) |
| `POST /sessions/:id/isolation-emergency-override` | `guardDialysisSessionParam` + `requireRole(...DIALYSIS_EMERGENCY_OVERRIDE_ROUTE_ROLES)` | `dialysis_isolation_emergency` | §3.4 emergency exception (an explicit authorised workflow, not a loophole) |
| `POST /sessions/:id/reuse-register` | existing guard | `dialysis_reuse_register` (`retainOnServerError`) | §5.1 reprocessing record (existing route; now claims a key — behaviour change; the policy-on path DEPENDS ON Phase 1, the dark path does not) |
| `GET /sessions/:id/reuse-register` | existing | — | unchanged shape plus the new columns |
| `PATCH /sessions/:id/machine` | `guardDialysisSessionParam` | `dialysis_session_machine` | reassign machine, re-evaluate isolation (DEPENDS ON Phase 1) |
| `GET /machines` | role gate only | — | machine master |
| `POST /machines`, `PATCH /machines/:id` | `requireRole(...DIALYSIS_MACHINE_ADMIN_ROUTE_ROLES)` | `dialysis_machine` | machine master writes |
| `POST /sessions/:id/cancel` | existing guard | — | existing; now runs the un-capture hook inside its transaction (§5.1 Cancel): 409 `RPD_USAGE_NOT_CANCELLABLE` / `RPD_RETURN_REQUIRED` |
| `POST /patients` | existing | — | **unchanged by this lane**: the enrol default (`'unknown'`) and any body guard on the three serology fields are Phase 1's (§3.3) |
| `GET /patients`, `GET /patients/:id`, `GET /today` | existing | — | responses projected per §3.5 (there is no `GET /sessions/:id`; the session read is `GET /sessions` and the today board). None of the three calls the resolver in this lane: a resolver-backed roster / patient-detail surface (the only place `includeMarkers` may be forwarded, behind the audience check) is Phase 1's, and an existing read must keep answering on a build without Phase 1 |
| `POST /machines/ingest` | existing | — | unchanged by this lane; **#1021 closed the tenant predicate on `main`** (`ingestMachineObservations` is tenant-scoped) — the plan's Task 4 preflight is verify-only |

`startSession` and `scheduleSession` (existing routes) evaluate §3.4 and accept `isolation_override_reason`; `scheduleSession` evaluates before its insert (§3.4). Both DEPEND ON Phase 1: until the resolver is on `main` the two routes behave exactly as today.

### 6.2 CSSD (`/api/v1/cssd`, `CSSD_ROUTE_ROLES`; no PHI logger, no patient identity in any payload)

The cath queue at `/devices/*` is unchanged (its ids are cath register ids). The platform register is a sibling sub-tree with the **same** role narrowing (`router.use('/reprocessable-devices', requireRole(...CSSD_DEVICE_ROUTE_ROLES))`) and its own scope, so a client key can never replay one register's stored response against the other:

| Method and path | Scope | Purpose |
|---|---|---|
| `GET /reprocessable-devices?domain=&status=&facility_id=&limit=` | — | queue; rows carry `domain`, `category`, `device_tag`, `manufacturer_serial`, `hospital_asset_id`, `model_name`, `set_code`/`set_status` for OT, cycle, status, `exposure_flag`, `active_hold_types[]`, `quarantine_reason` (a code), `version`, `last_processing_event_id`, `updated_at` — no note, no marker |
| `GET /reprocessable-devices/:id/holds` | — | hold rows (type, reason code, status, placed / released by-role, adjudication); `source_marker_row_id` only for audience roles (§3.5) |
| `GET /reprocessable-devices/:id/label` | — | §6.5 label |
| `POST /reprocessable-devices/:id/receive` | `cssd_reprocessable_device_transition` | awaiting → in_cssd |
| `POST /reprocessable-devices/:id/reprocessed` | same plus the resolved domain's role gate | one domain-aware command (§5.6): dialysis requires `device_usage_id` plus the complete statutory evidence and delegates to `recordDialyserReprocessing` or the append-only attempt command; OT requires `sterilization_load_id` and delegates to the applicable-load command. The controller contains no transition logic |
| `POST /reprocessable-devices/:id/quarantine` | same | `{ reason_code, note, expected_version }` — places a `manual` hold; the code from the vocabulary, the note on the hold |
| `POST /reprocessable-devices/:id/holds/:holdId/release` | `cssd_reprocessable_device_hold_release` | §5.5: `{ adjudication, protocol_id?, evidence?, requires_processing, expected_version }`; roles per hold type (`HOLD_RELEASE_IC_ROUTE_ROLES` / `HOLD_RELEASE_CSSD_ROUTE_ROLES`); releasing the last active hold applies `quarantined → awaiting_reprocessing` in the same transaction; never straight to `available` unless `requires_processing = false` |
| `POST /reprocessable-devices/:id/release` | `cssd_reprocessable_device_transition` | the single-hold convenience: releases every active hold the caller's role may release with one `adjudication`; refused (`RPD_HOLD_RELEASE_FORBIDDEN`) when any active hold is outside the caller's authority |
| `POST /reprocessable-devices/:id/discard` | same | `{ reason, note, expected_version }`; retires the set for OT; a device under a `prion_exposure` hold with `prion_rule = 'discard'` has this as its only exit |
| `POST /issues` | `cssd_set_issue` | existing; now claims a key and accepts `acknowledgement` |
| `POST /issues/:id/cancel` | existing | existing; runs `onIssueCancelledTx` inside `transitionIssue` (§5.2); body accepts `pack_opened: true`; 409 `RPD_RETURN_REQUIRED` / `RPD_USAGE_NOT_CANCELLABLE` |
| `POST /loads` | existing | `createSterilizationLoad` now runs `applyLoadOutcomeTx` for a load created already `passed` / `failed` (§5.2); response gains `affected_devices[]` |
| `PATCH /loads/:id/status` | existing | response gains `affected_devices[]` and, on `failed`, `affected_usages[]` (the failure response, §5.2) |

`retainOnServerError` stays unset on the transitions for 765's reason (every `from` list excludes its own `to`); it is set on `POST /issues` because `issueSet` is not self-blocking on a set whose issue row was written but whose response was lost.

### 6.3 Theatre (`/api/v1/theatre`, `THEATRE_ROUTE_ROLES`, `phiAccessLogger('OPERATING_THEATRE')`)

`GET /theatre/:id/reprocessable-sets` (`guardTheatreCase`): the case's set issues joined to their register rows and load evidence (`load_code`, indicator results, `released_at`), plus `reuse_restriction` (projected) — the WHO sign-in strip's data. THEATRE_ROUTE_ROLES ⊂ CLINICAL_STAFF_ROUTE_ROLES, so the projection is a no-op today; it is applied anyway so a later widening cannot leak.

### 6.4 Reprocessing governance (`/api/v1/reprocessing`, new mount)

Mounted in `app.js` beside `/api/v1/cath-reprocessing` with `requireRole(...REPROCESSING_POLICY_ROUTE_ROLES)` where `REPROCESSING_POLICY_ROUTE_ROLES` is an alias of `CATH_REPROCESSING_POLICY_ROUTE_ROLES` (`QUALITY_OFFICER`, `INFECTION_CONTROL_OFFICER`, `ADMIN`, `SUPER_ADMIN`) — the same constant, aliased, not a copy that can drift. Clinical-mount posture: no step-up, IP allowlist or admin rate limiter (the cath spec §9.5 reasoning). The admin `/api/proxy` allowlist gains `api/v1/reprocessing`.

| Method and path | Scope | Purpose |
|---|---|---|
| `GET` / `PUT /domains/:domain/settings` | `reprocessing_domain_policy` | §4.1 |
| `GET` / `PUT /domains/:domain/policies` | same | §4.2, whole set per domain; a `reprocessable = true` entry with no active protocol is refused (`RPD_PROTOCOL_REQUIRED`) |
| `GET` / `PUT /domains/:domain/protocols` | same | §4.2a; `PUT` under `ISOLATION_GROUP_ADMIN_ROUTE_ROLES` (`RPD_PROTOCOL_INVALID` on the locked matrix cells) |
| `POST /exposure-outbox/drain` | `reprocessing_exposure_drain` | §5.7 on-demand reconciler; returns `{ drained, pending, failed }` |
| `GET /devices/:deviceId/history` | — | uses (case anchor, `patient_uid`, `captured_at`, cycle, disposition) and transitions. PHI with no single subject: writes one `hipaa_access_log` row per distinct patient in batches of 25 (`record_type = 'DIALYSIS'` or `'OPERATING_THEATRE'` by domain, `request_id` `"<incoming id, truncated> rpd_device:<id>"`) before responding — the 765 shape |

`:domain` is validated against `DOMAINS` by a param guard before any key is claimed (400 `RPD_DOMAIN_INVALID`).

### 6.5 Label endpoint (designed for reuse)

`GET /cssd/reprocessable-devices/:id/label` answers the same shape as the #1015 endpoint `GET /cssd/devices/:id/label` (`getDeviceLabel`) and the existing set-label endpoint (`getInstrumentSetLabel`): `{ device_id, device_tag, external_ref, domain, item_name, cycle_count, barcode, barcode_symbology: 'code39', svg, generated_at }` with `barcode = device_tag` and `svg = code39Svg(device_tag, { module: 2, height: 44 })`, stamps `metadata.label_printed_at/by` on the register row and audits `rpd.device.label_printed`. The OpenAPI schema is `ReprocessableDeviceLabel`; the Admin print path renders both registers.

### 6.6 OpenAPI

New overlay `apps/backend/scripts/openapi/schemas/reprocessableDevices.mjs` registered in `SCHEMA_MODULES`: `ReprocessableDevice`, `ReprocessableDeviceUsage`, `ReprocessableDeviceLabel`, `ReprocessingDomainSettings`, `ReprocessingDomainPolicy`, `DialysisMachine`, `DialysisSessionDialyser`, `DialysisIsolation`, `DialysisIsolationDecision` (the Phase 1 `Decision` shape, §3.3), `TheatreReprocessableSet`, `DispositionOptions`, request bodies and envelopes, and every operation in §6.1–§6.5 with error codes in descriptions. `bloodborneMarkers.mjs` is not changed by this lane (§4.10). `openapi:generate`, `openapi:check`, `openapi:sync-core`, `openapi:check-core` and `openapi:lint-budget` (zero new findings) run after every route change. The stale generated Dart client noted by the scouting brief (`/sessions/{id}/reuse-register` missing) is repaired by the same regeneration.

**The response validator is named (review 2026-09-06, additional conditions).** `additionalProperties: false` in an overlay is a validation rule, and nothing at runtime validates a response against it. The validator this lane relies on is **`apps/backend/src/tests/helpers/assertSchema.js`** — `assertResponse(method, path, body, status)` and `assertData(schemaName, data)`, an ajv (`strict: false`, `allErrors`) compiled over the generated `src/docs/openapi.json` — extended by this lane with `assertErrorBody(code, body)` for the error envelope (`RpdError`: `{ success: false, code, message, details }`, with `details` declared per code and `additionalProperties: false`). It is exercised in three places: (a) `reprocessableDevicesOpenApiContract.test.js` validates a success fixture for **every** operation in the overlay **and** every documented error body, including the nested `isolation_warnings[]` items, the projected `reuse_screen` / `post_use_screen`, `holds[]`, `receipt` and `affected_devices[]`; (b) the canary harness passes every 2xx and 4xx body it walks through `assertResponse` (a test-only `openapiResponseGuard` in `tests/helpers`), so an unprojected or over-wide body fails the schema before the sentinel is even consulted; (c) both deep suites call `assertData` on the objects they read back. There is deliberately no production response-validation middleware: the cost is a schema compile per route and the failure mode (a 500 on a live clinical read) is worse than the defect it would catch; the tests are the enforcement, and they are named here so nobody mistakes the overlay for one.

### 6.7 Admin (Next.js)

- `dashboard/cssd` Devices tab: a **domain filter** (`Cath | Dialysis | OT`, default Cath so the existing view is unchanged); cath rows keep calling `lib/api/cathDevices.ts`, the other two call the new `lib/api/reprocessableDevices.ts`; a unified row type; `ReprocessableDeviceActions.tsx` mirrors `DeviceActions.tsx` with the platform scope, plus a **Print label** action; the `in_case` and `discarded` rows offer nothing, as for cath.
- `dashboard/quality/reprocessing` (new page under the `quality` segment, nav entry "Reprocessing policies"): a domain switch and, per domain, the settings form (three serology fields, plus isolation enforcement and the **class → group mapping** (`isolation_groups`) for dialysis — four rows, one per marker class, values free-text bay labels with a datalist of the groups already on registered machines, rendered read-only for `QUALITY_OFFICER` and editable only by `ISOLATION_GROUP_ADMIN_ROUTE_ROLES` (§4.1), with the server's "this group is on no machine" warning shown inline; `serology_validity_days` on the dialysis row carries the note that the Phase 1 rule applies no window, §4.1) and the per-category policy rows (`tcv_min_pct` shown for dialysis only). Layout and class names copied from `ReprocessingPolicyTab.tsx`; `setQueryData` before `invalidateQueries` (the Plan 3 save-revert lesson).
- `dashboard/dialysis`: `SessionTab`'s `ReuseRegisterModal` moves from `fetchAdminAPI` to `core.ts` with a key (scope `dialysis-reuse-register`), shows the derived cycle count read-only when a device is captured, adds TCV / agent / contact minutes / concentration; a **Dialyser** panel (capture by serial with the dedication refusal shown verbatim, restriction strip, isolation warnings); a **Machines** tab (master with the routing group `isolation_group`, a free-text label with a datalist of the groups already in use); `RosterTab`: the enrol form is Phase 1's to change (this lane leaves it), and the roster's serology chips render `—` for a projected `null` (the amber `unknown` chip from the resolver is Phase 1's surface); `TodayBoardTab` shows an isolation-warning badge from `isolation_warning_codes`.
- `dashboard/quality/reprocessing` also carries the **protocol** form per domain (§4.2a): basis, reference, approver, TCV floor (min 80), baseline / mid-life rule, residual and integrity requirements, agents with bounds, the reuse matrix with `hbsag` / `hiv` / `isolation_mixed` rendered locked at `no_reuse` and `hcv` as `no_reuse | dedicated_reuse`, surveillance intervals, prion rule; retiring a protocol shows the categories it deactivates. The approved bay vocabulary (`approved_isolation_groups`) is edited here with the deny-list refusal rendered verbatim.
- `dashboard/cssd` Devices tab also gets a **Holds** dialog per device (type, reason code, status, adjudication for the release form — the marker pointer is not rendered in the console even for audience roles), the release action gated per hold type, the row's `version` sent as `expected_version` on every action, and `RPD_VERSION_CONFLICT` rendered as "this device changed — reload"; the loads screen renders `affected_devices[]` / `affected_usages[]` after an outcome.
- `dashboard/dialysis`: the reuse-register modal renders the release verdict (`released`, or `not_established` with the missing list) and an **Attempts** panel per dialyser for the append-only record; the capture form carries the residual-test field when the read says `residual_test_pending`.
- `dashboard/theatre`: nothing (the OT surface is Staff).
- `src/__tests__/dashboard/cssd/router-coverage.test.ts` scans the new API module too, so every new CSSD route has a caller or a named exemption.

### 6.8 Staff (Flutter)

- **Dialysis is a new feature from zero** (`apps/staff/lib/features/dialysis/`). Prerequisite in the backend tree: a `dialysis` entry in `rolePolicyGraph.js`'s sidebar feature list (`capability_group: 'specialty_services'`) granted to the roles that run a unit, and `scripts/generate-staff-role-contract.mjs` re-run so `staff_role_contract.g.dart` carries it; then `role_config.dart` (`_dialysis` feature, route `/dialysis`), `app_router.dart`, `staff_route_policy.dart`. Screens: `DialysisTodayScreen` (`GET /dialysis/today`; per session: patient, station, machine, status, isolation badge, dialyser state); `DialyserCaptureSheet` (serial or tag entry, model, baseline TCV, the residual-test field when the read says `residual_test_pending`, the hold banner when the device is held, the restriction strip reused from cath's `CathReuseRestrictionStrip` generalised to `ReuseRestrictionStrip` in `lib/core/widgets/`, the dedication refusal rendered from `DIALYSER_DEDICATED_TO_ANOTHER_PATIENT` as a named error, the exposure acknowledgement dialog when the server demands it); `IsolationWarningStrip` (codes, override reason field at start); `DialyserReprocessingSheet` (integrity, TCV measured with the live `%` of baseline, agent, contact minutes, concentration, the allowed dispositions rendered from `allowed_dispositions`, acknowledgement when required, and the release verdict — `not_established` renders the missing list). Writes go through `IdempotencyAttemptRegistry` scopes `dialysis-dialyser-capture:<session>` and `dialysis-reuse-register:<session>`.
- **OT**: `theatre_screen.dart`'s schedule card gains a **Sets** expansion (`GET /theatre/:id/reprocessable-sets`): each set with set code, last load code and indicator results, cycle, exposure; the `ReuseRestrictionStrip` for the case patient; an **Issue set** action (barcode scan or typed `set_code` → `POST /cssd/issues` with a key, acknowledgement dialog on `CSSD_SET_ACKNOWLEDGEMENT_REQUIRED`). `OT_NURSE` / `OT_STAFF` / `OT_INCHARGE` are on `CSSD_DEVICE_ROUTE_ROLES`, so the issue call is reachable from the theatre app.
- **Five-locale string budget**: **75** new keys × 5 locales = 375 lines in `app_strings.dart` (300 non-English lines for OPEN-21). Review 2026-09-06 added ten: the capture sheet's residual-test field, its "required" refusal and the hold banner (3); the reprocessing sheet's `not_established` verdict, its missing-evidence label, and the baseline-required, use-not-ended and not-eligible refusals (5); the `DIALYSIS_ISOLATION_GROUP_UNMAPPED` code (1); the theatre panel's "on hold" label (1). The plan's Task 7 key list is authoritative; this breakdown must sum to it: restriction strip 3 (restricted, unknown, `more_reasons` — aliased from cath's keys), today screen 8, policy-disabled banner 1, capture sheet 17, device card 3, isolation strip 8 (**four** codes, `required_group` — the live key under D11 = NO — override label, override-required hint, blocked), reprocessing sheet 23, theatre sets panel 10, feature/nav 2. Every non-English rendering carries `// REVIEW:` per the OPEN-21 convention. Keys under `s4.lib.dialysis.*`, `s4.dynamic.dialysis.*`, `s4.lib.theatre.sets.*`, `s4.dynamic.theatre.sets.*`, `s4.lib.reuse.*`, `s4.dynamic.reuse.*`, `role.feature.dialysis`, `role.nav.dialysis`. There is no `DIALYSIS_SEROLOGY_UNKNOWN` string (§3.4) and no `required_class` string (D11). Refusal messages stay English-only, as in Plans 2–3 (`ApiResponse.code` is not mapped through the tables; §10). The un-capture and the hold release add no Staff strings: neither Staff surface cancels a session or an issue, and holds are released from the console.

## 7. Error codes, audit events, rollout

### 7.1 Error codes

| Code | HTTP | When |
|---|---|---|
| `RPD_DOMAIN_INVALID` | 400 | `:domain` outside `dialysis` / `ot` |
| `RPD_DOMAIN_MISMATCH` | 409 | a tag resolves to a device of another domain |
| `RPD_TAG_INVALID` | 400 | tag does not match `RD[0-9]{8,19}` |
| `RPD_DEVICE_NOT_FOUND` | 404 | tag or id unknown in tenant |
| `RPD_DEVICE_NOT_AVAILABLE` | 409 | status ≠ available at capture/issue; carries the status |
| `RPD_INVALID_TRANSITION` | 409 | any action outside 765's table; names the state |
| `RPD_POLICY_NOT_REPROCESSABLE` | 409 | no reprocessable policy for the category |
| `RPD_MAX_CYCLES_REACHED` | 409 | reprocess at the ceiling (the discard branch) or a mint above it — **never a capture** (§2, `max_cycles` semantics) |
| `RPD_HOLD_ACTIVE` | 409 | capture / issue / release / reprocess / attempt on a device with an active hold; carries `hold_types[]` — **replaces the earlier `RPD_EXPOSURE_BLOCKED`**: nothing refuses on the historical flag (§5.5) |
| `RPD_HOLD_NOT_FOUND` / `RPD_HOLD_RELEASE_FORBIDDEN` | 404 / 403 | hold release: unknown hold; the caller's role may not release this hold type (§5.5) |
| `RPD_PRION_HOLD_DISCARD_ONLY` | 409 | a `prion_exposure` hold released without a protocol whose `prion_rule = 'ic_pathway'` |
| `RPD_REUSE_NOT_ELIGIBLE` | 409 | the protocol matrix / surveillance / hold verdict at capture; carries `verdict` (a code), never a class (§5.3) |
| `RPD_BASELINE_TCV_REQUIRED` / `RPD_RESIDUAL_TEST_REQUIRED` | 409 | the release criteria (§5.1) — the first is also named inside a `not_established` record's `missing_evidence`; the second is a capture-time refusal |
| `RPD_USE_NOT_ENDED` | 409 | a dialysis reprocessing record on a session that is not `completed` / aborted (§5.1) |
| `RPD_UNOPENED_CONFIRMATION_REQUIRED` | 400 | an un-capture with no affirmative unopened condition (§5.1 Cancel, §5.2) |
| `RPD_ATTEMPT_NOT_ALLOWED` | 409 | an attempt on a device that is not awaiting, has an open usage or an active hold |
| `RPD_PROTOCOL_REQUIRED` / `RPD_PROTOCOL_INVALID` | 409 / 400 | a reprocessable policy with no active protocol; a protocol body outside §4.2a (incl. any value but `no_reuse` on the three locked matrix cells) |
| `RPD_POLICY_DEACTIVATED` | 409 | enrolment, capture or release under a deactivated policy (§4.2) |
| `RPD_LOAD_REQUIRED` / `RPD_LOAD_NOT_APPLICABLE` / `RPD_LOAD_SET_NOT_IN_LOAD` | 400 / 409 / 409 | the OT arm of the generic release command (§5.6) |
| `RPD_DIALYSIS_USAGE_REQUIRED` | 400 | the dialysis arm of the generic release command lacks the usage that binds the statutory record or append-only attempt (§5.6) |
| `RPD_VERSION_CONFLICT` | 409 | `expected_version` ≠ `version`; carries `current_version` (§5.8) |
| `RPD_ISOLATION_BLOCK_PRECONDITIONS` | 409 | the `block` switch, or a machine / mapping edit that would break the validated mapping while `block` is on (§3.4) |
| `RPD_ISOLATION_GROUP_LABEL_CLINICAL` / `RPD_ISOLATION_GROUP_NOT_APPROVED` | 400 / 409 | a bay label on the deny-list; a machine label outside the approved vocabulary (§4.1) |
| `DIALYSIS_ISOLATION_EMERGENCY_FORBIDDEN` | 403 | the emergency override from a role outside `DIALYSIS_EMERGENCY_OVERRIDE_ROUTE_ROLES` (§3.4) |
| `RPD_ACKNOWLEDGEMENT_REQUIRED` | 400 | rule permits but reason missing |
| `RPD_SEROLOGY_REQUIRED` | 409 | `unknown` under `block_return` at a reprocess request |
| `RPD_DISPOSITION_NOT_ALLOWED` | 409 | requested disposition outside the computed set; carries `allowed` |
| `RPD_CYCLE_TYPE_INVALID` / `RPD_CYCLE_TYPE_NOT_ALLOWED` | 400 / 409 | outside the six / outside the policy (carries the allowed list) |
| `RPD_EXTERNAL_REF_TAKEN` | 409 | serial or asset id already registered in the domain |
| `RPD_LOAD_NOT_FOUND` / `RPD_LOAD_NOT_PASSED` | 404 / 409 | `sterilization_load_id` on a manual reprocessed call |
| `RPD_USAGE_NOT_CANCELLABLE` | 409 | cancel of a session / issue whose device use was recorded (usage closed, or a 418 row references the usage); the cancel fails (§5.1 Cancel, §5.2) |
| `RPD_RETURN_REQUIRED` | 409 | cancel of an `in_progress` session, or of an issue with `theatre_use_started_at` set or `pack_opened: true` declared — return / reprocess first; the cancel fails |
| `DIALYSER_ALREADY_CAPTURED` | 409 | the session already has an open usage |
| `DIALYSER_DEDICATED_TO_ANOTHER_PATIENT` | 409 | §5.1 step 3; no override |
| `DIALYSER_TCV_INVALID` | 400 | non-positive or non-numeric TCV |
| `DIALYZER_REUSE_CYCLE_DERIVED` | 409 | typed cycle count disagrees with the device |
| `DIALYZER_REUSE_REGISTER_SETTLED` | 409 | a second, different write after the usage returned |
| `DIALYSIS_ISOLATION_OVERRIDE_REQUIRED` | 400 | warnings present, no reason; or a reason with no named actor (`dialysis_sessions_isolation_override_check` makes reason / by / at all-or-nothing) |
| `DIALYSIS_ISOLATION_MACHINE_BLOCKED` | 409 | `UNREGISTERED`, `MISMATCH` or `UNMAPPED` under `block` (§3.4) |
| `DIALYSIS_MACHINE_NOT_FOUND` / `DIALYSIS_MACHINE_NO_TAKEN` | 404 / 409 | machine master |
| `RPD_ISOLATION_GROUPS_FORBIDDEN` | 403 | a settings body carries `isolation_groups` from a role on the governance mount but outside `ISOLATION_GROUP_ADMIN_ROUTE_ROLES` (§4.1); the rest of the body is unaffected |
| `RPD_ISOLATION_GROUPS_INVALID` | 400 | a mapping key outside the four marker classes, or a value that is not non-empty text of at most 40 characters (§4.1) |
| `CSSD_SET_QUARANTINED` / `CSSD_SET_EXPOSURE_BLOCKED` / `CSSD_SET_ACKNOWLEDGEMENT_REQUIRED` | 409 / 409 / 400 | issue-time refusals |
| `RPD_ISOLATION_RESOLVER_UNAVAILABLE` | 503 | the Phase 1 resolver is not importable (a dialysis policy is on but Phase 1 is not on this build) — fail closed, never fall back to a stub or to the columns (§3.3) |
| `RPD_ISOLATION_DECISION_INVALID` | 500 | the resolver answered with no `Decision` for a uid, a `status` outside the three values, or — **when the caller asked for the class** — an absent or out-of-range `isolation_class`; a wrong shape must not route a patient (§3.3). With `includeIsolationClass` off, a `Decision` with no class is valid |

(`DIALYSIS_SEROLOGY_FIELDS_NOT_ALLOWED` and any `BLOODBORNE_MARKER_INVALID` change moved to Phase 1 with the enrol guard and the surveillance source, §3.3.)

Every code reaches the envelope root via `relayAppError`. Concurrency: device rows are read `FOR UPDATE` in every transition; nothing uses the bare Prisma client (plain `prisma` no longer bypasses RLS).

### 7.2 Audit events (`audit_logs`, the CSSD column shape)

`rpd.device.minted`, `rpd.device.captured`, `rpd.device.uncaptured` (metadata carries `cancel_target` and the owner id), `rpd.device.returned`, `rpd.device.received`, `rpd.device.reprocessed`, `rpd.device.quarantined`, `rpd.device.released`, `rpd.device.discarded`, `rpd.device.exposure_flagged`, `rpd.device.label_printed`, `rpd.settings.updated`, `rpd.policy.updated`, `dialysis.machine.created`, `dialysis.machine.updated`, `dialysis.session.isolation_evaluated` (metadata carries `required_group`, `machine_no`, `enforcement`, the persisted `codes` and the `Decision`'s `status`, `evidence` and `asOf` — **never** `reasons`, markers or `isolation_class`: under D11 = NO the audit row names the **group**, so the marker class is written nowhere at all, `audit_logs` included), `dialysis.session.isolation_overridden`, `dialysis.session.isolation_emergency_override`, `dialysis.reuse_register.recorded`, `dialysis.reuse_register.release_not_established`, `cssd.set.issued` (existing; metadata gains `device_id`, `usage_id`), and — review 2026-09-06 — `rpd.hold.placed`, `rpd.hold.released`, `rpd.hold.satisfied`, `rpd.processing_event.recorded`, `rpd.load.failure_response`, `rpd.attempt.recorded`, `rpd.protocol.updated`, `rpd.exposure_outbox.delivered` / `.replayed` / `.failed`. No `dialysis.patient.*` event: this lane writes nothing to the roster (§3.3). Every transition's metadata carries `idempotency_key`, `device_tag`, `domain`, `from`, `to`, `version_after` and the reason **codes**; free text and marker names appear in no audit row of this lane — a deep assertion greps every `audit_logs.metadata` row the suites produce for marker words (§8), and the canary walks audit-derived responses (§3.6).

### 7.3 Rollout defaults

- Dark by default: no `reprocessing_domain_policies` rows → nothing is minted, the CSSD hooks do nothing, the dialysis reuse-register route runs its legacy path. A tenant activates a domain by creating policy rows **under an approved protocol** (§4.2a) — stage 3 of §1.2; the quality or infection-control officer owns that step. **Deactivation** (a policy switched off after devices exist) stops enrolment and release and keeps tracking and settlement (§4.2).
- Settings defaults when no row: dialysis `discard` / `warn` / 90 / `warn`; OT `quarantine` / `warn` / 90 (the dialysis 90 has no consumer while Phase 1's rule is window-less, §4.1); `isolation_groups` defaults to `{}` on both, so an unmapped unit routes by "any isolation machine" (§3.4).
- Isolation is `warn` until a tenant flips it; unregistered machines warn only for restricted patients; `unknown` patients raise nothing per session (§3.4).
- **Phase 1 dependency.** Every dialysis isolation and post-use decision reads the Phase 1 resolver; this lane's dialysis arm is gated on Phase 1 being on `main` (§3.3), and with a dialysis policy on but no resolver importable the capture, reprocessing, schedule and start paths fail closed with `RPD_ISOLATION_RESOLVER_UNAVAILABLE` (§7.1) rather than fall back to the columns. The OT arm has no such dependency. Nothing here writes the roster; there is no sync and no backfill in this lane. The census script (§7.6) is the Phase 2 gate for the future DROP, not a prerequisite for this lane.
- **Q1 as revised (owner, 2026-09-06; Phase 1's rule, §3.3):** a legacy `'positive'` restricts on its own; a legacy `'negative'` is **not evidence**, so a roster row with only the enrolment default and no marker or `dialysis_serology` row resolves `unknown`. Day-one amber is therefore **higher** than the literal Q1 predicted, and under `unknown_serology_rule = 'warn'` each such patient's first dialyser reprocessing needs an acknowledgement; this document states the fact and makes **no blanket acknowledgement-cost claim either way**. `serology_validity_days` remains inert for dialysis under the window-less Phase 1 rule (§4.1).
- **Release CI fails when activation is possible and the resolver is missing (review 2026-09-06, additional conditions).** `scripts/ci/check-dialysis-activation-gate.mjs` runs in `ci.yml`'s check chain: if `apps/backend/src/services/clinical/dialysisReuseService.js` exists (the dialysis arm is on the build, so a tenant *could* create a dialyser policy) and `dialysisIsolationResolver.js` does not, the job **fails** — it never passes with the dialysis suite skipped. In CI the dialysis deep suite runs under `RPD_REQUIRE_PHASE1=1`, under which a missing resolver is a failing test, not a `describe.skip`. And at boot, `dialysisActivationGuard.js` (the `exposureHandlerBootstrap` pattern from #1017) refuses to start an API whose database holds any `reprocessing_domain_policies` row with `domain = 'dialysis' AND reprocessable = true` while the resolver is unimportable — a deployed build cannot be one migration away from a fail-open.
- No data migration of 418 rows: legacy rows keep `device_id NULL`.
- Existing instrument sets get register rows on their next issue after a policy exists, at `cycle_count = 0` with `enrolled_from_existing_set` — the count starts from enrolment, which is stated on the row.

### 7.4 Prefix-mount lockout class

Every new role gate is an intersection with its mount's audience and is asserted as a strict subset in `reprocessableDeviceRouteWiring.test.js`: `CSSD_DEVICE_ROUTE_ROLES ⊂ CSSD_ROUTE_ROLES` (existing), `DIALYSIS_MACHINE_ADMIN_ROUTE_ROLES ⊂ DIALYSIS_ROUTE_ROLES`, and `REPROCESSING_POLICY_ROUTE_ROLES` applied at the mount level (not under `/api/v1/admin`). `app.js`'s source is pinned in the same test so the harness cannot mount a different audience than production.

### 7.5 CI tier routing

Backend, Staff and Admin changes land in their owning trees (`apps/backend`, `apps/staff`, `apps/admin`); the role-contract regeneration for Staff writes a file under `apps/staff` from a script in the repo root and is committed with the Staff task. The last commit before hand-back carries `[full-ci]`.

### 7.6 Operator scripts

**`apps/backend/scripts/dialysis-isolation-census.mjs [--tenant <uuid> | --all-tenants] [--json] [--compare-drop] [--integrity]`** — the **Phase 2 gate** (§3.3), widened by review 2026-09-06 (point 10). Read-only. The count modes need no Phase 1 code; `--compare-drop` needs the resolver. For each tenant it prints:

| Field | Meaning |
|---|---|
| `orphan_patient_uid_rows`, `orphan_patient_uids` | `dialysis_patients` rows whose `(tenant_id, patient_uid)` has no `users` row (168 declares no FK; 764's `fk_patient_bloodborne_markers_patient` requires one, so these rows can never hold a marker; Q3 in §9 is the declaration path for the real carriers among them) |
| `unbackfilled_positive_patients`, `unbackfilled_positive_columns`, `unbackfilled_by_marker` | patients with a legacy `'positive'` in any roster row (`bool_or` over every row of the patient) and no non-voided reactive marker for that marker |
| `declared_and_corroborated_negative_patients`, `declared_and_corroborated_negative_by_marker` | per analyte: a negative-shaped roster value accompanied by a non-reactive `dialysis_serology` row. The name is the operator-facing category requested by the owner; semantically the row is the evidence and the legacy negative is inert. These patients resolve `clear` for that analyte from the evidence row and lose nothing at the DROP |
| `defaulted_negative_patients`, `defaulted_negative_by_marker` | a `'negative'` column, **no** serology row, **no** marker for the analyte — the enrolment default. These resolve `unknown` today and after the DROP alike; the count is the day-one amber volume, reported for the unit's planning and never a gate |
| `column_only_declared_negatives` | **must be zero by construction** — today's schema cannot tell a declared `'negative'` from the default inside the column, so nothing may be counted here; the script exits 3 and prints the offending query if it is not zero. A large number here is a **bug in the census**, not a finding |
| `retrospective_captures` | usages with `capture_provenance = 'retrospective'` (§5.1) |
| `decision_bearing_declarations` | every legacy declaration still deciding anything under the asymmetric rule = the un-backfilled positives. The companion declared-and-corroborated negative count is retained for reconciliation, but its serology rows already carry the deciding evidence and survive the DROP. Phase 1 must preserve positives as `clinical_declaration` markers under a named actor with `tested_on = NULL` |
| `--compare-drop` (DEPENDS ON Phase 1) | for **every** roster patient, the resolver's `status` **and** the routing `required_group` (through the tenant's map) computed twice — with the columns as they are, and with the three columns simulated as `NULL` — and every patient whose status **or** group differs listed by uid with both values. The DROP gate is **zero differences** |
| `--integrity` | relationship checks the constraints cannot express: a `current_usage_id` pointing at a closed usage; a dialysis usage whose `patient_uid` differs from its session's roster patient; a usage whose `ready_processing_event_id` post-dates its `captured_at`; an `available` device with an active hold; a `released` 418 row with no processing event. All must be zero; any non-zero is a defect, printed with ids |
| `roster_rows`, `active_roster_rows` | denominators |

**What the census gates.** The **DROP** of the three columns (a future migration outside this lane) requires: orphans 0, un-backfilled positives 0, `column_only_declared_negatives` 0, `--compare-drop` differences 0, in every production tenant. It gates **nothing about the additive migration `NNN`**, which touches neither the columns nor the resolver. The two-count census of the earlier draft is not authority for the DROP on its own; the comparison is.

**`apps/backend/scripts/role-assignment-census.mjs [--all-tenants] [--json]`** — the D10 **actual-assignment check** (§2). For each of `DIALYSIS_TECHNICIAN`, `BLOOD_BANK_STAFF`, `BLOOD_BANK_TECHNICIAN` it counts active `users` rows per tenant and prints whether the role is in `NON_ASSIGNABLE_HUMAN_ROLES` today; the PR body quotes the counts beside D10a–c. Non-assignability is a provisioning restriction, not proof that nobody holds the role.

**`scripts/reconcile-bloodborne-markers.mjs`** (#1017, existing) gains the outbox drain phase (§5.7) and prints `exposure_outbox: pending / failed / drained` before any repair.


## 8. Tests

**Unit** (`src/tests/unit`):

- `reprocessableDeviceRules.test.js`: `deviceTransition` for every allowed and refused pair including `in_case --uncapture--> available`, `available --uncapture-->` refused and `in_case --quarantine-->` refused; `reuseEligibility` for every verdict code — the matrix's three locked cells, HCV under `no_reuse` and `dedicated_reuse` (dedicated and not), `unknown` under `warn` / `block_return`, `surveillance_overdue` on and off, an active hold of each type, the ceiling at 1 / 2 / `null`, a `restricted` Decision with no class read as mixed; `computeDispositionOptions` for every domain × rule × verdict combination including the `quarantine` branch, the `serology_required` and `post_issue_restriction` dispositions and the holds-before-ceiling order; the **parity test** against `computePostUseOptions` **over the shared input domain only** (§5.3), with explicit expectations outside it; `computeIsolationWarnings` for every row of §3.4's table under `warn` and `block` — `UNREGISTERED` warn/block, `MISMATCH`, `UNMAPPED` warn-satisfies/block-refuses, the `unknown` silence, `clear` on an isolation machine, two classes on one label, a `NULL`-group machine never satisfying; `isolationGroupLabelValid` (deny-list: `"HIV bay"`, `"Hep B side"`, `"positive room"` refused; `"Bay 1"`, `"Red side"`, `"Isolation 2"` accepted; case-insensitive; token-split so `"Ship"` is not `hip`); `releaseCriteria` (§5.1) for every missing item → `not_established` with the item named, below threshold → `tcv_below_threshold`, integrity fail → `integrity_test_failed`, an agent outside the protocol list / a concentration out of bounds / a short contact time → `not_established`, **exactly 80 % passes and 79.9 % does not; a missing baseline is `not_established`, never a pass** (the reversal of the earlier `tcvVerdict`); `max_cycles` semantics: a table-driven walk of the full lifecycle at `max_cycles` 1, 2 and unlimited asserting the number of permitted uses is `max + 1`, the capture at the ceiling is admitted, the reprocess at the ceiling discards, `mintDeviceTx`'s bound admits `= max` and refuses `> max`, and every ceiling comparison in the module is behind `ceiling !== null`; `normalizeDeviceTag`; `validatePolicyInput` per domain (dialysis refuses `steam`, OT refuses `function_check_required`, `tcv_min_pct` below 80 refused, `procedure_pack` reprocessable refused, reprocessable without `protocol_id` refused); `validateProtocolInput` (the three locked cells, `hcv`'s two values, the agents shape, `nominal_tcv_ml` required under `manufacturer_nominal`, the TCV floor); `exposureStamp` (`{ exposure_flag }` only — the module exports no marker array and no class → marker map, and `JSON.stringify(module)` matches no marker word).
- `reprocessableDeviceProjection.test.js`: roster row, serology row, restriction (both shapes), **and the usage row** (`projectUsageForRole`: for a non-audience role both screens lose `markers`, get `isolation_class: null` and an **empty `reasons`**; an audience role reads them unchanged), for one audience role and one non-audience role each; keys blanked, never dropped; the hold-row projection (`source_marker_row_id` dropped for non-audience roles).
- `dialysisIsolationAdapter.test.js`: against the named stub (`__stubDialysisIsolationResolver`), the adapter calls the resolver with the tx client it was given, passes `includeMarkers` and `includeIsolationClass` only when told, returns a snapshot with neither a `markers` nor an `isolation_class` key (no `keepClass` form), throws `RPD_ISOLATION_RESOLVER_UNAVAILABLE` when the Phase 1 module is absent, and pins **`assertDecisionShape` across all three statuses under both flag settings** (§3.3): flag on — `restricted` with a valid class passes, `restricted` with `null` / absent fails, `clear` / `unknown` with `null` pass, `clear` / `unknown` with a class fail; flag off — absent passes, present `null` passes, present class fails; a status outside the three fails under either.
- `reprocessableDeviceRouteWiring.test.js`: the census pattern of `cathDeviceReuseRouteWiring.test.js` — every command claims a key with the right scope, reads do not, guard-before-claim ordering on every guarded route, the subset assertions of §7.4 **including `HOLD_RELEASE_IC_ROUTE_ROLES`, `HOLD_RELEASE_CSSD_ROUTE_ROLES` and `DIALYSIS_EMERGENCY_OVERRIDE_ROUTE_ROLES`**, `app.js` source pinned for the new mount, every role named is real, and every queue command declares `expected_version` in its request schema.
- `dialysisSerologyWriters.test.js`: textual pin that **no module this lane creates** names `hbsag_status` / `hcv_status` / `hiv_status` in an `UPDATE` or `INSERT` — the columns are Phase 1's (§3.3).
- `cssdLoadOutcomeCallSites.test.js` (replaces the enumerated load half of the earlier pin): **discovers** every load-outcome write in `cssdService.js` by regex, asserts each sits in a function that calls `applyLoadOutcomeTx`, and asserts the discovered population equals the pinned count (today two functions / seven statements) — a shrink or a growth fails and must be raised deliberately. `cssdReuseHookCallSites.test.js` keeps the issue / return / cancel pins (`onSetIssuedTx` after the INSERT, `onSetReturnedTx` in the returned branch, `onIssueCancelledTx` before the shared UPDATE) and the `set_issue_log` write-statement count.
- `dialysisReuseHookCallSites.test.js`: `cancelSession` calls `onSessionCancelledTx` before its status UPDATE and inside `setTenantTx`; `scheduleSession` calls `assessIsolationTx` before its INSERT and inside `setTenantTx`; `startSession` evaluates inside its transaction; no dialysis decision path reads a frozen `reuse_screen` / `post_use_screen`; **the `includeIsolationClass: true` asker count is exactly two by function name — `assessIsolationTx` and `reuseEligibilityTx` — and no other function names the flag**; `recordDialyserReprocessing` checks the session's end-of-use state before it reads the device.
- `releaseToAvailableCallSites.test.js`: no statement in the lane's modules writes `status = 'available'` outside `applyDeviceTransitionTx`, and every `reprocessed` / `uncapture` call of it is inside `releaseToAvailableTx` (pinned population, §5.6).
- `lockOrder.test.js`: every `FOR UPDATE` in the lane's modules names a table at a rank not lower than the previous one in the same function (`LOCK_ORDER`, §5.8).
- `exposureOutboxService.test.js`: `enqueueExposureEventsTx` inserts one row per reactive event and is a no-op on replay (the unique); `markExposureDeliveredTx` marks `delivered` only when every registered handler has acknowledged; the backoff schedule is 588's; a claimed row past its lease is reaped back to `pending`.
- `reprocessableDevicesOpenApiContract.test.js`: `assertResponse` over a success fixture for every operation and `assertErrorBody` over every documented error code, incl. nested warnings, projected screens, holds, receipts and `affected_devices[]` (§6.6).
- `serologyDisclosureCanary.test.js`: **four** mounts added, the predicate widened to device fields / snapshots / error bodies / audit-derived responses, the D11 value sentinel over the fixed `D11_FIXTURE_ROLES` population, the liveness assertions, snapshot regenerated and read (§3.6).
- `prismaCoverage.test.js`: pins for the eleven tables (nine mutable-no-delete, two append-only) and the sequences.

**Deep** (own tenant `…d1a1` for dialysis, `…0700` for OT; 30 s budget; run twice on a fresh database):

- `reprocessable-devices-dialysis.deep.test.js` — **DEPENDS ON Phase 1**: behind `describeIfPhase1` (the resolver module resolves on this checkout), which under `RPD_REQUIRE_PHASE1=1` (release CI) fails instead of skipping (§7.3); never run against a stub. Capture mints, dedicates, freezes the `Decision` as `reuse_screen`, writes `ready_processing_event_id` NULL for the new dialyser and `capture_provenance = 'live'`; **end of use**: the record on an `in_progress` session is refused (`RPD_USE_NOT_ENDED`) and accepted after `completeSession`; a patient with no serology (`unknown + warn`) is refused reprocessing without an acknowledgement and accepted with one (safety review written); **release criteria**: pass + TCV 85 % + agent in bounds releases, increments, writes the processing event, the usage's post-use event and `release_status = 'released'`; **missing baseline → `not_established`**: the usage settles as `released_not_established`, the device stays `awaiting_reprocessing`, the 418 row names `baseline_tcv` in `missing_evidence`; an **attempt** supplying the baseline under `first_measured_with_ic_signoff` releases and is attempt 1; the second session on the same patient captures at cycle 1 and, because the protocol requires it, refuses without `pre_use_residual_test: 'negative'` (`RPD_RESIDUAL_TEST_REQUIRED`), then accepts; a different patient is refused (dedication); a typed cycle count that disagrees is refused; a settled row refuses a different write; TCV 70 % discards with the right reason on device, usage and 418 row; **the matrix**: an HBsAg-reactive patient's dialyser is captured for its first use, the record offers discard only (`not_eligible_matrix`), `override_allowed` does not open it, the register row records it, the device carries `exposure_flag: true`, and **`JSON.stringify(deviceRow)` matches no marker word**; an HCV-reactive patient under a protocol with `hcv: dedicated_reuse` reprocesses with an acknowledgement, and under `hcv: no_reuse` does not; **lifecycle at `max_cycles` 1, 2 and unlimited** through capture → record → capture until the discard, asserting `max + 1` uses; **snapshot vs decision**: a reactive marker recorded after capture and before start reaches the machine assignment while the frozen `reuse_screen` still reads `unknown`, and one recorded before the record turns the disposition; a `clear` `Decision` is honoured whatever the marker's age (Phase 1's rule, through the adapter); nothing writes or asserts a `dialysis_patients.*_status` value; isolation: `UNREGISTERED` / `MISMATCH` / `UNMAPPED` under `warn` and `block`, `block` refused until the map is validated (`RPD_ISOLATION_BLOCK_PRECONDITIONS`), mismatch under `block` refuses at start **and at scheduling with no orphan row**, the emergency override from an in-charge proceeds past a block and writes its safety review while a technician's is 403, an `unknown` patient raises nothing; a machine label on the deny-list is refused, an unapproved label is refused; **cancel**: a scheduled session with `dialyser_condition: 'sealed_unopened'` releases the dialyser (`available`, cycle unchanged, usage `cancelled_before_use`, `reuse_count` nulled) and the same dialyser captures again at the same cycle; without the condition it is refused (`RPD_UNOPENED_CONFIRMATION_REQUIRED`); an `in_progress` session refuses (`RPD_RETURN_REQUIRED`); a recorded use refuses (`RPD_USAGE_NOT_CANCELLABLE`) and stays uncancelled; a session with nothing captured cancels as before; **a hold arriving while captured**: a late reactive marker on the dedicated patient places a `pending_return` hold on the `in_case` dialyser, and the cancel then lands it `quarantined`, not `available`; **the full hold cycle**: exposure → `quarantined` + active hold → capture refused (`RPD_HOLD_ACTIVE`) → IC release with adjudication and protocol → `awaiting_reprocessing` → an attempt releases → hold `satisfied`, `exposure_flag` still `true` → **re-capture succeeds**; **versions and receipts**: every command response carries `receipt.version`, a stale `expected_version` is refused (`RPD_VERSION_CONFLICT`) and the same key replays the same receipt; **the crash test** of §5.7; **race 1 and race 2** of §5.8 amplified 20×; the platform exposure handler writes nothing to `dialysis_patients`; `--compare-drop` on a patient whose only clearing evidence is a serology row (no difference) and one whose only restriction is a `'positive'` column (a difference); **no marker word in any audit row** the suite produced (a grep over `audit_logs.metadata` for the tenant); **RLS + grant probe** on all eleven tables, with the two append-only tables asserting `has_table_privilege(…, 'UPDATE') = false`.
- `reprocessable-devices-ot.deep.test.js`: issue under a policy mints from the set and captures with `instrument_set_id` and a NULL `ready_processing_event_id` (enrolled mid-life; the row says so); issue without a policy leaves no register row (dark); return on a clear patient → awaiting; load creation already `passed` (`createSterilizationLoad` with `completed_at` and three passed indicators) increments through `applyLoadOutcomeTx` exactly as a transition does, writes the processing event and the usage's post-use event; a metadata-only PATCH on that passed load increments nothing (exactly once); a load completed before the device's last return is `not_applicable`; a forbidden cycle type quarantines with a hold; the full exposure → hold → authorised release → processing → re-issue acceptance path succeeds while `exposure_flag` stays true; post-issue restriction and `serology_required` use distinct holds and usage dispositions; inspection failure remains separate from sterility evidence; a late failed load identifies affected devices/usages/patients, places holds including `pending_return`, writes alerts, and notifies infection control; the generic `reprocessed` OT arm requires the applicable load (`RPD_LOAD_REQUIRED`, `RPD_LOAD_SET_NOT_IN_LOAD`, `RPD_LOAD_NOT_APPLICABLE`), while the dialysis deep suite proves the same endpoint delegates to the statutory record or append-only attempt; cancel requires `sealed_unopened`, otherwise requires return; race 3 runs 20×; a late reactive event with `tested_on = NULL` uses the unbounded lookback; discard retires the set; the implant/load tenant FK is probed; every response is schema-validated; RLS and append-only grants are probed.
- `dialysis-isolation-census.deep.test.js` (no Phase 1 dependency; own tenant `…d1a2`): an orphan roster row counts once; a legacy `'positive'` with no marker counts once per `(patient, marker)` and once per patient; the older-row positive counts (`bool_or`); a marker-backed positive counts zero; a voided reactive does not clear it; a `'negative'` with a `dialysis_serology` non-reactive row counts as **corroborated**, one with no row as **defaulted**, and **`column_only_declared_negatives` is 0** against a fixture that tries every column value; `--integrity` reports a planted `current_usage_id` at a closed usage; a clean tenant reads all zeros; `--json` round-trips the same numbers.

**Mutation checks** (the PR #973 lesson — a test that cannot fail proves nothing): delete the dedication refusal → the other-patient test red; flip the holds/ceiling order in `computeDispositionOptions` → the discard-reason and parity tests red; make the reprocessing record read the frozen `reuse_screen` → the marker-after-capture test red; push a code for `unknown` in `computeIsolationWarnings` → the silence tests red; replace `bool_or` with `ORDER BY id DESC LIMIT 1` in the census → the re-enrolled test red; delete the `onSetReturnedTx` call → its pin red; delete `onSessionCancelledTx` → the dialysis cancel test and pin red; force `partialUse = false` → the `RPD_RETURN_REQUIRED` tests red in both suites; **remove the `applyLoadOutcomeTx` call from `createSterilizationLoad` only** → the discovery pin red on the population **and** the load-creation deep test red; **drop one regex from the discovery pin** → the pinned population count red (the pin cannot go quiet by shrinking); make `releaseCriteria` pass a missing baseline again → the `not_established` unit and deep tests red; remove the `hbsag` lock from `validateProtocolInput` (leave the CHECK) → the unit test red, and remove the CHECK (leave the validator) → the deep constraint probe red; make `issueSet` read `exposure_flag` instead of the holds → the re-issue acceptance test red; make the hold release clear `exposure_flag` → the "flag still true" assertion red; drop `expected_version` handling → the version-conflict test red; move `enqueueExposureEventsTx` after commit → the crash test red; **add `required_class: 'hbsag'` onto the session read's `isolation` payload** → the canary red **for every role in `D11_FIXTURE_ROLES`** (not the non-entitled subset), the resting `required_group: 'Bay 1'` green; **put `'Late reactive hcv'` into a stored `quarantine_reason`** → the canary's device-field check red and the rules' code-vocabulary check red; **poison `reuse_screen.reasons` and return the usage unprojected** → the canary red on the snapshot check; poison an `AppError.details` and relay it → red on the error-body check; and the population / positive-control mutations (14), (15a), (15b) of the plan's Task 8 with their exact one-red patterns. The deep-test contradiction of the earlier draft is gone: no test asserts a marker name on a device row, and the mutation that reintroduces a class → marker map fails the `exposureStamp` unit test and the deep suite's marker-word grep together.

**Seeder**: the comprehensive seeder runs twice on a fresh database (second run: 0 new rows); `db:contracts:seeded` green; the eleven new tables have overrides (a dialysis settings row with `reactive_patient_rule = 'discard'`; a `retired` protocol row with the locked matrix so the seeded policy can reference one while staying non-reprocessable; `reprocessing_domain_policies` `dialysis` / `dialyser` / not reprocessable; one dialysis device `available` with `version 1`; one **closed** usage; one link; one general machine; one `released` hold; one `chemical_reprocessing` processing event; one attempt with verdict `not_established`; one `delivered` outbox row).

**Gates**: lint chain **plus `check-dialysis-activation-gate.mjs`**; migration numbers / immutability / session-GUC; inline-check census static and `--verify-db` (manifest unchanged); `check:prisma-relations`; schema drift; OpenAPI check / core sync / lint budget; security stage; Flutter analyzer, widget tests, i18n guard and five-locale parity; Admin type-check, lint ratchet, format, suites; canonical `ci.yml` with `[full-ci]` on the last commit; the dialysis deep suite **run, not skipped** (`RPD_REQUIRE_PHASE1=1`).


## 9. Owner decisions — decided and pending

**Decided (recorded here so the history stays legible; the design above is written to them, not around them):**

- **D11 — NO** (owner, 2026-09-06, via the merge authority). A routing role may learn *that* a patient is isolated, never *which* marker. The full decision, its lettered consequences and the costing history are in §2 (D11); the shipped shape is §3.4, §4.1 and §4.6. **This is no longer a question and nothing in the lane waits on it.**
- **D10a / D10b / D10c — NO, each decided separately (§2).** `DIALYSIS_TECHNICIAN`, `BLOOD_BANK_STAFF` and `BLOOD_BANK_TECHNICIAN` do not join the serology-detail audience in this lane. The actual-assignment census remains mandatory because role non-assignability is not evidence of zero assignments.
- **Q1 — REVISED the same day (owner, 2026-09-06, via the merge authority): the legacy columns are asymmetric evidence.** A legacy `'positive'` restricts on its own; a legacy `'negative'` is not evidence — never an input to `clear`, never corroborated, never ANDed — and a row with only a `'negative'` and no marker or `dialysis_serology` row resolves `unknown`; invariant (d) is a positive fallback (§3.3). Phase 1 implements it; **migration 767 is unblocked** with this rule, not the literal one. Day-one amber is higher than the literal rule predicted and no blanket acknowledgement-cost claim is made (§7.3). `serology_validity_days` stays inert for dialysis (§4.1).
- **Two-phase carve.** Phase 1 owns the derivation, resolver and migration 767; Plan 4 consumes the resolver and owns the Phase 2 census. The absence of a `767_*` file never transfers its number or scope.
- **Owner-review clinical and lifecycle choices.** The whole platform register is marker-free; the hold-release and emergency role sets in §3.4/§5.5 are the design; HBsAg/HIV/mixed are database-locked to `no_reuse`, HCV defaults to `no_reuse` and may be `dedicated_reuse` only under an approved protocol; end-of-use is required before the reprocessing record; and infection control approves the non-clinical group vocabulary at tenant activation. These are stage 1 approval items, not open runtime switches beyond the explicitly bounded protocol fields.

**Existing non-blocking follow-ups, not new questions created by this revision:** the implant lane; eventual cath convergence; the final statutory dialyser-register form; upstream `facility_id` ownership; Q2 marker-void authority; and Q3's manual IC declaration/correction path for orphan roster identities. They remain outside Plan 4's implementation boundary and do not weaken the conservative defaults above.

Settled since round 1 (not an owner decision, recorded so the history is legible): `isolation_class` on the `Decision` is **agreed with dev-1b as an opt-in field** behind `includeIsolationClass` (§3.3), not the unconditional fifth field Plan 4 first asked for. dev-1b rejected that on the ground that a value of `'hiv'` is serology whatever the caller calls it — the "routing instruction" reframing is what produced two earlier disclosures — and the opt-in shape is the counter-proposal both lanes accepted. D11 = NO then removed the last user-facing use of a class; review 2026-09-06 added the reuse-eligibility path as a second in-process caller, so **two** server-side callers pass the opt-in flag (`assessIsolationTx`, `reuseEligibilityTx`), both pinned by name (§8), neither serialising, snapshotting or auditing the class.

## 10. Deferred / follow-ups

- Cath convergence per §2.6, including `legacy_tag`.
- A dialyser catalogue (`manufacturer` / `model_name` are free text); per-model default TCV and cycle ceilings would hang off it.
- `facility_id` upstream and the register's `facility_id` becoming NOT NULL.
- A recall **dashboard**, and a BI-to-patient lookback across loads *earlier* than the one that failed. **The minimum failure response is in scope since review 2026-09-06** (§5.2): holds on every device the failed load prepared (whatever their state), affected usages and patients listed, `cds_alerts` per patient, infection control notified. The register now knows which usages a load prepared (`ready_processing_event_id`) and which it reprocessed (`post_use_processing_event_id`), which is the data the wider lookback needs. The implant lane (`audit/cath-implant-lifecycle`, §1.1, §9.2) stays out.
- ~~An exposure outbox and the reconciliation sweep~~ — **in scope since review 2026-09-06** (§5.7; `bloodborne_exposure_outbox`, `drainExposureOutbox`, the scheduler job and the sweep's drain phase). What stays deferred is a richer notification body for the cath handler's alerts; the cath handler is already a consumer of the drained events.
- **The Phase 2 DROP** of `dialysis_patients.hbsag_status / hcv_status / hiv_status` and the generated `isolation_required`, once the census (§7.6) reads zero in every production tenant — a future migration outside this lane, with the blast radius stated in §3.3 (`dialysis_today`, the partial index, and the unrelated `infection_cases.isolation_required`).
- **Review round 1 follow-ups (recorded, not hidden):**
  - `POST /machines/ingest` tenant predicate — **closed on `main` by #1021** (`ingestMachineObservations` is tenant-scoped); the plan's Task 4 preflight is verify-only.
  - The backfill's exposure burst and its dry-run device count — handed to Phase 1 with the script (§3.3 hand-over notes).
  - The negatives lag and the correction path for a wrong legacy `'positive'` were properties of the sync; with no sync in this lane they are Phase 1's (Q3, §9).
  - `sterilization_failed` holds are cleared by `release`, not by the next passed load (§5.2) — by design; the friction is one click per failed load per device.
  - Staff has no cancel surface, so `RPD_RETURN_REQUIRED` / `RPD_USAGE_NOT_CANCELLABLE` reach only the Admin console and API clients; a Staff cancel, if one is ever added, must render both.
- **Carve-out follow-ups:** `isolation_class` on the `Decision` — **closed**: agreed with dev-1b as opt-in behind `includeIsolationClass` (§3.3); what a routing role may *see* was owner decision **D11**, now **decided NO** (§2, §9) — the machine layer is keyed on `isolation_group`; a dialysis validity window if a unit ever wants one (`serology_validity_days` is inert for dialysis today, §4.1); an amber `unknown` chip on the Staff today screen once Phase 1's roster chip exists (this lane shows only `isolation_required`). **New, from D11 = NO:** a `dialysis_machines.isolation_group` → group-registry table if a unit ever wants bays as first-class rows (today a group is a free-text label validated against the machines that carry it); and the Phase 2 DROP note now covers `isolation_class` never having existed on a machine row, so nothing has to be un-published.
- A single-use-device flag on OT items and a reprocessing ban keyed on it (single-use-device reprocessing is a separate regulatory question, §1.1; today only `procedure_pack` is refused by CHECK).
- Bloodlines as slots (a second usage per session) if a tenant ever reuses them (§1.1).
- The dialyser / model catalogue hanging off `reprocessing_protocols` (§4.2a): manufacturer, model, nominal TCV, the IFU document.
- A production response-validation middleware (the tests are the enforcement today, §6.6).
- Relaxing the three locked matrix cells is an owner decision (§9), not a follow-up.
- Priming volume and fibre-bundle-volume fields, if a tenant's form requires them.
- `dialysis_machine_qa_logs.machine_no → dialysis_machines` FK, and `dialysis_sessions.machine_no` becoming a FK once units have registered machines.
- The CSSD cycle-type picker and cath device-label endpoint are already closed on `main` by #1015; Plan 4 only extends their shared shapes to the platform register.
- Staff refusal messages localised (`ApiResponse.code` mapping); OPEN-21 linguistic review of the **300** new non-English lines (**75 keys × 4 locales**).
- The admin `Modal` focus trap (Plan 2 follow-up, exercised again by the new dialogs).
- `dialysis_today` view gaining `isolation_warning_codes` (the today board reads them from the session row for now).

## 11. Revision ledger — owner review of 2026-09-06 (PR #1014)

Every numbered point of the review and every additional release condition, the change made, and where it lives in this document (S) and the plan (P). Nothing in the architecture, D1–D11, the two-phase carve, the resolver contract or the canary discipline changed.

| # | Owner point | Change | S | P |
|---|---|---|---|---|
| 1 | D11 contradicted by the late-reactive handler; free-text reasons; canary coverage; D11 test population; group vocabulary; inference risk; deep test vs mutation | No `exposure_markers` column; `quarantine_reason` is a code; free text and the marker pointer live on the hold; the handler, the return, the record and the audit are marker-free for both domains; `reuse_screen` / `post_use_screen` sanitised on response incl. `reasons`; canary walks device fields, snapshots, error bodies and audit-derived responses; the D11 value test runs over the fixed `D11_FIXTURE_ROLES`; approved bay vocabulary + deny-list; inference risk stated; the deep assertion is flag + hold + no marker word | §2 D11(e), §3.2, §3.5, §3.6, §4.1, §4.3, §4.12, §7.2 | Task 1 (SQL), Task 2 (rules and projection), Task 3 (writers), Task 6 (canary), Task 8 (mutations) |
| 2 | Alternative release path via generic `/reprocessed`; append-only recovery after settlement; deactivation reopening the bypass; OT load not optional / must be the applicable load | `releaseToAvailableTx` is the one path to `available`; the generic command resolves the domain and delegates to the dialysis statutory record/attempt or the OT applicable-load command; `dialyser_reprocessing_attempts`; dark vs deactivated defined | §4.2, §4.14, §5.6, §6.2 | Task 1, Task 3 (`releaseToAvailableTx`, pin), Task 4 (attempts), Task 5 |
| 3 | Release did not restore reusability | `exposure_flag` is history; `reprocessable_device_holds` is the restriction; issue / capture check holds; IC release records adjudication, protocol and evidence; processing satisfies the release; acceptance test through re-issue | §4.12, §5.5, §5.1, §5.2, §8 | Task 1, Task 3 (`placeHoldTx` / `releaseHoldTx`), Tasks 4–5 (domain integration), Task 8 (deep acceptance) |
| 4 | Load creation hook missing; load evidence identity; both loads per usage; failure response | `applyLoadOutcomeTx` for creation and transitions with a discovery pin and a population snapshot; `device_processing_events` exactly once with the applicability witness; `ready_` and `post_use_processing_event_id` write-once; the minimum failure-response workflow now | §4.4, §4.13, §5.2, §6.2 | Task 1, Task 5 (handler, pin, deep tests) |
| 5 | Decision.status doing three jobs; same-patient exception; block escapes; unknown/old negatives; Q1 provenance; per-marker; prion | Three decisions / three functions; pathogen-specific matrix on an approved protocol with three DB-locked cells and HCV per protocol; the same-patient exception withdrawn; `block` refuses unregistered / unmapped, preconditions validated, emergency override an authorised workflow; Q1 as revised — asymmetric — as INPUT to Phase 1; per-marker `clear`; evidence date and surveillance currency separate; `prion_exposure` its own hold and release rule | §2, §3.3, §3.4, §4.2a, §5.3, §5.5 | Task 2 (`reuseEligibility`, protocol validation), Task 4 (eligibility, emergency route, preconditions), Task 9 (PR body) |
| 6 | Release criteria too permissive; category-level reprocessable insufficient; bloodlines / packs; OT sterility vs function | Missing evidence → `not_established`; baseline required with an approved mid-life alternative; TCV default 80 %, floor 80 %; integrity, agent bounds, contact time validated; residual test at capture; `reprocessing_protocols` required before a category is reprocessable; dialysis = `dialyser` only; `procedure_pack` never reprocessable; sterility and function evidence recorded apart | §1.1, §4.2, §4.2a, §4.5, §5.1, §5.2 | Tasks 1–2 (schema/rules), Tasks 4–5 (domain records), Task 7 (protocol form), Task 8 (deep tests) |
| 7 | Null class; null ceiling parity; cycle semantics; quarantine from `in_case`; acknowledgement point; disposition vocabulary | `assertDecisionShape` per status × flag; explicit non-null ceiling guard and a parity test scoped to the shared domain; `max_cycles` = reuses, `max + 1` uses, tested at 1 / 2 / unlimited; quarantine always after `return`; the post-issue acknowledgement point defined (hold, IC release); `quarantined_serology_required` and siblings | §2, §3.3, §4.3, §4.4, §5.1, §5.2, §5.3 | Task 2, Task 3, Task 4, Task 5 |
| 8 | Relationship integrity; bloodlines; end-of-use; affirmative unopened; hold while captured; retrospective capture | Composite FKs for `current_usage_id` (three-column), the 418 pair (device and session), usage ↔ schedule ↔ patient, usage ↔ issue ↔ set ↔ device; bloodlines removed; `RPD_USE_NOT_ENDED`; `unopened_confirmation` required; un-capture lands `quarantined` under an active hold; `capture_provenance = 'retrospective'` counted | §1.1, §4.3, §4.4, §4.8, §5.1, §5.2, §7.6 | Task 1 (SQL), Task 3 (`uncaptureDeviceTx`), Task 4, Task 5 |
| 9 | Crash window affects safety; `tested_on = NULL` lookback | `bloodborne_exposure_outbox` written in the marker's transaction; fast path + idempotent `drainExposureOutbox` from the scheduler, the #1017 sweep and an on-demand route; crash test; undated declarations use an unbounded lookback with IC review | §3.2, §4.15, §5.7 | Task 1, Task 3 (`exposureOutboxService`, writer transaction, scheduler/reconciler), Task 8 (crash test) |
| 10 | Census insufficient after Q1 | Counts every decision-bearing positive plus declared-and-evidence-backed negative observations and defaulted negative-shaped roster values separately, with `column_only_declared_negatives` asserted zero; the evidence row, not the negative column, decides; `--compare-drop` compares status **and** routing per patient; `--integrity`; gates the DROP, never `NNN` | §3.3, §7.6, §8 | Task 4 (census), Task 9 (hand-back report) |
| A1 | Lock order and three concurrency tests; receipts bound to lifecycle / version | `LOCK_ORDER` + pin; `version` on the register, `expected_version`, `RPD_VERSION_CONFLICT`, `receipt` on every command; races 1–3 amplified 20× | §4.3, §5.8, §8 | Tasks 1, 3–5 (implementation), Task 8 (races), Task 9 (evidence) |
| A2 | Release CI must fail when activation is possible and the resolver is missing | `check-dialysis-activation-gate.mjs` in the check chain; `RPD_REQUIRE_PHASE1=1` turns the deep skip into a failure; `dialysisActivationGuard.js` at boot | §7.3, §8 | Task 4 (guard), Task 8 (script, CI, required deep suite) |
| A3 | Name and test the response validator | `tests/helpers/assertSchema.js` (`assertResponse` / `assertData`) + `assertErrorBody`; the contract test over every operation and error body; the canary harness validates every body it walks; no production middleware, stated | §6.6, §8 | Task 6 |
| A4 | D10 per role with an actual-assignment check | D10a/b/c; `role-assignment-census.mjs`; the PR body quotes counts | §2, §7.6, §9 | Task 6 (role gates), Task 9 (assignment report) |
| — | Three-stage approval model | Written into §1.2 and the hand-back task | §1.2 | Task 9 |
| — | Stale baseline | `github/main` at `db30fe80b`; every code claim re-verified by function name; #1015 / #1017 / #1018 / #1021 / #1022 / #1024 / #1025 attributed correctly | header | header, Task 0 |

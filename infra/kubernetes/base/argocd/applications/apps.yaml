# argocd/applications/apps.yaml — Argo CD Application for the VH Health
# workload apps (backend, admin) deployed into the `vhhealth` namespace.
# Source path is infra/kubernetes/apps; manifests inside set their own ns.
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vhhealth-apps
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: vhhealth
    app.kubernetes.io/managed-by: kustomize
spec:
  project: vhhealth
  source:
    repoURL: https://github.com/Bahuleyandr/VH-Health-Platform
    targetRevision: HEAD
    path: infra/kubernetes/apps
  destination:
    server: https://kubernetes.default.svc
    namespace: vhhealth
  syncPolicy:
    # Deploy-safety (audit 2026-06-18): prod sync is MANUAL — NOT auto-tracking
    # `main` HEAD. A merge to main no longer auto-deploys to the live hospital
    # cluster; an operator triggers sync deliberately after review. Re-enable
    # `automated` only against a pinned release tag once a release-promotion
    # flow exists (do not point automated sync at a moving HEAD).
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 5

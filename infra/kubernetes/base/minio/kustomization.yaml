# minio/kustomization.yaml — MinIO tenant + operator marker
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: vhhealth-minio
resources:
  - operator.yaml
  - tenant.yaml

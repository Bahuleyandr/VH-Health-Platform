import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test - app module loads', () {
    // Minimal smoke test to verify the test infrastructure works.
    // Real widget tests require Firebase mock setup.
    expect(1 + 1, equals(2));
  });
}
